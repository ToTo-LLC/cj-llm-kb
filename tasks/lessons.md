# brain — Lessons Learned

> Running log of corrections, patterns, and rules. Updated after any user correction, any plan step that went sideways, and any subagent that needed re-dispatch. Review at the start of every session and before authoring each new plan.

## How to use

- Each entry is a short rule with context: **what went wrong**, **why**, **what to do instead**.
- Rules are scoped: project-wide, per-agent, or per-sub-plan.
- Obsolete lessons are struck through with `~~` rather than deleted, so the history is preserved.
- New entries include the date (YYYY-MM-DD) they were added.

## Project-wide

### Deferred architectural questions

- **2026-04-14 — iCloud + uv's `editable=false` workaround creates ghost `* 2.py` / `* 3.py` duplicate files in `.venv/lib/python3.12/site-packages/brain_core/`.** After many `uv sync --reinstall-package brain_core` cycles, Finder's iCloud sync fought with uv's wheel unpacking and produced Finder-style duplicate filenames (`writer 2.py`, `types 3.py`, etc.) inside the installed wheel. These dragged `pytest --cov` reported coverage from 94% down to 78% by adding ~330 lines of "0% covered" phantom code. They are NOT in `src/` and do not affect runtime behavior. **Fix:** `find .venv -name "* [0-9].py" -delete`. **Rule:** if coverage suddenly drops with no source changes, suspect ghost files first. Long-term fix is Plan 01's follow-up to flip `editable = true` once uv's hidden `.pth` bug is resolved. Raised by Task 24 sweep.

- **2026-04-14 — `ClassifyOutput.domain` is a hardcoded `Literal["research","work","personal"]`.** Custom user domains will fail pydantic validation, which the pipeline catches as a generic FAILED with an opaque message. Deliberate for now because the three-domain model is baked into CLAUDE.md (`personal` as a privacy rail), but revisit when user-configurable domains land. **Decision: defer** — tracked for a future config-driven domains task. Raised by Task 17 code quality review.

- **2026-04-14 — `failures.record_failure` silently overwrites on slug collision.** `<slug>.error.json` is keyed only by slug, so a retry destroys the prior failure record. For a single-user tool this is low-stakes, but a `"{slug}.{timestamp}.error.json"` naming convention would preserve retry history. **Decision: defer** — touches the Task 12 `failures.py` module, small follow-up. Raised by Task 17 code quality review.

- **2026-04-14 — Plan 02 handler-hardening sweep COMPLETE.** The 10-item deferred backlog accumulated during Plan 02 Tasks 3–10 was consolidated into three commits after all 8 handlers landed:
  - **Batch 1** (`f36f223`) — targeted behavior fixes: `pdf.py` context-manager via `with fitz.open(...)`, `url.py` raise `HandlerError` on empty trafilatura body, `email.py` tightened `can_handle` (require `@` in From) + removed dead `else` branch, `transcript_vtt.py` `\n\n` separator + dropped `str(spec)`, `transcript_docx.py` dropped `str(spec)`.
  - **Batch 2** (`cd8aad0`) — error-wrapping sweep: raw library exceptions wrapped in `HandlerError` with plain-English "next action" messages across text/url/pdf/transcript_text/transcript_vtt/transcript_docx/tweet. Exception classes used: narrow where available (`UnicodeDecodeError`, `httpx.HTTPStatusError`/`RequestError`, `fitz.FileDataError`, `webvtt.errors.MalformedFileError`/`MalformedCaptionError`, `ValueError` for `json.JSONDecodeError`), bare `Exception` only for `transcript_docx` (python-docx raises heterogeneous types from `zipfile.BadZipFile` to `KeyError`). `email.py` intentionally skipped — stdlib `email.message_from_string` is deliberately forgiving and never raises. All `raise ... from exc` chains preserved.
  - **Batch 3** (`f9553b9`) — test strengthening: new parametrized `test_handler_extract_guards.py` exercises every handler's `extract` bad-input guard (13 cases across 8 handlers), `TranscriptTextHandler` stem-guard tests, `TweetHandler` archive-path assertion.

  Final state: **112 passed, 1 skipped** (up from 88), mypy strict + ruff clean. **Items still deferred:** `can_handle` async→sync Protocol change (out of sweep scope — consult user before touching Protocol); URL `timeout` / PDF `min_chars` config injection (deferred to Task 17 orchestrator); plan-authoring retrospective notes (Task 5 fixture sizing, Task 10 title spec bug); dispatcher ordering contract (enforced in Task 11 test); Plan 01 UndoLog parser and VaultWriter rollback (unrelated to ingest).

- **2026-04-13 — UndoLog parser uses `END_PREV` sentinel, ignores written `PREV_LEN` byte-count (Task 14 nit).** If a `prev_text` line happened to be exactly `END_PREV`, the parser would terminate early. Currently safe because none of our content will contain that literal, but the parser should be hardened to slice by the `PREV_LEN` byte count instead of scanning for a sentinel. **Decision: defer** — low-priority robustness fix, no failure case under the current vault content policy. Revisit if user notes are imported that contain arbitrary text blobs.

- **2026-04-13 — VaultWriter rollback hardening (Task 13 nits).** The paranoid review of `VaultWriter` flagged three non-blocking robustness issues that are legitimate but not urgent: (1) individual rollback steps aren't wrapped in try/except, so a mid-rollback failure aborts remaining reverts; (2) undo records are only persisted after all mutations succeed, so a process crash between `_atomic_write` calls and `_write_undo_record` leaves no on-disk undo; (3) `Receipt.applied_files` is not cleared on rollback (moot today because the exception re-raises, but defensive hygiene). **Decision: defer** — all three require careful design (partial-undo semantics, crash-recovery ordering) that benefits from real-world stress testing first. Revisit after Plan 02 exercises `VaultWriter` at ingest volume. The `log_entry` newline-sanitization nit (finding #1) WAS fixed inline during Task 13 with a regression test.

- **2026-04-14 — `SourceHandler.can_handle` declared `async` but is required to be network-free / side-effect-free — the `async` is vacuous.** Every call site has to `await` a pure string/path check. More importantly, future handlers copying the pattern may wrongly assume `can_handle` is a safe place for lightweight I/O. **Decision: defer** — changing the Protocol affects Task 1 (`base.py`) and would propagate to 7 handlers. Revisit as part of the dispatcher (Task 11) work or as a standalone cleanup once all handlers land. Raised by Task 4 code quality review.

- **2026-04-14 — URL handler silently returns empty `body_text` when trafilatura extracts nothing.** `trafilatura.extract(...) or ""` hides the difference between "extracted zero-length" and "extraction failed" (JS-rendered pages, login walls, pure nav chrome). Downstream the LLM summarize step gets garbage input with no clear signal. **Decision: defer** — should be folded into the cross-cutting `HandlerError`-wrapping sweep so all handlers get consistent "no readable content" semantics. Raised by Task 4 code quality review.

- **2026-04-14 — Plan 02 Task 5 fixture spec was internally inconsistent.** Plan's suggested fixture content (`'Plan 02 PDF fixture\nParagraph one.\nParagraph two.'`, ~49 chars) is below `PDFHandler`'s default `min_chars=200`, so the happy-path test would spuriously raise `ScannedPDFError`. Implementer (correctly) expanded the fixture to ~295 chars preserving the required assertion substrings. **Rule for plan authoring:** when a handler has a configurable threshold AND the happy-path test uses a fixture under the threshold, explicitly size the fixture to clear it. Revisit during plan-authoring retrospective.

- **2026-04-14 — Plan 02 Task 10 spec bug: title template didn't match test assertion.** Plan's `f"Tweet by {display}"` with fixture `display="Andrej Karpathy"` produces `"Tweet by Andrej Karpathy"` — the test assertion `"karpathy" in es.title` is case-sensitive and matches "Karpathy" with capital K, NOT the lowercase "karpathy". Implementer fixed inline by adding `@screen_name` into the title (e.g. `"Tweet by Andrej Karpathy (@karpathy)"`). **Rule for plan authoring:** run the test's string assertions against the test's fixture values by hand when writing plans — case-sensitive substring checks are a frequent spec bug source.

- **2026-04-14 — TweetHandler test omits `archive_path.exists()` assertion that URL handler test has.** Archive-write is production-relevant behavior; the tweet test relies on no regression protection. **Decision: fold into the post-Task 10 test-strengthening sweep** — add archive assertions to all handler tests consistently. Raised by Task 10 review.

- **2026-04-14 — Tweet handler must be dispatched BEFORE URL handler; the ordering contract lives in the dispatcher, not the handlers.** Tweet URLs also match URLHandler's `can_handle`, so if dispatcher iteration order is wrong, tweets silently get trafilatura-scraped. Task 11's `_default_handlers()` list already places Tweet before URL (per plan lines 1368–1378) and the dispatcher test `test_dispatch_tweet_url_picks_tweet_handler_before_url_handler` enforces it. No action here; just document the contract so future handler additions don't break it. Raised by Task 10 review.

- **2026-04-14 — TranscriptVTTHandler joins cues with `"\n"` while TranscriptDOCXHandler joins paragraphs with `"\n\n"`.** Inconsistent across handlers, and the VTT choice collapses intra-cue newlines into inter-cue separators — LLM loses "continuation of same utterance" vs "new speaker turn" distinction. **Decision: fix in the post-Task 10 handler-hardening sweep** — standardize all handlers on `"\n\n"` paragraph separators and re-verify test assertions still pass (should, since they use substring containment). Raised by Tasks 7–9 code quality review.

- **2026-04-14 — Redundant `str(spec)` conversions in transcript_vtt.py and transcript_docx.py.** `webvtt.read`/`webvtt.from_srt` and `docx.Document` all accept `Path` directly at runtime. The `str()` wrap is spec-verbatim but inconsistent with `text.py`'s direct `spec.read_text()` style. Minor cleanup — fold into the same sweep. Raised by Tasks 7–9 code quality review.

- **2026-04-14 — TranscriptTextHandler's `"transcript" in spec.stem.lower()` stem guard is the single load-bearing line that separates it from TextHandler, and it is untested.** Task 11 dispatcher tests will implicitly exercise it (`hello.txt` must route to TextHandler, not TranscriptTextHandler), but a direct can_handle-true and can_handle-false pair is trivial to add. **Decision: defer to the post-Task 10 test-strengthening sweep** — same pass that adds extract-on-bad-input tests across all handlers. Raised by Task 7 code quality review.

- **2026-04-14 — EmailHandler body-extraction has dead `else` branch copied from plan spec.** For non-multipart messages, `msg.get_payload(decode=True)` always returns `bytes` (never a non-bytes value), so the `if isinstance(payload, bytes) / else` inside the non-multipart branch has an unreachable fallback. Test passes because the happy path hits the bytes branch. **Decision: defer cleanup** — spec is verbatim, behavior correct. Revisit as part of post-Task 10 handler-hardening sweep. Raised by Task 6 code quality review.

- **2026-04-14 — EmailHandler `can_handle` heuristic has false-positive risk.** Accepts any string whose first 10 lines contain `from:`, `to:`, `subject:` as headers — a pasted meeting-notes doc or Markdown snippet with those tokens would be misrouted. Cheap fix: also require the `From` value to contain `@`. Not fixed here because deviating from the spec's heuristic could ripple into Task 11 dispatcher tests. **Decision: defer to handler-hardening sweep post-Task 10**, then tighten all `can_handle` heuristics at once. Raised by Task 6 code quality review.

- **2026-04-14 — Handlers that open external resources (fitz, docx) should use context managers when the library supports them.** `PDFHandler` uses explicit `try/finally` per the plan spec. `with fitz.open(spec) as doc:` is cleaner and eliminates the split open/close. Not a bug (no leak: if `fitz.open` raises, nothing was opened), but worth unifying as part of the same cross-cutting sweep that wraps handler errors in `HandlerError`. **Decision: defer** with the HandlerError sweep at end of Task 10 / before Task 11. Raised by Task 5 code quality review.

- **2026-04-14 — Handler tests don't exercise `extract`'s own guard (non-Path / missing file).** `TextHandler` and `PDFHandler` both have `if not isinstance(spec, Path) or not spec.exists(): raise HandlerError(...)` but only `can_handle` is tested against that case. The `extract` guard is dead-tested code. **Decision: defer to a cross-cutting test-strengthening pass** once all handlers land, so all get the same parametrized "extract-on-bad-input raises HandlerError" test at once. Raised by Task 5 code quality review.

- **2026-04-14 — URL handler fetch params (`timeout=30.0`) are hardcoded magic numbers.** Should come from `brain_core.config` once handler-layer config injection is designed. **Decision: defer** — config plumbing for handlers isn't in Plan 02. Revisit when the pipeline orchestrator (Task 17) starts passing config through. Raised by Task 4 code quality review.

- **2026-04-14 — Task 4 implementer duplicated `respx>=0.21` into `packages/brain_core/pyproject.toml` `[project].dependencies` when it was already in the root `[dependency-groups].dev`.** Root cause: the implementer hit an import error during TDD, assumed the dep was missing, added it at the first place that worked, and didn't check the root. **Rule for dispatching future handler tasks:** when a task needs a test-only library, include in the prompt "before adding any dep, check root `pyproject.toml` `[dependency-groups].dev` — dev/test deps live there, not in package `pyproject.toml` files." Fixed in commit `1e3c244`.

- **2026-04-13 — Cross-cutting: wrap handler read errors in `HandlerError` with plain-English messages (Task 3 nit).** `TextHandler.extract` calls `spec.read_text(encoding="utf-8")` which raises a raw `UnicodeDecodeError` on non-UTF-8 input; project principle is "plain English with a next action." Same concern will apply to every handler that touches bytes (pdf, email, transcripts, docx). **Decision: defer to a cross-cutting sweep** after 2–3 more handlers land — batch the `try/except → HandlerError` wrapping so the pattern is consistent across all handlers rather than bespoke per file. Revisit at the end of Task 10 (tweet handler) or before Task 11 (dispatcher).

- **2026-04-13 — Code quality reviewer misread registration pattern (Task 3).** Reviewer flagged `TextHandler` never calling `register()` as "blocking." Not a bug: Task 11's dispatcher instantiates handlers explicitly via `_default_handlers()` — the `HANDLERS` list / `register()` in `base.py` is unused by the plan. **Rule for future reviewer prompts:** include the dispatcher's intended wiring (or a pointer to it) in the review context so reviewers don't chase phantom registration gaps. Also: consider whether `base.py`'s dead `register()`/`HANDLERS` should be removed in a cleanup pass once the dispatcher lands — deferred, low priority.

- **2026-04-13 — Does `anthropic` belong as a direct dep of `brain_core`, or should the LLM layer be split into a separate `brain_llm` package?** Current layering (per Plan 01): the Anthropic SDK is a `brain_core` dep but is only imported from `brain_core/llm/providers/anthropic.py` (enforced via CLAUDE.md rule). This keeps the `LLMProvider` abstraction clean but means `brain_core` has a production SDK dep. A future split would isolate providers into `brain_llm` so `brain_core` stays provider-free. **Decision: defer.** Revisit if a second provider is added (OpenAI, local) and the layering starts to creak. Raised by Task 2 code quality review.

## Per agent

### brain-core-engineer
_none_

### brain-mcp-engineer
_none_

### brain-frontend-engineer
_none_

### brain-ui-designer
_none_

### brain-prompt-engineer
_none_

### brain-test-engineer
_none_

### brain-installer-engineer
_none_

## Per sub-plan

### Plan 01 — Foundation

- **2026-04-13 — Root `pyproject.toml` needs `dependencies = ["brain_core"]` to actually install workspace members into the venv.** `[tool.uv.sources] brain_core = { workspace = true }` only tells uv *where* to resolve `brain_core` from; the root project still has to depend on it. Task 1 of Plan 01 omitted this; Task 2's implementer fixed it by adding the dependency. **Rule for future scaffolding tasks:** when adding a new workspace member, update the root `pyproject.toml` dependencies in the same commit. **How to apply:** before marking a workspace-member-introducing task complete, verify `uv run python -c "import <new_pkg>"` works.

- **2026-04-13 — macOS + uv 0.11.6 + Python 3.12.4 editable install bug**: uv writes the editable `.pth` file (`_brain_core.pth`) with the macOS `UF_HIDDEN` flag set, and Python's `site.py` explicitly skips hidden `.pth` files ("Skipping hidden .pth file"). Result: editable install silently fails to expose the package on `sys.path`. **Workaround in use**: set `editable = false` in `[tool.uv.sources]` so uv installs a built wheel instead. **Downside**: source edits to `brain_core` require `uv sync` to propagate — this will bite during rapid iteration. **How to apply:** if `import brain_core` ever fails after `uv sync`, check `.venv/lib/python3.12/site-packages/` for a hidden `_brain_core.pth` first. **Follow-up TODO:** monitor uv releases for a fix and flip `editable` back to `true` when available; or evaluate switching to a hatchling editable redirector.

- **2026-04-13 — Non-editable install requires explicit reinstall when adding new submodules.** Corollary of the editable=false workaround above. When a new subpackage (e.g. `brain_core.config`) is added under `packages/brain_core/src/brain_core/`, the installed wheel does not see it until you run `uv sync --reinstall-package brain_core`. The symptom is `ModuleNotFoundError` from the fresh test file even though the source is correct. **Rule for every TDD task that adds a new submodule:** run `uv sync --reinstall-package brain_core` after creating the source file and before running the tests for the first time. Hit during Task 4 execution.

- **2026-04-13 — Use `enum.StrEnum` for string-valued enums, not `class X(str, Enum)`.** ruff UP042 flags the legacy `(str, Enum)` pattern in Python 3.12. `StrEnum` is semantically equivalent for our use (.value still returns the string, comparisons work) and was added in 3.11. **Rule:** for any enum where every member's value is a string, use `from enum import StrEnum` and `class X(StrEnum):`. Hit during Plan 02 Task 1.

- **2026-04-13 — Run ruff in every task, not just at sweep time.** Plan 01 Tasks 1–20 never invoked `ruff check` or `ruff format`, so Task 21's sweep found 49 lint errors + 9 format diffs accumulated across 18 files. All were mechanical (UP017 `timezone.utc`→`datetime.UTC`, UP037 unquoted annotations, I001 import order, C408 `dict()`→`{}`, RUF003 unicode math symbols, UP040 `TypeAlias`→`type`). **Rule for every future TDD task:** include `uv run ruff check . && uv run ruff format --check .` in the self-review checklist alongside pytest and mypy. Catching lint drift per-task prevents big-bang fix commits at sweep time.

- **2026-04-13 — Async generator methods in Protocols must use `def`, not `async def`.** Declaring `async def stream(self, ...) -> AsyncIterator[X]:` in a Protocol types it as a coroutine returning an AsyncIterator, which does NOT match an implementation that uses `async def stream(...): yield ...` (that's an async generator). mypy strict correctly rejects the mismatch. **Rule:** for async-generator methods in a `Protocol`, write `def stream(...) -> AsyncIterator[X]: ...` (no `async`), then implementations can be either `async def` + `yield` (generator) or `async def` returning an explicit `AsyncIterator`. Hit during Task 16.

- **2026-04-13 — Adding a 3rd-party Python dep also requires its type stubs for mypy strict.** Adding `pyyaml>=6.0` as a `brain_core` runtime dep was not enough — mypy strict raised `import-untyped` on `import yaml` until `types-PyYAML>=6.0` was added to the root dev group. **Rule:** when a task adds a new non-trivial runtime dep, also check whether the package has a `types-*` stub package and add it to root dev deps in the same commit. Not needed for packages that ship their own `py.typed` marker (most modern libs) or have inline types. Hit during Task 8.

- **2026-04-13 — pydantic `Literal` validation tests require `# type: ignore[arg-type]`.** When writing a runtime-rejection test like `Config(active_domain="marketing")` for a `Literal` field, mypy strict will (correctly) fail the test file before it ever runs because the string isn't in the Literal set. The standard idiom is `Config(active_domain="marketing")  # type: ignore[arg-type]` with a comment explaining it's a deliberate runtime-validation probe. **Rule:** any test that deliberately passes a statically-invalid value to exercise runtime validation must carry a `# type: ignore[<code>]` and a brief justification comment. Hit during Task 4.

- **2026-04-13 — Plan 01 complete.** 64 unit tests + 1 windows-skipped, 92% coverage on brain_core, mypy strict clean, ruff clean. Demo gate green: `uv run python scripts/demo-plan-01.py` prints 6 checks + `PLAN 01 DEMO OK`. Tagged `plan-01-foundation`.

### Plan 02 — Ingestion

- **2026-04-14 — Plan 02 complete.** 191 passed + 1 windows-skipped, **94% coverage** on brain_core (exceeds 85% target), mypy strict clean, ruff + ruff-format clean. Demo gate green: `uv run python scripts/demo-plan-02.py` ingests 5 source types (text, URL-mocked, PDF, VTT transcript, tweet-mocked) end-to-end with 10 LLM calls total, proves idempotency with zero additional LLM calls on re-ingest, prints `PLAN 02 DEMO OK`. Tagged `plan-02-ingestion`. 44 commits since `plan-01-foundation`.

- **2026-04-14 — Subagent-driven development gave ~15 task commits of clean first-pass implementations plus 4 targeted fixes from quality review.** The two-stage review rhythm (spec compliance first, then code quality) caught real issues: `_integrate` feeding JSON instead of markdown to the LLM (`746f44d`), `BulkImporter.apply` ignoring per-item classified_domain (`603dd6a`), prompts package hardening (`06023f6`), URL handler respx dup dep (`1e3c244`), dispatcher error-message match assertion (`76b834d`). None of these were caught by the spec reviewer — all by the code quality reviewer. **Rule:** do NOT skip the code quality review even when spec compliance is green.

- **2026-04-14 — Handler-hardening sweep was the right shape for deferred work.** Tasks 3–10 accumulated ~10 cross-cutting concerns that the user correctly chose to batch into a post-Task-10 sweep (4 batches, 5 commits). Advantages: consistent patterns across all 8 handlers (error wrapping taxonomy, context managers, test coverage), one mental model per concern rather than bespoke per-file, no half-finished handlers. Disadvantage: the deferred-items list grew to 10 lessons and became its own mini-backlog. **Rule for Plan 03:** when reviewers raise the same class of concern on 2+ consecutive tasks, prefer batching to a sweep rather than fixing inline; let the deferral list be the sweep plan.

- **2026-04-14 — Task batching tradeoff: Task 17 (pipeline orchestrator) was split into 17A (pure helpers) + 17B (orchestration) and that was the right call.** Landing helpers with unit tests first meant Batch B's end-to-end test failures (none happened) would point at orchestration, not helpers. Counter-evidence: Task 19 (bulk import) was NOT split and got a 10-test file on first pass — but the review caught a real per-item classification bug that a narrower split would have surfaced sooner. **Rule for Plan 03:** split tasks with ≥200 LoC or multi-stage logic; single-subagent OK for small modules but always include a regression test for the interface contract.

- **2026-04-14 — iCloud + uv `editable=false` workaround produces ghost `* 2.py` / `* 3.py` files in the installed wheel after repeated `uv sync --reinstall-package` cycles.** Phantom files added ~330 lines of "0% covered" code to pytest reports, dragging Task 24's coverage from 94% to 78%. Not a real regression — fix is `find .venv -name "* [0-9].py" -delete`. Long-term fix is Plan 01's deferred TODO to flip `editable = true` once the uv hidden-`.pth` bug is resolved. **Rule:** if coverage suddenly drops with no source changes, grep the venv for ghost files first.

- **2026-04-14 — VCR cassette infrastructure landed, recording deferred.** Task 20 landed `pytest-vcr` marker, `vcr_config` fixture with header redaction (`authorization`, `x-api-key`, `anthropic-api-key`), empty cassettes directory, and docs at `docs/testing/prompts-vcr.md` with the `skipif` test template. Tasks 21 (record) and 22 (contract assertions) deferred indefinitely — they need an `ANTHROPIC_API_KEY` and are not a merge gate per the plan. When a key is available, a future implementer can copy the template from the docs and land both in one sitting.

- **Plan 02 → Plan 03 handoff items:** (1) `ClassifyOutput.domain` is a hardcoded `Literal["research","work","personal"]` — revisit when configurable domains are needed. (2) `failures.record_failure` silently overwrites on slug collision — add timestamp suffix for retry history. (3) `timeout=30.0` in URLHandler is hardcoded — plumb via config once Task 17-equivalent orchestrator design for Plan 03 (chat) decides how. (4) Related-notes retrieval for `_integrate` is stubbed — lands in a later plan. (5) Tasks 21–22 VCR cassettes deferred.

### Plan 03 — Chat

- **2026-04-15 — Plan 03 complete.** 366 passed + 5 skipped, 91% total brain_core coverage, mypy strict clean in both packages, ruff + format clean. Demo gate green: `uv run python scripts/demo-plan-03.py` → `PLAN 03 DEMO OK` (7 gates: Ask mode, Brainstorm propose_note, Draft edit_open_doc, thread persistence, auto-title rename, idempotency, scope guard). Tagged `plan-03-chat`. 42 commits since `plan-02-ingestion`.

- **Coverage note.** `brain_core.chat.*` averages 96% (autotitle 94, context 100, modes 100, pending 99, persistence 99, retrieval 93, session 88, tools 93–100, types 100). `brain_core.state.db` 97%. `brain_cli.commands.chat` 58%, `brain_cli.commands.patches` 55% — below the 85% target because the interactive stdin paths are only exercised by the demo, not the unit tests. Rendering (`stream.py`) is 80%. Total brain_core 91% (down from Plan 02's 94% — the delta is almost entirely the Anthropic provider at 35%, unchanged from Plan 01, plus the chat session loop's streaming branches). Gap documented; no feature-freeze-breaking fixes in Task 25.

- **Subagent-driven development retrospective.** 25 tasks, 6 groups, 7 checkpoints. Main loop dispatched fresh `brain-core-engineer` subagents per task with spec-compliance review + code-quality review in parallel after each. Two-stage review caught consistency issues the spec reviewer missed: Task 10 `is` vs `==` spec bug, Task 14 brace escaping in wrong prompt section, Task 17 `set_open_doc` SYSTEM turn inconsistency, Task 18 `len == 4` fragility, Task 20 missing 📝 emoji. Most tasks landed on first try. Tasks 2, 3, 14, 17, 18, 19, 20 needed follow-up fix commits (all caught by reviewers, not by post-hoc debugging). Checkpoint cadence (after Tasks 3, 11, 14, 15, 18, 20, 25) let main loop pause for user review without stalling per-task progress.

- **Handoff items to Plan 04 (MCP server):**
  - `state.sqlite` ready to extend — add a new migration file in `packages/brain_core/src/brain_core/state/migrations/0002_*.sql` for any MCP-specific tables (tool_call audit log, rate-limit tracking, etc.) without touching `0001_chat_and_bm25.sql`.
  - `PendingPatchStore` is reusable by MCP's `brain_propose_note` tool — same file-per-patch queue format.
  - `BM25VaultIndex` retrieval is reusable by MCP's `brain_search` tool.
  - `LLMProvider.complete(request)` with `request.tools` works end-to-end; MCP's stdio tool surface can wrap it.
  - Chat thread format at `<domain>/chats/<id>.md` is the canonical persisted thread — MCP can read these for cross-session history if needed.
  - `ChatSession` is NOT reusable by MCP (Claude Desktop IS the chat in MCP world per spec §6). MCP exposes READ tools only.

- **2026-04-15 — uv `[project.scripts]` + `package = false` gotcha.** A workspace root with `[tool.uv] package = false` silently ignores `[project.scripts]`. Entry points must live in the packaged workspace member's own `pyproject.toml`. Discovered in Task 19. Relevant for Plan 04+ any time a new workspace member (brain_mcp, brain_api, brain_web) needs a console script.

- **2026-04-15 — PEP 561 `py.typed` marker needed from day one.** `brain_core` was missing `src/brain_core/py.typed`, causing 24 spurious mypy import-untyped errors when running mypy from `packages/brain_cli/`. Fixed in Task 20. Every new workspace member needs a `py.typed` marker file.

- **2026-04-15 — `editable = false` workaround bites source edits.** The Plan 01 workaround for uv's hidden-`.pth` bug means every source edit requires `uv sync --reinstall-package <pkg>` to propagate. Hit this during most of Plan 03's follow-up fix commits. Operational friction, no correctness concern. Still waiting on upstream uv fix.

- **2026-04-15 — Self-review mypy-from-wrong-cwd trap.** Implementers running `uv run mypy src tests` from the repo root (vs `packages/brain_core/`) hit "no mypy config, no issues found in 0 files" or phantom "pre-existing errors in unrelated files" and report false status. Task 8, 14, 16, 20 all tripped on this. **Rule for future plan execution:** self-review checklist must say `cd /Users/chrisjohnson/Code/cj-llm-kb/packages/brain_core && uv run mypy src tests` explicitly.

- **2026-04-15 — Plan-author API verification.** Plan 03 text referenced imagined APIs (`PromptLoader` class, `FakeLLMProvider.queue_response`) instead of the real Plan 02 shapes (`load_prompt` function, `.queue`). Implementers correctly adapted. **Rule for future plan authoring:** open the real source files and verify signatures before writing task prompts.

- **2026-04-15 — Plan-author spec bug: `is` vs `==` on round-tripped pydantic models.** Plan 03 Task 10 spec asserted `result.proposed_patch is env_obj` but `store.list()` re-reads from disk and reconstructs `PendingEnvelope` — identity equality is structurally impossible. Implementer flagged and relaxed to `==`. **Rule:** for any assertion involving an object that round-trips through serialization, use `==` not `is`.

- **2026-04-15 — Plan-author arithmetic traps: `len(turns) == 4` bypassed by slash commands.** Plan 03 Task 18 spec used `len(_turns) == 4` to detect "end of turn 2", but any SYSTEM turn appended by `switch_mode`/`switch_scope`/`set_open_doc` between turns 1 and 2 bypasses it (count becomes 5 at the moment the check fires, never matches again). Fixed to count USER turns. **Rule:** state-detection checks should count INVARIANT data (USER turns monotonically increase by 1 per real turn), not ALL data.

- **2026-04-15 — Two scope-sensitive deviations were approved.** `VaultWriter.rename_file` (Task 14a) and the `LLMProvider` tool_use extension (Task 15) both touched Plan 01/02 code. Both were additive, both preserved existing behavior, both passed regression with zero Plan 02 test changes. **Rule:** touching existing code is OK when the change is strictly additive, has a clear hard regression gate, and is documented in the plan as an explicit exception.

- **2026-04-15 — Cross-platform surprises from Task 22.** One finding: `_write_rename_undo_record` was missing `newline="\n"`, would have produced CRLF on Windows. Fixed with a regression test that monkeypatches `Path.write_text` to assert the kwarg. Zero other findings. Plan 03 code held up well to the cross-platform audit because every writer routes through `_atomic_write_text` (which already enforces LF) or `os.replace` (atomic on both OS).

- **2026-04-15 — Task 24 hardening sweep.** 22 deferred items grouped into 4 batches (A behavior, B tests, C comments, D defer). 3 commits landed (`3cbb8a6`, `9f1db05`, `197dec9`), +9 new regression tests. Batch D items (5 NICE-TO-HAVEs) deferred to Plan 07 or later without commit — documented in lessons.

### Plan 04 — MCP

- **2026-04-17 — Plan 04 complete.** 502 passed + 8 skipped across 3 packages (brain_core 368 + brain_cli 30 + brain_mcp 104). Total coverage 92%; by package: brain_core 94%, brain_cli 75%, brain_mcp 94%. mypy strict clean on all three packages, ruff + format clean, 0 ghost files. Demo gate green: `uv run python scripts/demo-plan-04.py` → `PLAN 04 DEMO OK` (14 gates: tool + resource discovery, list_domains, search, read_note, list_pending_patches baseline, propose_note→apply→undo, propose_note→reject, cost_report, config_get, scope guard, BRAIN.md resource, domain index resource, ingest stage-only, `brain mcp selftest` subprocess round-trip). Tagged `plan-04-mcp`. 44 commits since `plan-03-chat`.

- **Subagent-driven development retrospective.** 25 tasks grouped into 6 Groups + 6 Checkpoints (Checkpoints after Tasks 3, 9, 13, 19, 21, 24). Main loop dispatched fresh `brain-mcp-engineer` subagents per task. A few surprises from the MCP SDK integration:
  - **AnyUrl drift for resources.** The MCP SDK takes `AnyUrl` for resource URIs but `read_resource` hands back an `AnyUrl` object whose equality semantics differ from plain strings. Fixed in Task 8 by stringifying the URI once at the handler boundary and matching on string, keeping the hardcoded URI constants in each resource module plain strings.
  - **`raise_exceptions=True` exception-group pattern.** The lowlevel `Server.run` call surfaces handler exceptions as an `ExceptionGroup` — the top-level exception is an `AnyioExceptionGroup`, not the original error. Tests that assert on tool errors round-trip through the MCP server need to unwrap `.exceptions[0]` to get at the real `ScopeError` / `FileNotFoundError` / etc. Demo script does the same.
  - **Non-editable reinstall quirk.** The Plan 01 `editable = false` workaround still bites — every source edit needs `uv sync --reinstall-package <pkg>` before tests see it. Hit this 6+ times during Plan 04. Same operational friction lesson as Plan 03.

- **Plan-author API drift (Tasks 11, 12, 13).** Task 11's `IngestPipeline` signature in the plan skeleton differed from the real Plan 02 implementation; Task 12's `classify()` function signature was off (plan said class, reality is function); Task 13's `BulkImporter.plan()` kwargs didn't match reality. Implementers correctly read the real source and adapted. **Rule for future plan authoring:** before writing a task prompt that constructs or invokes an existing class, open `packages/brain_core/src/...` and paste the actual signature into the plan. Imagined APIs waste a review round per task.

- **Handoff items to Plan 05 (API):**
  - `brain_mcp.server.create_server` is reusable — the tool-dispatch pattern (18 tools registered from `tools/*` modules, each exposing `NAME / DESCRIPTION / INPUT_SCHEMA / async handle(arguments, ctx)`) maps cleanly to FastAPI endpoint registration. Reuse the `ToolContext` dataclass directly if the API needs the same primitives.
  - `brain_mcp.tools.base` (scope_guard_path, text_result, ToolContext, ToolModule Protocol) is pure enough to import from a FastAPI layer with zero MCP dependencies. Consider moving to `brain_core.tools.base` if Plan 05 wants it cleaner.
  - `brain_mcp.rate_limit.RateLimiter` / `RateLimitConfig` is pure stdlib — reusable as-is in any HTTP context.
  - `brain_core.integrations.claude_desktop` has no API counterpart, but its OS-aware config detection pattern (detect, backup, atomic-write, verify) is a direct model for future integrations (Cursor, Zed, web-browser extension).
  - `brain_mcp.tools.apply_patch` pattern (`envelope → writer.apply → mark_applied` with rollback on failure) is the canonical write-gate flow; Plan 05's `POST /patches/:id/apply` endpoint should look identical.

- **2026-04-17 — VaultWriter + relative-path bug went uncaught for 3 tasks.** `VaultWriter.apply(...)` passed `NewFile.path` / `Edit.path` straight to `scope_guard`, which calls `Path.resolve()` — resolving relative paths against CWD, not vault_root. The staging envelope serializes vault-relative paths (Task 16 design for portability), so the MCP `apply_patch` tool built a local `_absolutize_patchset` workaround. The CLI `brain patches apply` command had the same latent bug and no workaround — it shipped broken for `NewFile` patches (no test covered it). Task 25 Batch A fixed the root cause (`VaultWriter._absolutize(path)` now joins relative paths onto `vault_root` before `scope_guard`), deleted the MCP workaround as dead code, and added a CLI regression test. **Rule for future work:** a workaround in layer N over an API defect in layer N-1 is a red flag that the fix belongs in N-1. If the first use of an API needs wrapping, the next use will silently ship broken.

- **2026-04-17 — selftest subprocess env hardcoding silently tested wrong vault.** `brain mcp selftest` spawned `brain-mcp` with a hardcoded `env={"BRAIN_VAULT_ROOT": "~/Documents/brain"}`, even when the user had installed with `--vault /somewhere/else`. A false-pass failure mode: selftest would happily green-light a config that didn't actually match what Claude Desktop would launch. Fixed in Task 25 by reading the env dict from the installed `mcpServers.brain.env` entry via `read_config(config_path)`. **Rule for cross-process tests:** if command A (install) writes config C, test B (selftest) must read config C, not reconstruct defaults.

- **2026-04-17 — `_SETTABLE_KEYS` allowlist didn't match real schema.** `brain_config_set` advertised `budget.daily_cap_usd` and `active_domain` as settable. The real schema field is `budget.daily_usd` (the `daily_cap_usd` name never existed). `active_domain` is set per-session via `allowed_domains`, not via a persisted mid-session toggle — keeping it settable invited scope-guard confusion. Task 25 fixed the name and dropped `active_domain`. **Rule:** when an MCP tool exposes a config field, point the test at the real pydantic model and assert `hasattr(Config, field)` — otherwise typo-grade bugs leak into the client contract.

- **2026-04-17 — Cross-platform surprises from Task 23.** One finding: a `brain mcp install` test relied on `/bin/sh` resolving as a fake `brain-mcp` executable path — would have broken on Windows CI (no `/bin/sh`). Fixed to use `shutil.which("python")` which resolves on both OSes. Same class of finding as Plan 03 Task 22 (`_write_rename_undo_record` CRLF translation). Cross-platform audits keep catching exactly one leak per plan.

- **Handler-hardening + test-surface batching.** Task 25 itself bundled Plan 03's Task 24 (hardening sweep) and Task 25 (coverage + tag close) into one task because Plan 04's surface is thinner. That worked: 4 sweep commits + 1 close commit was the right cadence. **Rule for future plan closes:** if deferrals fit under 15 items, bundle sweep + close.

- **Plan 04 deferred items (not code-changed during Task 25, logged here for future plans):**
  - `BulkImporter.plan()` doesn't expose a `max_files` kwarg — MCP layer truncates post-classify, which wastes classifier calls on overflow items when the caller already knows the cap. Clean fix belongs in `brain_core.ingest.bulk`; not Plan 04 scope.
  - `_TOOL_BY_NAME` dict lookup in `brain_mcp.server` — currently linear scan over 18 modules. Fine at this size; revisit if the tool surface grows past 50.
  - `RateLimiter._Bucket` dataclass refactor — current `list[float]` bucket state works but mypy narrowing is weaker than a dataclass. Also unused `self._config` field and a `elapsed = max(0.0, now - last)` defensive guard for clock-backwards. Batched NICE-TO-HAVE — deferred with no correctness impact.
  - Pre-existing `rank_bm25` / `webvtt` mypy untyped-import errors from earlier plans — not Plan 04 scope; owned by Plan 02's BM25 wiring and Plan 02's transcript_vtt handler.
  - BM25 demo scaffolding IDF quirk (documented inline in the demo script) — demo-only workaround; not a production code path.
  - `brain_config_set` persistence — docstring explicitly flags "Plan 04: in-memory only — persistence lands in Plan 07." Unchanged; Plan 07 picks it up alongside the Settings page.

### Plan 05 — API

- **2026-04-21 — Plan 05 complete.** 685 passed + 11 skipped across 4 packages (brain_core 427 + brain_cli 30 + brain_mcp 99 + brain_api 129). Total coverage 92%; by package: brain_core 91%, brain_cli 75%, brain_mcp 90%, brain_api 94%. Per-module coverage targets all met: `brain_api.app` 100%, `brain_api.auth` 96%, `brain_api.errors` 100%, `brain_api.routes.tools` 100%, `brain_api.routes.chat` 89%, `brain_api.chat.session_runner` 86%, `brain_core.rate_limit` 100%. mypy strict clean on all four packages (dropped from 18 errors to 0 after Batch A typed WS helper), ruff + format clean, 0 ghost files. Demo gate green: `uv run python scripts/demo-plan-05.py` → `PLAN 05 DEMO OK` (14 gates: healthz, /api/tools discovery, authed list_domains, unauthed 403, evil-Origin 403, brain_search scope, brain_read_note, propose_note stage, apply_patch, personal scope 403, ingest rate-limit 429 + Retry-After, WS handshake, WS turn round-trip, WS reconnect turn_count>=1). Tagged `plan-05-api`. 38 commits since `plan-04-mcp` (25 execution + 8 authoring checkpoints + 5 Task 25 sweep).

- **Subagent-driven development retrospective.** 25 tasks grouped into 6 Groups + 6 Checkpoints (same shape as Plan 04). Main loop dispatched fresh `brain-mcp-engineer` subagents per task (role-overloaded: the agent originally scoped to brain_mcp inherited brain_api because the domain boundary is the same — "thin HTTP/WS wrapper over brain_core"). A few surprises worth calling out:
  - **TestClient WS ignores `base_url`.** `starlette.testclient.TestClient.websocket_connect` hardcodes `ws://testserver` regardless of the `base_url=...` kwarg on the TestClient. Every WS test must explicitly pass `headers={"Host": "localhost"}` or OriginHostMiddleware rejects the upgrade with 1008. Not documented in starlette; the first WS test lost 30 minutes to this before it was pinned in a file-level comment.
  - **Pydantic v2 discriminated-union syntax.** The typed WS client-message parser uses `Annotated[UnionOfMessages, Field(discriminator="type")]` — NOT `Union[...]` alone with a `class Config: discriminator = "type"`. The v1 syntax silently passes type-check but returns untyped `dict` at runtime. Task 18 caught this within a checkpoint because the test asserted field access on the parsed object.
  - **`asyncio.shield` + async-gen `aclose` ordering.** Task 21 discovered `ChatSession.turn`'s `finally` appends to `_turns` — if the WS route's outer `finally` cancels the task before `aclose` finalizes, `_turns` ends up empty and persist writes nothing. The fix is `await asyncio.shield(turn_gen.aclose())` inside `SessionRunner.run_turn`'s finally — `shield` detaches aclose from the outer cancellation so the generator's own finally runs to completion before the cancel unwinds the runner.
  - **`BaseHTTPMiddleware` silently skips non-HTTP scopes.** Task 8 originally used `BaseHTTPMiddleware` for OriginHostMiddleware. The HTTP-only tests passed, but WebSocket upgrades bypassed the check entirely — a real CSRF hole. Fixed by rewriting as a pure ASGI middleware that branches on `scope["type"] in ("http", "websocket")`. Lesson: any middleware that enforces policy must be ASGI-level, not `BaseHTTPMiddleware`-level, if the app exposes WebSockets.
  - **Non-editable reinstall quirk (same as Plans 03 + 04).** `uv sync --reinstall-package brain_api` (and `brain_mcp`) after every source edit. Hit this 8+ times during Plan 05. Same operational friction; the workspace still uses `editable = false` by design.

- **Handler extraction retrospective (Task 14).** Plan sketched moving `ToolContext` + `RateLimiter` + `scope_guard_path` + `text_result` from brain_mcp to brain_core so brain_api doesn't transitively depend on brain_mcp. Fear was that brain_mcp's 105 tests would need touching. Reality: the refactor was strictly additive — brain_core got the classes, brain_mcp got a 3-line re-export file, all 105 brain_mcp tests passed unchanged. ~38 new brain_core smoke tests landed alongside to cover the moved code. **Rule for future structural refactors:** if the abstraction already has tests at the old site, "move via re-export" is nearly always the least risky path; the test suite does the verification.

- **Plan-author API drift (Tasks 17–21).** Chat/WS task prompts consistently assumed APIs that differed from Plan 03's reality. Every implementer corrected them but the pattern is worth logging:
  - `ChatSession.turn(user_message) -> AsyncIterator[ChatEvent]` — plan said `run_turn`.
  - `ChatEvent` is a single Pydantic model with `kind: ChatEventKind` (StrEnum) + `data: dict[str, Any]`. Plan 05 sketches assumed per-kind classes (`DeltaChatEvent`, `ToolCallChatEvent`, etc.) which don't exist.
  - `ChatSession.__init__` takes `config: ChatSessionConfig` + a pre-built `ContextCompiler` + a populated `ToolRegistry`. Plan sketches listed flat kwargs (`allowed_domains=`, `mode=`, `writer=`, ...).
  - No `ChatSession.persist()` method — `ThreadPersistence.write(...)` fires automatically inside `ChatSession.turn`'s body. Plan 05 sketches assumed a caller-owned persist step.
  - No `load_or_create` — Task 21a added an `initial_turns=` kwarg to `ChatSession` so the WS route can rehydrate history from disk without a new constructor. Plan's `load_or_create` sketch was over-engineered for what was actually a 3-line builder change.
  **Rule (same as Plan 04 retro):** before writing a task that invokes an existing class, open the file and paste the real signature into the plan. Imagined APIs waste one review round per task.

- **Handoff to Plan 06 (UI design):**
  - **Frontend contract #1 — `GET /api/tools` (unauthenticated).** Lists every tool with `name`, `description`, `INPUT_SCHEMA` (JSON-Schema with `minimum`/`maximum`/`enum` honoured post-Task-25). Plan 06 mockups reference the tool list directly; frontend builds form inputs from the schema.
  - **Frontend contract #2 — `brain_api.chat.events`.** The WS wire format is pinned by `SCHEMA_VERSION = "1"` exported from that module. Every server event has a `type` string for the JSON discriminator. Client messages use the same discriminator. Frontend must parse both.
  - **Error envelope — `{"error": str, "message": str, "detail": dict | None}`.** Task 25 aligned middleware + route + 500 to the same shape. Frontend error boundary parses one shape for every 4xx/5xx. 429 responses also include a `Retry-After: <seconds>` HTTP header that the frontend should respect before re-issuing the request.
  - **Token — server-side only.** Next.js reads `<vault>/.brain/run/api-secret.txt` via an SSR route and attaches `X-Brain-Token: <hex>` before proxying to the API. Never expose to the browser (the file has 0600 on POSIX, token in-memory on NTFS).
  - **Concurrent WS per thread — last-writer-wins.** Nothing in brain_api prevents two tabs opening the same `thread_id`; each runner loads + writes the vault file independently. Plan 07 UI must enforce single-tab-per-thread (e.g. disable "new tab" when a WS is live on the same thread_id, or close the old WS on new-tab focus). Backend-side locking is out of scope.

- **Cross-platform surprises (Task 23).** The bare thread_id regex `^[a-z0-9][a-z0-9-]{0,63}$` accepted Windows-reserved device names (`con`, `prn`, `aux`, `nul`, `com1`–`com9`, `lpt1`–`lpt9`). Windows NTFS reserves these regardless of extension — a thread called `con` succeeds the handshake on Mac but silently fails every persist-to-vault write on Windows (the user's chat appears to work but nothing saves). Fixed in Task 23 via `_WINDOWS_RESERVED_NAMES` frozenset filter AFTER the regex match. Pinned by parametrized test that runs on Mac (where `con.md` is a valid filename) so future regressions can't slip through without a Windows runner.

- **2026-04-21 — Batch A fixes (Task 25 sweep).**
  - **JSON-Schema `minimum` / `maximum` / `enum` builder support.** `brain_api.schema.build_model_from_schema` silently dropped numeric range constraints for the full plan — the `brain_search.top_k = {"type": "integer", "minimum": 1, "maximum": 20}` schema was enforced only by the tool handler's runtime `min(top_k, _MAX_TOP_K)` clamp. Task 25 wired `minimum`/`maximum` to Pydantic `Field(ge=..., le=...)` and `enum` to `typing.Literal[...]`. Real-tool regression test pinned: `top_k=0` and `top_k=21` now reject at the dispatcher edge with a clean 400 `invalid_input`, no more silent clamp.
  - **Middleware 403 envelope shape parity.** `OriginHostMiddleware` emitted `{"error", "message"}` (two keys) while every 4xx from the route layer emits `{"error", "message", "detail"}` (three keys). Frontend error boundaries would have to branch on "middleware or route refusal?" to pull `detail` safely. Task 25 added `"detail": None` to the middleware body. Pinned by a new regression test asserting `set(body.keys()) == {"error", "message", "detail"}` on a Host-based refusal.
  - **Typed WS test helper (18 mypy errors → 0).** `TestClient.app` is typed as opaque ASGI callable per starlette stubs, not `FastAPI`; every `fresh.app.state.ctx.*` access in `test_ws_chat_*.py` tripped `[attr-defined]`. Extracted two helpers to `tests/_ws_helpers.py` (`get_app_ctx(client) -> AppContext`, `get_app_token(client) -> str` with None-narrowing assert) and replaced 18 sites across 4 files. Added `mypy_path = "tests"` to brain_api/pyproject.toml so bare-name import resolves under `uv run mypy src tests`.

- **Plan 05 deferred items (Batch C — not code-changed during Task 25, logged here for future plans):**
  - **Pytest `importmode=importlib` unification.** Task 22 (VCR cassette infra) relied on `explicit_package_bases = true` in brain_api and brain_mcp to avoid a duplicate `prompts` top-level package between `packages/brain_mcp/tests/prompts/` and `packages/brain_api/tests/prompts/`. A rootdir-level pytest `importmode = importlib` change would drop the special case and eliminate the need for bare-name imports like `from _ws_helpers import ...`. Defer to a future tooling sweep.
  - **Concurrent same-thread WS connections.** Plan 07 UI will enforce single-tab-per-thread; no backend lock needed. Documented in `routes/chat.py` endpoint docstring (Batch B).
  - **`ChatSession.turn_count` semantics — USER turns, not total.** Verified at Plan 05 close: `ChatSession.turn_count` returns the count of USER entries in `_turns`, not total entries. The WS `thread_loaded` frame's `turn_count` field reflects the same — "how many user messages have been sent on this thread." Ambiguous in the plan; now pinned by `test_reconnect_reports_real_turn_count` assertion `turn_count == 1` after exactly one user turn.
  - **`SessionRunner._ensure_session` remains synchronous.** The plan sketched an `async def _ensure_session` (matching `ChatSession.turn`'s async shape), but reality is synchronous — no awaitable ops in the path (`ThreadPersistence.read` is sync, `ChatSession(...)` is sync). Keeping it sync means the WS route can call it directly from the handshake path without `await`, which Task 17's handshake ordering requires. Pinned at the call sites.
  - **BM25 demo-scaffolding IDF quirk** inherited from Plan 04 demo script — same workaround, same defer. Not a production code path.
  - **Windows ACL for the token file.** `brain_api.auth.write_token_file` uses `os.chmod(0o600)` on Windows which only toggles the read-only bit; real lockdown needs `pywin32` `SetFileSecurityA`. Explicitly out-of-scope per the Plan 05 spec (no new deps); the `# TODO(Windows ACL)` comment in `auth.py` captures the deferral.
  - **IPv6 loopback support (`::1`).** `_LOOPBACK_HOSTS = frozenset({"localhost", "127.0.0.1"})` accepts IPv4 only. Defer until Plan 08 exposes IPv6 via uvicorn config; adding `::1` preemptively would create a false sense of IPv6 coverage the tests don't actually exercise.
  - **Request-id correlation for 500 bodies.** Plan 07 will add a request-id middleware that stashes an ID on `request.state`, includes `X-Request-ID` on every response, and expands the 500 body's `detail` to `{"request_id": ...}`. Deferred per `TODO(Plan 07)` in `errors.py`.
  - **`_TOOL_BY_NAME` for `brain_mcp.server`.** Same O(n) linear scan Plan 04 Task 25 deferred. brain_api already fixed this via `AppContext.tool_by_name`; the MCP side is a one-line change still waiting on its next sweep. Documented inline in `brain_mcp/server.py::handle_call_tool` (Batch B).
  - **VCR chat cassettes.** Task 22 built the VCR contract-test infrastructure but the three intended chat cassettes (Anthropic streaming responses covering Ask, Brainstorm, Draft) remain deferred. They require real Anthropic API credentials at record time; authoring without credentials is tracked per the D9a D-slot rationale inherited from Plan 03. Tests marked with `@pytest.mark.vcr` + `pytest.mark.skip` so they green-skip without a cassette.

### Plan 07 — Frontend

**Completed 2026-04-21, tag `plan-07-frontend`.** 25 tasks planned / 25 tasks shipped. 39 commits since `plan-05-api`. Test counts: brain_core 647 + brain_api 76 + brain_cli 29 + brain_mcp 72 = 824 pass + 11 skip (Python); brain_web 229 pass + 1 skip (Vitest) + 14 pass Playwright (5 e2e flows + 8 axe-core a11y sweep + 1 setup-wizard nav). 14-gate demo green (`PLAN 07 DEMO OK`). Final tool surface: **34 tools** (Plan 05 baseline 18 → +4 Task 4 → +10 Task 25 sweep).

**Subagent-driven-development retrospective (Next.js 15 + Tailwind + shadcn/ui + Zustand + React Query + Playwright + axe-core, all-new stack).** Plan 07 was the first plan that bolted a substantial client-side stack onto brain. The subagent cadence (one task per agent, fresh context, two-stage review) scaled better than expected — 25 agents built the full surface without drift because Plan 06's design-brief.md + CJ Knowledge LLM v3 mockups were authoritative. Tasks that tried to refactor primitives mid-plan (Task 11's `accent` color mapping, Task 17's folder-picker scheme) got caught in review before landing. The subagent pattern also made Task 25's sweep cheap: 10 new tools landed in one task because each tool is a <50-line self-contained file with unit test + shim + frontend wiring, and the agent had no context to preserve across tools.

**8 scope-expanded tools surfaced during frontend work.** Groups 3–5 surfaced tools not planned at Plan 05 close: `brain_get_pending_patch` (pending-detail fetch), `brain_mcp_install` / `uninstall` / `status` / `selftest` (Settings → Integrations), `brain_fork_thread` (fork dialog), `brain_set_api_key` (Providers panel), `brain_ping_llm` (test-connection button), `brain_backup_create` / `list` / `restore` (Backups panel), `brain_delete_domain` (Settings → Domains dangerous-delete). Deferring these to the Task 25 sweep rather than threading each through its originating task kept the frontend agent unblocked — the UI rendered empty states until the sweep landed, and the tool surface review happened in one batch instead of ten.

**Handoff to Plan 08 (install / packaging).** `pnpm build` in `apps/brain_web/` produces a production Next.js bundle under `.next/`. Plan 08's `brain start` should spawn `uvicorn brain_api:app --port 4317` + `pnpm --filter brain_web start` (port 4316) as sibling subprocesses, then open `http://localhost:4316` in the user's default browser. The per-run token file at `<vault>/.brain/run/api-secret.txt` is the only shared-state coupling — `brain_api.create_app()` writes it on boot and Next.js's `readToken()` reads it. Plan 08 inherits the `BRAIN_VAULT_ROOT` env propagation pattern Plan 07 established.

**Handoff to Plan 09 (ship).** Manual QA checklist at `docs/testing/manual-qa.md` covers the 5-flow Playwright set + fresh-install sweep + Claude Desktop round-trip. The axe-core AA gate is enforced by `tests/e2e/a11y.spec.ts` — Plan 09 should not relax it. WCAG 2.2 AA + 14-gate demo are both hard gates for v0.1.0.

**Cross-platform surprises (Task 24 findings).** (a) `⌘K` keyboard shortcut for the search overlay must map to `Ctrl+K` on Windows — `navigator.platform` sniff wired into `useEffect` handler. (b) Long Windows paths via `\\?\` prefix are NOT needed in the frontend proxy layer because the Next.js proxy passes absolute paths through unchanged; the scope_guard + VaultWriter already handle the prefix. (c) The setup wizard's `/chat` redirect was pre-rendered at `pnpm build` time and baked in the `token missing → /setup` branch; fixed inline via `export const dynamic = "force-dynamic"` on `/chat/page.tsx` and `/chat/[thread_id]/page.tsx`.

**BRAIN_E2E_MODE backdoor (Task 25C).** `FakeLLMProvider`'s "empty queue raises RuntimeError" contract is a load-bearing safety rail for unit tests (it catches programmer error when a test forgets to prime a response). But the Playwright specs + 14-gate demo run across a subprocess boundary — the test driver can't reach into the spawned `brain_api`'s in-memory `FakeLLM` to queue responses. Solution: opt-in env flag `BRAIN_E2E_MODE=1` flips the empty-queue fallback from "raise" to "return a prompt-aware canned response" (classify returns JSON domain=work, summarize returns a SummarizeOutput, integrate returns a valid PatchSet, everything else returns a greeting). Unit-test contract is preserved because the env is only exported by `start-backend-for-e2e.sh` / `.ps1` and the demo script. 6 new `tests/llm/test_fake.py` tests pin both branches.

**`uvicorn[standard]` needed for real WS upgrades.** Playwright's chat-turn spec hung indefinitely on "reconnecting…" because the spawned `brain_api` was running bare `uvicorn` (no `websockets` / `wsproto` transport library). In-process `TestClient` has its own WS shim via httpx + starlette, which is why every Python WS test passed. Pinned by bumping the dep from `uvicorn>=0.32` to `uvicorn[standard]>=0.32` in `packages/brain_api/pyproject.toml` — brings in `websockets` + `httptools` + `uvloop`. Without this, every CI Playwright run would fail at the first WS test.

**color-contrast token tweak (Task 25C).** Task 23 landed the axe-core a11y gate with `color-contrast` disabled pending a design review. Task 25C re-enabled it and nudged three tokens: `--text-muted` 0.60 → 0.70, `--text-dim` 0.38 → 0.58 (dark) / 0.40 → 0.56 (light), and `--accent: var(--surface-3)` → `var(--tt-cyan)` (accent was mapped to a #232323 dark-gray surface, making every `text-[var(--accent)]` link invisible at 1.17:1). All 8 a11y routes now clear WCAG 2.2 AA with 0 violations.

**Deferred items (still open after Plan 07 close).**
  - **Broken-wikilink detection** in Browse / patch diff views. The UI renders `[[slug]]` links as plain text with no "unreachable" affordance; surfacing broken links needs a lint pass over `state.sqlite`'s note index. Defer to **Plan 09**.
  - **PDF upload via multipart form** on `/api/proxy/upload`. The route rejects binaries with a 415 + "PDFs coming soon" toast. PDF extraction works server-side via `pypdf`; the frontend's multipart body needs a base64 or temp-file handoff. Defer to **Plan 09**.
  - **Thread list tool (`brain_list_threads`)** for the left-nav recent-chats panel. Task 20 stubs it via client-side state; a proper tool would let the MCP side list threads too. Defer to next sweep.
  - **Embeddings / semantic search.** Out-of-scope per spec §5; BM25 is the day-one retrieval surface. No plan yet.
  - **True desktop folder-picker** for bulk import on Mac / Windows. The browser `<input webkitdirectory>` reads metadata only; Plan 08's Electron wrap (if adopted) would expose `showDirectoryPicker()` via the system dialog.
  - **Bulk-import `plan` vs `items` response shape drift.** Latent bug: `brain_bulk_import` returns `data.items`, but `step-pick-folder.tsx` was reading `data.plan`. Task 25C fix: dual-read `data.plan ?? data.items`. Next sweep should pick one name and deprecate the other.

### Plan 08 — Install + Packaging

**Completed 2026-04-23, tag `plan-08-install`.** 12 tasks planned / 10 fully shipped + 2 harnessed-deferred (Tasks 10 + 11 clean-VM dry runs). 11 commits since `plan-07-frontend` (10 task commits + close). Test counts unchanged: brain_core 647 + brain_cli 59 + brain_mcp 72 + brain_api 152 = **930 pass + 16 skip** (Python); brain_web **224 pass + 1 skip** (Vitest); Playwright **14/14**. 11-gate demo green (`PLAN 08 DEMO OK`). Tool surface still **34** (install layer, no new tools).

**Static-export pivot retrospective (Group 0 — Tasks 1 + 2).** The biggest architectural decision of the plan: flip Next.js from `next start` (Node runtime) to `output: "export"` (SPA build) + serve via brain_api's StaticFiles mount. This dropped the user-facing runtime from two processes on two ports to one process on one port. What broke during the port: (a) every dynamic segment route (`chat/[thread_id]`, `browse/[...path]`, `settings/[tab]`) needed an explicit `generateStaticParams: () => []` + SPA fallback in brain_api's `SPAStaticFiles` subclass; (b) the Next.js `/api/proxy/[...path]` route disappeared, which meant every `apiFetch` call had to be rewritten to hit `/api/*` same-origin + read token from a new Zustand-backed bootstrap context instead of server-side; (c) the `/api/proxy/upload` multipart shim became the in-process `POST /api/upload` endpoint on brain_api (Task 1). What held: the token flow (server writes `<vault>/.brain/run/api-secret.txt`, client reads via `/api/token` on boot, attaches `X-Brain-Token` thereafter), the WS URL construction (now `ws://${location.host}/ws/...` — same-origin means no hardcoded port), the `apiFetch<T>` call-site shape, the Zustand store pattern for state. Net test count drift: zero — every Next.js server component dropped had tests that translated directly to brain_api unit tests. The pivot unlocked the rest of Plan 08: `brain start` supervises ONE uvicorn child, not two, and `brain doctor`'s final check is `<install>/web/out/index.html` presence instead of a "Node on PATH" check.

**Subagent-driven development retrospective (install-heavy plan).** `brain-installer-engineer` owned 6 of 12 tasks (3–6, 7, 8, 9). That's the highest per-agent task count across every plan to date, and it worked. The supervisor verbs (start / stop / status / doctor / upgrade / uninstall / backup) are thematically adjacent but behaviorally independent — each agent started from a clean context, read `paths.py` + `pidfile.py` once, and wrote a self-contained command module + runtime helpers + tests. Two-stage review (spec + code quality) still surfaced real issues: Task 5's upgrade swap needed Windows retry logic for file handle linger (hit in review), Task 6's uninstall typed-confirm defaulted to `Y` for vault removal in one draft (caught in spec review, flipped to default-N because vault is sacred), Task 7's install.sh had Bash 4+ syntax (`[[ -v ]]`) that breaks on macOS's stock `/bin/bash` 3.2. None of these would have been caught by the spec reviewer alone.

**fnm cross-platform gotchas (Tasks 7 + 8 — partial, deferred to VM sweep).** install.sh + install.ps1 both set up `fnm` to install Node 20 into `<install>/tools/fnm/` as a build-time-only toolchain. Landed pattern works on Mac + Windows by not touching global PATH — the install script eval's `fnm env` only within its own subshell, then calls `pnpm install` / `pnpm build` from that subshell, and exits cleanly. Known open risks (to be cleared in the clean-VM dry runs, Tasks 10 + 11, deferred to Plan 09 sweep): (a) fnm's Windows installer uses PowerShell's `irm | iex` pattern which can trip Defender SmartScreen on a truly pristine user machine; (b) fnm installs Node into a `<install>/tools/fnm/node-versions/v20.X.Y/bin/` subtree that uses Windows-style path separators even when fnm was run from Git Bash. Both documented in `docs/testing/clean-mac-vm-host-instructions.md` + the Windows equivalent; the VM receipts are the proof artifact.

**`editable = false` + new brain_cli submodule friction (continuous from Plans 03 + 04 + 05).** Every new submodule under `packages/brain_cli/src/brain_cli/runtime/` or `.../commands/` required `uv sync --reinstall-package brain_cli` before tests could see the new module. Tasks 3–6 each hit this at least once. The operational cost is a single re-run of `uv sync --reinstall-package brain_cli` (~500ms) per new-module test run. The underlying fix — flipping `editable = true` in the workspace `[tool.uv.sources]` — is blocked on the upstream uv hidden-`.pth` bug (still open as of uv 0.11.6). No new wrinkles specific to brain_cli beyond the pattern already documented from earlier plans; this is now a well-understood friction tax.

**VM dry runs deferred (Tasks 10 + 11 — harness-only).** Plan 08 Tasks 10 (clean-Mac VM) + 11 (clean-Windows VM) were scoped as real VM executions that capture timings + screenshots + deviations against a pristine OS image. Neither task actually ran a VM. What landed in commit `80d4b15`: `scripts/serve-local-tarball.py` (tiny http.server for serving `brain-dev.tar.gz` + `install.sh` to a VM over a host-only network), `docs/testing/clean-mac-vm-host-instructions.md` + the Windows equivalent (step-by-step host-side commands), `docs/testing/clean-mac-vm-receipt.md` + the Windows equivalent (templates with blank slots for timings / screenshots / deviations). The actual VM executions are the gate for a **clean v0.1.0 ship** — they surface the last class of bugs no local demo can catch (Defender SmartScreen prompts, first-boot PATH propagation, browser launch against a restricted default). Handoff to Plan 09 as a pre-release sweep item. The demo gate (Task 12) runs on the developer's dev box against a tmp dir, which exercises the install.sh flow + supervisor + uninstall round-trip but not the OS-image freshness.

**Handoff to Plan 09 (ship).** Topics: (a) cut the first real GitHub release tarball (update `BRAIN_RELEASE_URL` default in install.sh + .ps1 from the placeholder to the real release URL, wire SHA256 into the release description per the `SHA256: <64-hex>` line convention), (b) run the VM dry runs on clean Mac 14 (Tart) + Windows 11 (UTM or Parallels) using the Task 10/11 harness, (c) version bump to v0.1.0 + update the `VERSION` file written by install.sh, (d) release notes automation (suggested: git log filter since `plan-07-frontend` grouped by task prefix), (e) complete the manual-QA checklist at `docs/testing/manual-qa.md` on the user's primary machine, (f) real-user-machine QA sweep: drag-drop 5 real sources, bulk-import a 50-file folder, run the full chat + approve flow, verify uninstall leaves the vault intact.

**Cross-platform surprises documented mid-plan (will re-surface in VM dry runs).** (a) Bash 3.2 on macOS: no `[[ -v var ]]` (use `[ -n "${var:-}" ]`), no `${var,,}` lowercase (use `tr '[:upper:]' '[:lower:]'`), no associative arrays. install.sh is bash-3.2-compatible throughout. (b) PowerShell 5.1 compatibility: install.ps1 avoids `??`, ternary operator syntax, and PS7-only cmdlet flags — tested with the stock Windows PowerShell that ships on Windows 10/11. (c) tar.exe availability on Windows 10 build 17063+ — install.ps1 calls `tar.exe -xzf ...` directly rather than the PS-native `Expand-Archive` because `Expand-Archive` can't read .tar.gz. (d) Windows file locking during `brain upgrade`'s atomic swap — uv run children sometimes hold `__pycache__` handles after termination; the swap module (Task 5) retries the rename 3x with a 500ms backoff before giving up, which clears the common linger window. (e) macOS token file ACLs: `os.chmod(0o600)` works; on Windows it's a no-op (ACLs are model-mismatched with POSIX permission bits). Plan 05's `# TODO(Windows ACL)` note at `brain_api.auth.write_token_file` still stands — a future hardening pass will add `pywin32` `SetFileSecurityA` once pywin32 is on the dep surface.

**`BRAIN_NO_BROWSER=1` added to the browser helper (demo need).** The Plan 07 demo driver had to monkey-patch `webbrowser.open` to avoid popping a real browser during the chat-turn gate. Plan 08's demo runs `brain start` for real (supervisor is the thing under test) — it can't reach into the subprocess to patch. Added an `os.environ.get("BRAIN_NO_BROWSER") == "1"` early-return to `brain_cli.runtime.browser.open_browser` so the supervisor still prints the URL + exits cleanly without opening a GUI browser during CI / demo runs. Behavior-neutral for real users (flag opt-in, default off).

**Ingest E2E-mode caveat surfaced by gate 7.** `BRAIN_E2E_MODE=1`'s canned integrate response returns `new_files=[]`. `brain_ingest`'s staging path uses `patchset.new_files[0].path if patchset.new_files else note_path` — with the empty canned list, the envelope's `target_path` falls back to the absolute `note_path`, whose `parts[0]` is `/` (POSIX) and not a domain name. `brain_apply_patch`'s defensive domain-derivation check then rejects with ScopeError → 403. Plan 08 demo gate 7 works around this by using a fresh `brain_propose_note` (which writes a vault-relative path) for the apply step — gate 6 still proves ingest staged a patch in the pending queue. Documented in the gate 7 docstring. For Plan 09: either (a) fix `FakeLLMProvider`'s canned integrate response to populate `new_files` with a vault-relative path, or (b) make `brain_apply_patch` tolerate absolute paths that resolve under `vault_root` by stripping the prefix before the domain check. Option (b) is cleaner (defensive in the apply path) and worth ~15 LoC + 2 tests.

### Plan 09 — Ship v0.1.0

**Completed 2026-04-24, tags `plan-09-ship` + `v0.1.0`.** 13 tasks planned / 11 fully shipped + 2 harnessed-deferred (Tasks 9 + 10 clean-Mac / clean-Windows VM dry runs; Plan 08's Task 10/11 harness is shipped and ready to run whenever a VM pass is commissioned). **25 commits since `plan-08-install`.** Test counts unchanged at close: brain_core + brain_cli + brain_mcp + brain_api = **947 pass + 18 skip** (Python); brain_web **231 pass + 1 skip** (Vitest); Playwright **14/14** + axe-core WCAG 2.2 AA clean. 12-gate demo green (`PLAN 09 DEMO OK`). Tool surface unchanged at **34** (ship plan — no new tools). GitHub release at v0.1.0 live; tarball SHA256 `657f9feaab04fc2a54fe9089b226ae7d118a6c523cc6c03b72dbaba829592c1a` (post-sweep).

**Pre-ship automated QA (Task 11) was the highest-value gate in Plan 09.** Driving the real install with Claude-in-Chrome MCP surfaced **5 BLOCKERs** + **several MEDIUM** findings that local-box dev testing had missed entirely: **IP1–IP4** (install-path bugs — shim PATH resolution, supervisor `uv run` nesting, `BRAIN_INSTALL_DIR` propagation from shim + `.app` launcher, install_lib bootstrap for the curl-standalone install path), plus **F5/F7** (setup-status BLOCKER — the pre-fix rule required BRAIN.md for `is_first_run=false`, which looped users back to the wizard after the "Skip this" step). All 5 BLOCKERs fixed in-sweep before external users could hit them. The tarball SHA cycled **four times** during Plan 09 (initial `edf81cb...` at Task 8, `1e14c9e...` after the install_lib fix, `fc8722d...` after the supervisor fix, `657f9fe...` after the final Task 11 sweep batch) — every re-upload used `gh release upload --clobber` which preserves the v0.1.0 tag + GitHub-release URL so install.sh's default URL never needed changing. Keep this pattern.

**Claude-in-Chrome QA methodology — `read_page` + JavaScript eval beat the screenshot tool.** The MCP screenshot tool timed out repeatedly when driving real flows (wizard step transitions, dialog open/close). Workaround: use `read_page` for semantic content + `javascript_tool` for state snapshots (localStorage, computed styles, aria attributes, element counts). This pairing gave fully-observable state — "how many cards are visible", "what's the aria-selected tab", "what's in localStorage.brain-setup-done" — without depending on the flaky screenshot path. For future releases, treat this as the default automated-QA approach on macOS; only fall back to `computer` / screenshots for visual-regression gates where pixel-level comparison is the point.

**Cross-platform VM dry runs (Tasks 9, 10) harnessed-deferred.** Per user decision, the clean-Mac + clean-Windows VM executions were not run in Plan 09. What shipped instead: `scripts/serve-local-tarball.py` (host-side HTTP server for the VM to pull install.sh + tarball), `docs/testing/clean-mac-vm-host-instructions.md` + Windows equivalent (step-by-step host commands), `docs/testing/clean-mac-vm-receipt.md` + Windows equivalent (blank-slot templates for timings, screenshots, deviations), and the receipt-filing pattern from Plan 08. Any future VM sweep runs directly against the live v0.1.0 GitHub release URL; no harness change required. Handoff to v0.1.1.

**Update-check nudge (Q2a) — full round-trip exercised, live-subprocess gap documented.** Task 4 shipped `_update_check_nudge` as a daemon thread in `brain start` + 4 unit tests monkey-patching `check_latest_release`. The unit tests cover (a) newer version → nudge printed, (b) same version → silent, (c) exception → silent + no crash, (d) `BRAIN_NO_UPDATE_CHECK=1` → no network call. The Plan 09 demo gate 6 goes one step further: spins up a local HTTP stub that mimics the GitHub `/releases/latest` schema (tag_name, assets[], body with `SHA256: ...` line), then calls `release.check_latest_release(..., url=<stub-url>)` directly from the demo driver. Asserts the full parse path. What's NOT covered: the nudge thread firing from `brain start`'s actual subprocess — the module-level `_GITHUB_LATEST_URL` constant in `runtime/release.py` has no env-var override, so a live-subprocess test would need either a code change (add `BRAIN_UPDATE_CHECK_URL` env hook) or a mock server on `api.github.com`. For v0.1.0 the unit tests + in-process demo stub are sufficient. Consider the env-var hook as a v0.1.1 hardening item.

**F5/F7 fix broke `setup-wizard.spec.ts` silently — caught during Task 13 close.** The BRAIN.md-presence rule removal in `b7e7a47` changed `is_first_run` such that Playwright's fresh-temp-vault fixture (which gets both a token AND a vault from brain_api's boot) now reports `is_first_run=false` → root route redirects to `/chat` instead of `/setup`. The spec's "first-run → walks 6 steps → lands on /chat" test had a hardcoded `page.goto("/"); page.waitForURL(/\/setup/)` expectation that was never re-run between the F5/F7 fix and the Task 13 close sweep. **Fix:** navigate directly to `/setup/` (mirrors the sibling `back nav` test which already used that pattern). Docstring updated to document the F5/F7 behavior change. **Lesson:** whenever a backend rule for a frontend redirect changes, grep the e2e specs for goto-and-wait-for-url patterns targeting the old destination. Add this to the Task 11 checklist for future releases.

**README screenshot embeds (Task 12) deferred to v0.1.1.** The QA sweep (Task 11) did not produce the 6-8 curated screenshots Q4b called for — `docs/testing/screenshots/v0.1.0/` is empty. The decision rationale: (a) MCP screenshot tool timeouts made capturing reliable stills during automated QA a tax on momentum, (b) the README ships text-only without impacting first-user install experience, (c) a targeted screenshot pass is a <30-min task in v0.1.1. README text embeds stripped in `aaa1c5d`.

**Handoff to v0.1.1.** (a) README screenshot embeds — capture during the next QA pass; (b) Clean-Mac + clean-Windows VM dry runs against the live release URL (harness ready); (c) F6 a11y label concatenation — theme cards + autonomy rows read concatenated labels to screen readers (captured in `docs/v0.1.0-known-issues.md`); (d) Windows token file ACL — drop the read-only bit in favor of a proper `SetFileSecurityA` call via pywin32; (e) Broken-wikilink detection — was a Plan 07 deferral, still open; (f) Real PDF web-upload — current endpoint only accepts text; (g) `BRAIN_UPDATE_CHECK_URL` env override so `brain start` nudge can be integration-tested against a local stub. The `gh release upload --clobber` pattern used 4× during Plan 09 worked cleanly — keep it for future hotfix turns on a tagged release.

## Plan 10 — Configurable Domains (closed 2026-04-27, tag `plan-10-configurable-domains`)

**Closure summary.** All 9 tasks landed in single-day execution against the locked D1–D10 decisions. Closes `docs/v0.1.0-known-issues.md` item #21. Demo gate (`scripts/demo-plan-10.py`) walks the 7-gate sequence (default-set boot → create hobby → ingest into hobby → rename work→consulting → reject rename(personal) → delete consulting → restart classifier with the new set) and prints `PLAN 10 DEMO OK`. Tool count unchanged (36 → 36) — Plan 10 didn't add MCP tools, it verified the four shipped in Plan 07 Task 4 + 25A and made them domain-set aware. Final test totals: 1022 brain_core/api/mcp/cli passed + 247 brain_web vitest cases. Tag commit captures `tag: plan-10-configurable-domains` for the bisect log.

**uv `editable=false` workaround has degraded — `_editable_impl_brain_core.pth` reappears with macOS UF_HIDDEN flag.** Root-caused during Task 1/Task 2 execution: even though `pyproject.toml` has `editable = false` for all four workspace packages, current uv writes a `_editable_impl_<pkg>.pth` shim alongside the dist-info anyway, with the macOS `UF_HIDDEN` flag set, and CPython's `site.addpackage` silently skips hidden .pth files (`_trace("Skipping hidden .pth file: ...")` then `return` — no warning to stderr). Symptom: `import brain_core` fails inside the project venv even though `uv sync` reported success and `ls site-packages/` shows the .pth file present. **Diagnostic:** `ls -lO .venv/lib/python3.12/site-packages/_editable_impl_brain_core.pth` — if the second-to-last column says `hidden`, that's the bug. **Workaround:** `chflags nohidden .venv/lib/python3.12/site-packages/_editable_impl_*.pth` before each pytest run that hits a package whose source you've modified. Re-runs of `uv sync` re-mark the file hidden — this is not a one-shot fix. **Filed for v0.1.1**: pin the upstream uv issue (or open one) and revisit `editable = true` once fixed; in the meantime add a `make test` target that runs `chflags nohidden` before pytest.

**Plan 10 Task 1 schema migration is conservative — `Domain` Literal stays one minor version.** When replacing the v0.1 `Domain = Literal["research","work","personal"]` constraint with the runtime `Config.domains: list[str]`, the locked decision (Task 1 step 5, plan-line 139) is to keep the `Domain` Literal alias as a deprecation re-export for one minor version — not a runtime warning, just a typing-time alias. `ALLOWED_DOMAINS` was harder to keep (it was a tuple literal whose contents would lie if the user added a domain), so Task 2 dropped it cleanly and routed any callers to `DEFAULT_DOMAINS`. Lesson for future schema migrations: when a constraint widens from "fixed enum" to "user-configurable list", the Literal type alias is a useful deprecation slot but a constant tuple is not — drop the tuple, keep the Literal.

**Pydantic v2 cross-field validation lives in `model_validator(mode="after")`, not `field_validator`.** Plan 10 Task 1 needed `active_domain` to validate against the live `Config.domains` list. v1's `field_validator` `values` dict is gone; v2's `field_validator` doesn't see other fields cleanly. Use `model_validator(mode="after")` and read `self.<field>` directly. Field validators still work for single-field rules (slug regex, duplicate detection inside a list); cross-field rules go in the model validator. Order matters: pydantic runs field validators first, then model validators in declaration order — so the model validator sees both fields populated.

**Pydantic context-driven validation: strict at the seam, permissive in the middle.** Plan 10 Task 3 needed `ClassifyOutput` to reject out-of-set domain replies — but the classifier wanted graceful degradation (return the offending slug + flip `needs_user_pick`) rather than raise. Resolution: model_validator with `info.context["allowed_domains"]` is *strict* when context is provided (used by direct test callers + future tools that want a hard-fail contract); loose when omitted. The classifier and pipeline parse permissively (no context) and apply their own in-set check. The pydantic validator becomes a contract for callers that want strict, not a runtime requirement for every parse. **Rule:** when adding a model_validator that consults `info.context`, document who calls with context and who without; the loose default is what keeps low-level tests + contract callers ergonomic.

**Test-fixture seeding has to track new permission rails.** Plan 10 Task 5 added a "last non-personal domain" guard to `delete_domain`. Four pre-existing `test_delete_domain.py` cases (typed-confirm, missing-domain, invalid-slug, success-path) all seeded a single non-personal domain — exactly the case the new guard refuses. Updating each test to seed two non-personal domains (`research` + `target-slug`) before exercising the OTHER rails was a one-line fix per test, but identifying *which* tests pre-empted my new rail required reading every error message. **Rule:** when adding a permission rail to a tool, run that tool's test file FIRST and audit any test whose fixture is sparse; if the new rail fires before the OLD rail being tested, seed extra fixture state rather than re-ordering rails.

**Hidden-flag surprise: `'work' in rendered` is too coarse a check on prompt text.** Test `test_classify_system_renders_listed_domains_only` initially asserted `"work" not in rendered.lower()` to prove the prompt enum excluded `work` from the call. The classify prompt's privacy-rail rule contains the phrase `"source medium is work-like"` — substring match tripped on it. Backtick-form check (`"`work`" not in rendered`) sidesteps the false positive because the prompt only backticks slug names. **Rule:** when asserting "X not in prompt-rendered text", ground the check on the slug's *syntactic* form (backticks, JSON quotes, list bullet) rather than the bare substring.

**Frontend cache invalidation needs to fan out to peer surfaces.** Plan 10 Task 7's `useDomains` singleton cache was the right shape, but Task 6's `panel-domains.tsx` had to learn to call `invalidateDomainsCache()` on every mutation (Add / Rename / Delete) so the topbar and Browse file-tree re-fetch on next mount. Without this, deleting a domain in settings would leave a phantom dot in the topbar's scope picker. **Rule:** when adding a module-level singleton cache, every writer that mutates the underlying data has to call the invalidation function — and the cache file should expose that function as a stable public seam, not an internal helper.

**The default scope ≠ active_domain follow-up was descoped explicitly.** Plan 10 Task 7's per-task review note asked for "default scope on first load = the user's `Config.active_domain`". Implementing that needs (a) `active_domain` exposed on `brain_list_domains` response or via a separate first-load fetch, and (b) a `scopeInitialized` flag on app-store to detect "never been set" vs "user reset to default". The pragmatic Plan 10 ship cut this and added a *prune-only* effect (drop persisted scope slugs missing from the live list). Filed as a Plan 11 follow-up. **Rule:** when a task's per-task review note describes a behavior change that needs schema/state plumbing (not just rendering changes), treat it as a separate task and don't try to bolt it on at the end of the parent task — the missed scope creep is what costs the demo gate.

## Plan 11 — Persistent Config + Per-Domain Overrides + Privacy Rail Generalization (closed 2026-04-28, tag `plan-11-persistent-config`)

**Closure summary.** All 10 tasks landed against the locked D1–D16 decisions. Closes `docs/v0.1.0-known-issues.md` item #27. Demo gate (`scripts/demo-plan-11.py`) walks the 8-gate sequence (empty-vault config_set persists → create_domain persists → domain_overrides resolver round-trips through reload → privacy_railed extension excludes the new slug from wildcard scope → Config validator refuses removing personal → Config validator refuses orphan override → corrupt config.json falls back to .bak with structlog warning → Playwright spec for the disk round-trip) and prints `PLAN 11 DEMO OK`. Tool count unchanged (Plan 11 didn't add new MCP tools — it persisted the five mutation tools shipped earlier). Final test totals: 1134 backend pytest passed + 273 brain_web vitest cases + Playwright `persistence.spec.ts` green alongside the existing 7 e2e specs. Tag commit captures `plan-11-persistent-config` for the bisect log. Plan 12 candidate scope captured in `tasks/todo.md` (4 new follow-ups: Settings UI for `active_domain`, cross-domain confirmation modal copy, `brain_config_get` snapshot drift, brain_mcp Config-wiring gap).

**Operational chflags gotcha refined three times during execution.** Task 1 found `chflags nohidden <single .pth>` worked once; Task 3 found `chflags 0` (clamp ALL flags, not just `UF_HIDDEN`) was needed because some macOS background process was setting other flags too; Task 4 found 1074 files re-hide on a few-second cadence (Spotlight/mds indexing under `~/Documents/Code/...`). The reliable form is `find <venv> -flags hidden -print0 | xargs -0 chflags -h 0` (with `-h` so symlinks are honored), but EVEN THAT doesn't last across `uv run` calls — uv's auto-sync re-marks files hidden mid-session. Two `.pth` files now exist (live + stale `_editable_impl_brain_core 3.pth` duplicate from earlier syncs); both need clamping. Task 4 implementer's escape hatch: invoke `.venv/bin/python` directly with `PYTHONPATH=packages/<pkg>/src` set, bypassing the .pth lookup entirely. **Rule:** the chflags step must be the IMMEDIATE prefix of the same command that runs python — not in a separate setup step — and the command must not invoke `uv run` (which re-syncs and re-hides). The Plan 11 Task 10 demo command line is `chflags 0 <pths> && .venv/bin/python scripts/demo-plan-11.py`; the e2e webServer can't drop `uv run` cleanly without changing shipped infra, so the workaround there is `PYTHONPATH=packages/.../src npx playwright test`.

**brain_api Config-wiring oversight surfaced by browser-in-the-loop verification (Task 7).** Plan 11 Task 4 wired `save_config()` into 5 mutation tools assuming `ctx.config` was set. `brain_mcp` and unit tests pass it through correctly; `brain_api` did NOT — every Plan 11 mutation dispatched via brain_web → brain_api silently fell to the `ctx.config is None` no-op branch, the toast said "saved" but the disk write never happened. Browser-verified (toast visible → no `config.json` on disk) caught it; no in-process test would have. Fix: `build_app_context()` accepts `config=` and the lifespan calls `load_config(...)` to thread it through. **Rule:** load-bearing wiring needs production-shape integration tests, not just unit-test-with-explicit-config tests. The Task 10 Playwright spec (`persistence.spec.ts`) is the regression guard — it asserts disk bytes after a UI action, so a future Config-wiring regression would fail this test before it shipped.

**Plan-text vs reality drift in `config_set`.** Task 4 plan said "today config_set sets a field on `ctx.config`"; actually it only validated keys without mutating. Task 10 hit the same drift on the demo gate: the plan said "`brain_config_set autonomous_mode=true` writes config.json" but `autonomous_mode` is NOT in `_SETTABLE_KEYS` — `log_llm_payloads` is the canonical "top-level bool, persists" key, so the demo uses that as a stand-in for the same shape. Implementer added the dotted-path apply (`_resolve_parent_and_field`) to make persistence meaningful and pinned `log_llm_payloads` in the demo gate's gate 1. **Rule:** plan text claims about pre-existing code state need verification at task-start, not assumption — and example field names in demo gates should be sanity-checked against the live allowlist before the demo is run.

**`.bak` path: name vs stem.** Plan said `<config_file.parent>/<config_file.stem>.bak`. But `Path("/x/config.json").stem == "config"` produces `/x/config.bak` which is NOT what `save_config` writes (which uses `name` → `config.json.bak`). Implementer used `path.parent / f"{path.name}.bak"` and pinned it with `test_bak_path_uses_name_not_stem`. **Rule:** when a plan specifies a derived path, double-check `Path` semantics — `stem` drops the extension while `name` keeps it. The drift wouldn't crash; it would silently never find the .bak and always fall through to defaults, which is exactly the bug the .bak fallback was added to prevent.

**Test design must match production semantics, not unit-test ergonomics (Task 4 flaky test).** First-cut concurrent test shared one `Config` across two threads doing parallel mutations. Production never shares one Config across handler threads (each request handler gets its own `ToolContext` with its own `Config`). Test rewrite pinned the actual filelock-serialization contract: each thread gets its own Config, both succeed via filelock, on-disk file is valid, exactly one new slug wins the race. **Rule:** when testing concurrency, model the production threading topology, not the easy-to-write shared-state topology. Sharing Pydantic models across threads is out-of-contract; testing that out-of-contract scenario tests an undefined behavior, not the system's real semantics.

**User-guide accuracy vs implementation (Task 9 caught two errors).** First draft of `docs/user-guide/domain-overrides.md` listed `autonomous_mode` as a working feature (it isn't — Task 5 landed the resolver `resolve_autonomous_mode` without a consumer; the autonomy gate at `brain_core.autonomy` reads per-category flags via `Config.autonomous.<category>`, a different field). Also claimed "in-flight requests continue with the config they started with" (wrong — implementation mutates `ctx.config` in-place via setattr; same-process re-reads see new value; the on-disk filelock serializes between processes but in-process state is shared by reference). **Rule:** user-guide doc content needs review against actual implementation semantics, not the plan's intended semantics. The lesson is to draft the doc AFTER reading the merged code, not from the plan; the plan describes intent, the code is reality.

**`brain_config_get` snapshots `Config()` defaults rather than `ctx.config` (Task 7 Plan 12 follow-up).** The Settings UI's `DomainOverrideForm` reads override values via `brain_config_get`, which constructs a fresh `Config()` rather than reading `ctx.config`. Result: after a successful save (which DOES persist correctly to disk), reloading the page re-renders an empty form even though `config.json` has the right bytes. The Playwright spec `persistence.spec.ts` asserts the disk-state guarantee Plan 11 ships and explicitly DOES NOT assert the input-value re-renders post-reload — that's a Plan 12 follow-up filed in `tasks/todo.md`. **Rule:** read-path tools that snapshot defaults instead of live state are a known anti-pattern in this codebase; new read tools must thread `ctx.config` through, and existing read tools should be audited as their own task before extending the UI surface that consumes them.

**Structured exception fields prevent forward callers needing to circle back (Task 2 reviewer's foresight).** `ConfigPersistenceError(message, *, attempted_path: Path | None, cause: str | None)` was forward-looking design with `cause` as a stable token (`"lock_timeout"`, `"replace_failed"`, `"io_error"`, `"parse_error"`). Task 4's 5 mutation tools could branch on `.cause` for uniform error UX rather than parsing message strings; if the error class had been a bare `Exception("...")` first, every consumer would have rewritten their error-handling later. **Rule:** when adding an exception class that 5+ callers will catch, design the structured fields up-front rather than discovering them at the consumer site — the message-string-parsing path is a refactor magnet.

**Pydantic v2 cross-field validation (already in Plan 10 lessons; reaffirmed in Plan 11).** Plan 11 added three more `model_validator(mode="after")` hooks on `Config`: `_check_privacy_railed_subset_of_domains`, `_check_domain_overrides_keys_in_domains`, plus the Plan 10 `_check_active_domain_in_domains` we already had. `field_validator` for single-field rules (slug regex, dup detection); `model_validator(mode="after")` for cross-field rules (must-be-in-domains, must-be-subset-of-domains). The pattern is now well-trodden enough to be reusable scaffolding for any future "cross-field set membership" rule.

**Handoff to Plan 12.** Four follow-ups captured in `tasks/todo.md` Plan 12 candidate scope: (a) Settings UI for `active_domain` editing — Task 8 implementer flagged that `brain_config_set` rejects the `active_domain` key and the verification screenshot required hand-editing config.json; (b) cross-domain confirmation modal copy — spec line 187 deferred the actual UI string ("warning about personal content"), needs `brain-ui-designer` microcopy first; (c) `brain_config_get` snapshot drift — discussed above, Settings UI shows stale values until restart; (d) `brain_mcp/server.py:_build_ctx` has the same Config-wiring gap brain_api just got — same fix pattern. Also deferred: `resolve_autonomous_mode` consumer wiring (resolver landed in Task 5 without a downstream reader; either delete as dead code or wire into the autonomy gate); per-domain budget caps; per-domain rate limits; repair-config UI; cross-process hot-reload of config changes; `validate_assignment=True` on Config.

## Plan 12 — Settings UX completion + Plan 11 correctness cleanup (closed 2026-04-28, tag `plan-12-settings-ux-and-cleanup`)

**Closure summary.** All 10 tasks landed against the locked D1–D11 decisions. Closes the four explicit Plan 11 closure-addendum items (#7 active_domain Settings UI, #8 cross-domain modal, #9 brain_config_get drift, #10 brain_mcp Config-wiring), the late-addendum cross-instance pubsub (#11), and the dead-resolver cleanup (#1). Demo gate (`scripts/demo-plan-12.py`) walks the 7-gate sequence (schema round-trip + DELETE markers → read-tool audit sentinel → brain_mcp stdio Config persistence → Playwright zustand pubsub → Playwright active_domain UI → Playwright modal trigger parametrized → Playwright acknowledgment lifecycle) and prints `PLAN 12 DEMO OK`. Schema gains 1 field (`Config.cross_domain_warning_acknowledged`); schema loses 1 field (`DomainOverride.autonomous_mode`); one module deletion (`brain_core.llm.resolve_autonomous_mode`). Tool count unchanged (D2 extended `brain_config_set` allowlist; no new MCP tools). Final test totals: 1140 backend pytest passed (delta-vs-Task-9 within the pre-existing Plan 11-era flakes captured below) + 316 brain_web vitest passed (matches Task 9 baseline) + 11/11 Playwright specs passed when scoped to non-pre-existing failures (the 8 a11y color-contrast violations + 1 setup-wizard a11y violation reproduce on a clean main HEAD pre-Task-10 and are not Plan 12 regressions). Plan 13 candidate scope captured in `tasks/todo.md` (eleven follow-ups: see "Handoff to Plan 13" paragraph below).

**Pydantic v2 `model_validator(mode="after")` doesn't fire on setattr or model_dump (Task 6 lesson).** With `validate_assignment=False` (the default), mutation paths that reach a Config field via `setattr(cfg, key, value)` bypass validators completely; same for persistence via `model_dump`/`persisted_dict`. Plan 11's `_apply_domain_override` already needed a tool-level pre-check mirroring the validator wording; Plan 12 Task 6 ran into the same shape with `_check_active_domain_membership` (the cross-field validator on `Config._check_active_domain_in_domains` won't fire when a tool calls `cfg.active_domain = "ghost"` — the persistence path lands a config.json with an invalid value that only fails on next-load). Both tools defensively pre-check before assignment. **Rule:** Pydantic v2 cross-field validators are read-time guarantees, not write-time. Any tool that mutates a Config field via setattr MUST mirror the validator's wording in a tool-level pre-check, OR the schema must enable `validate_assignment=True` (filed for Plan 13+ once measured).

**Cross-task whitelist drift (Task 9 catch).** Plan 12's File-structure section anticipated a `+ path for cross_domain_warning_acknowledged` line on `tools/config_set.py`, but neither Task 1's task-text (which added the schema field) NOR Task 6's task-text (which added `active_domain` to `_SETTABLE_KEYS`) explicitly listed `cross_domain_warning_acknowledged`. Task 9 caught the drift while wiring `setCrossDomainWarningAcknowledged()` and added the slug to `_SETTABLE_KEYS` itself. The drift-watchdog test was updated in the same commit. **Rule:** when a plan adds a new persisted Config field, the same task that adds the schema field must also extend `_SETTABLE_KEYS` (the brain_config_set allowlist) — otherwise the dependent UI task discovers the gap mid-implementation and the catch-rate depends on the next implementer reading the right doc.

**Plan 11 D9 active_domain ≠ topbar scope (Task 8 lesson).** Plan 12 plan-text said "topbar scope chip updates without page reload" — that's wrong. The topbar is per-session (post-`scopeInitialized` flip), NOT live-bound to `Config.active_domain`. Plan 11 Task 8 deliberately drew that distinction: `active_domain` is the persistent default for new chats / first-mount hydration, but the topbar's scope chip is an in-session override that doesn't reach back to the persisted field. Plan 12's plan text conflated these and the Task 10 e2e spec had to clear `localStorage.scopeInitialized` and reload to verify persistence — equivalent to a fresh-tab visit, not a live-binding. **Rule:** future plans touching `active_domain` should NOT assume the topbar is live-bound; the persistent-default vs per-session-override distinction is load-bearing.

**Browser-in-the-loop verification under React portal automation limitations (Task 9 manifest).** The `element.click()` approach (used by some manual debugging recipes) doesn't reliably fire React's `onClick` across Radix portals; DOM/state inspection is a workaround but isn't the canonical "screenshot triple" the spec asked for. Playwright DOES fire React events correctly via the real-browser dispatch path — the Plan 12 Task 10 e2e specs (`active-domain.spec.ts` + `cross-domain-modal.spec.ts`) prove the trigger gate, modal-open, checkbox-click, and Continue-button-click all work end-to-end through Radix portals. **Rule:** future tasks that need browser-in-the-loop verification of Radix-portal-hosted UI should plan for Playwright (which fires React events correctly) as the verification path, not a dev-tool DOM inspection — OR explicitly accept DOM-state verification as the equivalent (with a comment).

**Handoff to Plan 13.** Eleven follow-ups captured in `tasks/todo.md` Plan 13 candidate scope: (1) `apply_patch.py:_resolve_config` docstring stale post-Task 3; (2) `list_domains` None-policy asymmetry (config_get raises, list_domains silently falls back to defaults); (3) `config_set.py` lenient `cfg is None` branch — same asymmetry; (4) `panel-domains.tsx` local `domains: string[]` state parallel to the zustand store; (5) plan-text "topbar scope chip" inaccuracy (Task 8); (6) `brain start` CLI doesn't handle chflags gracefully; (7) modal "private" vs Settings "Privacy-railed" jargon split; (8) active-domain dropdown toast CTA "Pick a different domain" misleading on transport-failure mode; (9) active-domain dropdown toast `pushToast` outside try-block; (10) Task 9 `pendingSendRef.mode` dead field; (11) Task 9 `useCrossDomainGate` cross-instance gap (same shape as Plan 12 Task 5 fixed for `useDomains`). Also forwarded from Plan 12's NOT-DOING section: per-domain budget caps; per-domain rate limits; repair-config UI; cross-process hot-reload; `validate_assignment=True` on Config; per-domain autonomy categories; "Set as default" topbar button; per-thread cross-domain confirmation; generic "tool reads ctx.config" lint rule; migration tool for older config.json files; generic zustand promotion across other hooks.

## Plan 13 — Cross-instance cleanup + pre-existing test debt closure (closed 2026-04-29, tag `plan-13-cross-instance-cleanup-and-test-debt`)

**Closure summary.** All 7 tasks landed against the locked D1–D11 decisions. Closes the four cross-instance / lifecycle anti-pattern items from Plan 13 candidate scope (#A1 None-policy strictness on `list_domains` + `config_set`; #A2 `panel-domains.tsx` local state drop; #A3 `cross-domain-gate-store` pubsub; scope-adjacent: `apply_patch.py` docstring), AND the brain_api 13-failure pre-existing test debt (#B1; root cause was Plan 08 `SPAStaticFiles` mount shadowing — NOT the OriginHostMiddleware drift Plan 12 closure hypothesized), AND the 9 axe-core a11y color-contrast violations (#B2; root cause was the v4 brand-skin.css drop cascade-shadowing Plan 07 Task 25C's `tokens.css` nudges). 7-gate demo (`scripts/demo-plan-13.py`) walks the sequence (None-policy raises on list_domains + config_set + config_get reference policy; panel-domains store-only pin test; cross-domain-gate-store pubsub pin test; brain_api 13-failure re-pass; envelope shape parity regression-pin; a11y axe-core 8 routes; a11y axe-core setup-wizard) and prints `PLAN 13 DEMO OK`. Schema unchanged (Plan 13 was pure correctness + test debt; no field add/remove). Tool count unchanged (36 → 36). Final test totals: **1164 backend pytest passed + 11 skipped** (rises from Plan 12's 1140 by 13 newly-passing brain_api tests + 5 envelope-shape-parity assertions + the new pin tests across Tasks 1, 2, 3, 5, 6) + **334 brain_web vitest passed + 1 skipped** (rises from Plan 12's 316 by ~18 new pin tests across Tasks 2 + 3 + a11y-related) + **20/21 Playwright e2e passed** (the remaining flake is pre-existing `ingest-drag-drop.spec.ts` and passes in isolation — not caused by Plan 13). The a11y gate is now genuinely hard-failing across 8 routes + setup-wizard. Plan 14 candidate scope captured in `tasks/todo.md` (architectural follow-throughs + brain_api hardening + a11y consolidation + small cleanups carried forward + plan-file archival fixes + bigger architectural moves).

**Closure-summary hypotheses are not diagnoses (Plan 13 Task 4's big finding).** Plan 12's closure paragraph said the 13 brain_api failures were "likely TestClient/middleware-config drift since Plan 11 era" — based on the symptom (status 200 where 4xx expected) and the prior Plan 05 BaseHTTPMiddleware lesson. Task 4's read-only audit *refuted* H1 (OriginHostMiddleware), H2 (TestClient `Host`/`Origin` drift), H3 (envelope shape drift), H4 (`dependency_overrides` drift), and *confirmed* H5: Plan 08 Task 1's `SPAStaticFiles` mount at `/` shadows synthetic test routes registered AFTER `create_app()` returns; the trigger condition is environmental (`apps/brain_web/out/` exists, populated locally by Plan 13 Task 3's `npm run build`). Plan 09 close didn't catch the regression because `out/` wasn't built at that point in CI. **Rule:** when a closure summary says "likely X-era drift," that's a candidate hypothesis, not a diagnosis. Bisect by reproducing on the candidate-era HEAD (or earlier) before authoring the next plan's task. Plan 12 closure jumped past the bisect step; Plan 13's diagnose-first task discipline (D5) caught the cost. The follow-on lesson: when adding a route mount that catches all unmatched paths (Plan 08's static export pivot), audit every test fixture that registers ad-hoc routes via `@app.get(...)` decoration on the live `app` fixture — those routes are now downstream of the catch-all in Starlette's resolution order.

**CSS cascade shadowing is an accessibility-regression class of bug (Plan 13 Task 6's other big finding).** The e2e a11y gate was NEVER weakened post-Plan-07-Task-25C: `apps/brain_web/tests/e2e/fixtures.ts:46` has `DISABLED_RULES = []` and `expect.toEqual([])` hard-fails on any color-contrast violation. So the gate stayed strict; the violations regressed because the v4 brand-skin.css drop on 2026-04-24 redefined the same tokens Plan 07 Task 25C nudged in `tokens.css` (`--text-muted`, `--text-dim`, `--accent`) — and the brand-skin file cascades AFTER the tokens file, silently shadowing the AA-compliant alphas. Cause-and-effect: CSS cascade-shadowing + Playwright NOT in CI = silent regression. Task 6's token sweep cleared the violations on the same nudge precedent. **Rule:** when adding a new theme/skin file that overlaps token primitives with an earlier accessibility/contrast nudge, audit ALL tokens it redefines and confirm they don't shadow the earlier nudges (the `git log -L` for each token name across `*.css` is the diagnostic). Beyond that: **the structural fix is Playwright in CI**. Plan 14 candidate scope captures this as an explicit deferral with the Mac + Windows runner shape called out — chflags handling for editable installs needs to bake into the CI shell wrapper.

**Plan-text wording-match-verbatim adjudication (Plan 13 Task 1).** Plan 13 Task 1 cited an example match string (`match="ctx.config is not set"`) for the None-policy error wording the implementer should standardize on. The actual `config_get.py` wording was `"requires ctx.config to be a Config instance, but got None"` — the example match string in the plan text didn't match the cited file. Implementer kept the BODY verbatim (matching the canonical lifecycle-violation framing with diagnostic value) while swapping the tool-name prefix to differentiate ("brain_list_domains requires ..." / "brain_config_set requires ..."). **Rule:** when a plan cites an example match string from a "verbatim" source file, verify against the file before authoring; if drift is found mid-task, the BODY of the wording is the load-bearing part (it carries the diagnostic + lifecycle framing) and the prefix is the variable that differentiates per-tool. Don't hand-wave verbatim-vs-paraphrase across tasks — the next implementer's grep will land on whichever is in the plan text.

**Failure-mode UX regression on optimistic-delete refresh (Plan 13 Task 2).** Pre-Task-2, `panel-domains.tsx` used `setDomains((prev) => prev.filter(...))` to keep deleted rows out of view EVEN ON network-failure-of-refresh paths. Post-Task-2 (drop the local state in favor of the zustand store), the row reappears after delete + refresh-failure because the store still has the old list — there's no longer a local optimistic filter to honor. Plan 13 Task 2 review surfaced this as a regression worth a follow-up; recommended fix: add `removeDomainOptimistic(slug)` action to `domains-store.ts` (mirroring `setActiveDomainOptimistic`) and use it in `panel-domains.tsx`'s delete handler. Plan 14 candidate scope captures this. **Rule:** when collapsing a parallel-state seam into a store, audit the failure-mode behavior of the OLD seam (what used to happen on transport failure?) before declaring the simplification clean. The zustand store doesn't need to grow defensive optimistic filtering for every action, but the actions that previously had local optimistic UX need explicit decisions: keep the optimism via a store action, or surface the "row may flicker on refresh failure" UX gap as a deliberate design choice.

**Plan-text inconsistency between steps (Plan 13 Task 3 Steps 1 vs 3).** The plan's wording for `setAcknowledgedOptimistic` was "before the round-trip completes" in Step 1 vs "after calling ... successfully" in Step 3. Implementer chose BEFORE — matches `setActiveDomainOptimistic` precedent (Plan 12 Task 6), matches the literal name `setAcknowledgedOptimistic`, matches design intent (optimistic = update before confirmation). The `error` field added beyond the spec's `{privacyRailed, acknowledged, loaded}` shape lets `loading` derivation differentiate "still loading" from "errored-with-fallback" — Plan 14 candidate scope option to drop or wire to UI. **Rule:** plan-text drift across steps within a task is a soft-error catch-rate signal. When two steps disagree on a load-bearing semantic detail, flag it during plan-author review rather than letting the implementer pick — a quick diff between Step 1 and Step 3 wording would have caught this.

**Plan author's miscount drift (Plan 13 Task 5 audit).** Plan 13 cited `test_auth_dependency.py × 4` in three locations (lines 13, 133, 459); actual file has 3 tests (`grep -cE "^(async def |def )test_"` = 3). The headline "13 failures" was correct but the per-file breakdown was off by one (8 + 3 + 1 + 1 = 13, not 8 + 4 + 1 + 1 = 14). Plan 13 Task 7 corrected the three locations to "× 3" with explicit "Task 7 correction: was 4" annotations. **Rule:** when a plan makes per-file count claims, reconcile against `git log` or `grep -c` at plan-authoring time — the headline number can be right while the breakdown is wrong, and the breakdown is what implementers grep for. Add a "double-check counts" step to the plan-authoring checklist for plans that enumerate per-file test counts.

**chflags + canonical exec-prefix locked across Plan 13 (no new uv quirks).** Lessons 341 + Plan 11/12 refinements held: `chflags 0 .../_editable_impl_*.pth 2>/dev/null && .venv/bin/python ...` in the SAME command line is the load-bearing recipe. Two new `_editable_impl_brain_core 4/5/6.pth` duplicates appeared during Plan 13 execution (uv re-syncs across the seven-task arc accumulate stale `.pth` files); the wildcard glob `_editable_impl_*.pth` covers all of them. No additional escape hatches needed. **Rule:** the chflags glob pattern is the right shape; the `_editable_impl_*.pth` plurality is a uv bookkeeping side-effect, not a workflow change. Plan 14's chflags handling in `brain start` (deferred candidate scope) should treat the wildcard as the canonical form.

**Sentinel-domain pattern carries forward as the read-tool audit shape (Plan 12 D5 reaffirmed).** Plan 13 Task 1's strict-policy pin tests reused the Plan 12 D5 read-tool audit pattern: build a Config with a sentinel domain not in `DEFAULT_DOMAINS`, monkey-patch `ctx.config`, invoke the tool, assert the response reflects the sentinel. Plan 13 generalized to the inverse — build a `ToolContext(config=None)`, invoke the tool, assert RuntimeError. Both shapes pin the same contract (ctx.config IS the source of truth). The reusable scaffolding lives in `_READ_TOOLS_THAT_THREAD_CTX_CONFIG` (Plan 12) and the new strict-policy pins (Plan 13). **Rule:** the sentinel + monkeypatch + assert-reflection / sentinel + monkeypatch + assert-raise pattern is now well-trodden enough to be a generic test scaffolding for any future "tool reads ctx.config" contract. Generic ctx.config lint rule is a Plan 14+ candidate (lower priority than the per-tool pins, which are sufficient).

**Handoff to Plan 14.** Plan 14 candidate scope captured in `tasks/todo.md`. Themes: (a) architectural follow-throughs from Plan 12 + 13 reviews — orphan listDomains consumers in `bulk-screen.tsx` + `file-to-wiki-dialog.tsx`; `removeDomainOptimistic` action; `useDomainsStore.error` inline banner; `domainsLoaded` → `loaded` naming alignment; cross-domain-gate `error` field disposition; cross-tab BroadcastChannel pubsub; `setAcknowledgedOptimistic` early-return style; `panel-domains.tsx` 3-file split. (b) brain_api hardening — `SPAStaticFiles` non-http scope guard; 500-envelope `request_id` pin (if Plan 11 didn't already cover). (c) a11y — Playwright in CI on Mac + Windows; populated-state route coverage (chat with prose/citations/tool-calls; modal; patch-card edit dialog); `.prose a` dark-mode contrast; `text-[var(--bg)]` → `text-[var(--accent-foreground)]` semantic correctness; brand-skin hardcoded `#E06A4A` → `var(--tt-cyan)` consolidation; tokens.css consolidation OR CSS lint rule for duplicate token defs (structural fix for cascade-shadowing). (d) small cleanups carried forward from Plan 12 candidate scope (NOT picked up by Plan 13): plan-text "topbar scope chip" drift watch (lesson, not code); `brain start` CLI chflags handling; modal "private" vs Settings "Privacy-railed" jargon split; active-domain dropdown toast CTA wording on transport failures; `pushToast` outside try-block defensive cleanup; `pendingSendRef.mode` dead field. (e) Plan 13 plan-file archival cleanup: `× 4` → `× 3` (already landed in Task 7). (f) Test-quality follow-throughs: `_NO_CONFIG_MESSAGE` extraction to `tools/_errors.py` as `raise_if_no_config(ctx, tool_name)` helper; `_mk_ctx` signatures alignment; `apply_patch._resolve_config` Plan 07 Task 5 deferral docstring. (g) Bigger architectural moves (forwarded from Plan 12 + 13 NOT-DOING): per-domain budget caps; per-domain rate limits; repair-config UI; cross-process hot-reload; `validate_assignment=True` on Config; per-domain autonomy categories; "Set as default" topbar button; per-thread cross-domain confirmation; generic ctx.config lint rule; migration tool for old config.json files; generic zustand promotion across other hooks.

## Plan 14 — Hardening + CI restoration (closed 2026-04-28, tag `plan-14-hardening-and-ci`)

**Closure summary.** All 9 tasks landed against the locked D1–D11 decisions. Closes the eight items from Plan 14 candidate scope: (#B1) `SPAStaticFiles` non-http scope guard — `__call__` overridden to return 404 for WebSocket / lifespan scopes; production-shape pin test constructs a real ASGI WS scope dict and asserts the 404 response (NOT the parent `StaticFiles.__call__` AssertionError). (#B2) `request_id` pin in 500 envelope — `test_route_500_envelope_shape_includes_request_id` asserts `'request_id' in body['detail']` AND `len(...) > 0` (D4 wording-shape; deliberately not UUID-shape so RequestIDMiddleware can swap generators). (#C2.a) populated-state a11y dialogs — six dialog cases land in new `a11y-populated.spec.ts` (rename-domain, delete-domain, fork-thread, backup-restore, cross-domain modal, patch-card edit-approve); two deferrals (repair-config dialog UI surface; autonomy modal) filed for Plan 15 with explicit grep-receipts. (#C2.b) populated-state a11y menus + overlays — five additional cases land (topbar scope picker, Settings tabs walk × 8, search overlay, drop-zone overlay, toast notifications); the "file-preview overlay" deviation documented inline (the app has no dedicated Browse → file → preview overlay today; SearchOverlay covers the closest analog). (#C3) `.prose a` dark contrast routed through `var(--tt-cyan)` (single source of truth; `[data-theme="dark"] .prose a:hover` hardcoded `#E06A4A` removed from brand-skin.css). (#D8) `ingest-drag-drop.spec.ts` flake — diagnosed as a deterministic race (test arm; production race in `inbox-store.loadRecent` separately documented for Plan 15); fixed via `waitForResponse` not retry. (#C1.a + #C1.b) Playwright now runs on macOS-14 AND windows-2022 in `.github/workflows/playwright.yml` — sibling-step pattern (Mac chflags step gated on `runner.os == 'macOS'`; Windows pwsh step gated on `runner.os == 'Windows'`). 12-gate demo (`scripts/demo-plan-14.py`) walks the sequence (SPAStaticFiles WS guard → request_id 500 envelope pin → a11y patch-card edit dialog with prose → 5 dialog axe sweep → 2 menu cases → 3 overlay cases → `.prose a` `var(--tt-cyan)` route + brand-skin.css cleanup → ingest-drag-drop `--repeat-each=5` stability → workflow-file shape parse → full local Playwright suite → brain_api full pytest) and prints `PLAN 14 DEMO OK`. Tool count unchanged. Schema unchanged. Final test totals: **1169 backend pytest passed + 11 skipped** (rises from Plan 13's 1164 by 5 pin tests = Task 1 ws_guard cases + Task 2 request_id sub-test) + **334 brain_web vitest passed + 1 skipped** (matches Plan 13 baseline; no new vitest cases — populated-state coverage is e2e, not unit) + **32 Playwright e2e tests across 11 spec files** (rises from Plan 13's 21 by 11 new `a11y-populated.spec.ts` cases). The CI gate is now genuinely structural — all 32 specs run on Mac AND Windows on every push and PR. Spec footnote landed in §8 (Web UI) noting the new CI gate. Plan 15 candidate scope captured in `tasks/todo.md` (substantial — accumulated from Plan 14 task reviews; ~30+ items spanning architectural follow-throughs, a11y polish, CI follow-throughs, test-quality, and bigger architectural moves).

**Latent-bug-in-passthrough-class lesson (B1).** `StaticFiles.__call__` accepts WebSocket scopes silently — by inheriting `__call__` from a base class that asserts on http-only, our override path that only intercepted `get_response` left the parent's first-line assertion as the production behavior for non-http scopes. Plan 13 Task 5 review M1 surfaced the latent bug as a class — Plan 14 D3 fixed it via override. **Rule:** when subclassing a Starlette / FastAPI / framework primitive, audit ALL methods of the parent that the subclass might inherit — `__call__`, `__init__`, `get_response`, `lookup_path`, etc. — and pin the ones whose contract differs from the subclass's intended scope. Defense-in-depth via override is generic enough to apply to other StaticFiles-derived classes (none in the brain_api today; the audit confirmed). The production-shape pin test (real ASGI scope dict, capturing both `http.response.start` AND `websocket.http.response.start` frame variants) is the regression guard.

**Test-as-pin-not-test-as-trigger lesson (B2).** Plan 13 Task 5 review M3 noted `test_route_500_envelope_shape` asserted top-level keys but left `body['detail']['request_id']` unpinned. The naive fix is to pin the UUID format; the locked D4 fix is to pin the assertion *shape* — `'request_id' in body['detail']` AND `isinstance(..., str)` AND `len(...) > 0` — without specifying that it's a UUID4. RequestIDMiddleware can swap to ulid / KSUID / nanoid without breaking the test, but it can't silently stop attaching `request_id` (which was the actual regression vector). **Rule:** when adding a regression-pin test for an attribute slot, pin the *load-bearing semantic* (presence + non-emptiness + type) but NOT the implementation detail (specific format, exact length). Over-specifying the implementation detail makes the test fragile against legitimate refactors and creates incentive for next-implementers to weaken the assertion.

**Populated-state-coverage lesson (C2.a + C2.b).** Empty-state a11y testing misses ~80% of the app surface. Plan 13's `a11y.spec.ts` covered 8 top-level routes (chat, inbox, browse, pending, bulk, settings × 3) — all empty/skeleton states. Real users hit dialogs, menus, overlays constantly; axe-core only flags rendered elements, so the dialogs / menus / overlays were never under scan. Plan 14 D5 + D6 closed it via `a11y-populated.spec.ts` — 11 new cases covering 6 dialogs + 2 menus + 3 overlays. The discoveries during implementation: the patch-card edit dialog renders `.prose` body content (canonical chat-thread-with-prose surface), the cross-domain modal exists only in the populated-state path (it's gated on scope ≥ 2 + privacy_railed intersection), the search overlay (⌘K) is the closest analog to "file-preview overlay" the dispatch text named (the app has no dedicated Browse → file → preview overlay today). **Rule:** when extending coverage to "populated states," audit each dialog / menu / overlay's *trigger preconditions* (scope, fixture state, route depth) at task-start — some triggers need backend seeding (patch-card edit needs a seeded patch via `brain_propose_note`), some need localStorage / IndexedDB seeding (cross-domain modal needs `scope` + `scopeInitialized` flags). The test-runtime cost amortizes: separate `test()` calls per dialog beat one mega-test because axe-core failure messages are diagnosable per case.

**Single-source-of-truth-token lesson (C3).** `.prose a` had been `color: var(--brand-ember)` directly (= `#C64B2E`); on `--surface-1` (`#141412`) in dark mode that gave ~3.9:1 — failed 4.5:1 AA for body text. The naive fix is a 5th hardcoded `#E06A4A` site (a `[data-theme="dark"] .prose a` override). The locked D7 fix is to route through `var(--tt-cyan)` — theme-aware (light = `#C64B2E` ember, dark = `#E06A4A` bright). When `--tt-cyan` is nudged in future the link color follows automatically; no manual sync. The hardcoded `[data-theme="dark"] .prose a:hover { #E06A4A }` override that previously sat below the rule was redundant (base color and dark hover were the same value); dropped along with the cleanup. **Rule:** when fixing a contrast violation, prefer the theme-aware token route over a `[data-theme="dark"]`-scoped hardcoded override. Theme-aware tokens are auto-fixing for any future palette nudge; hardcoded overrides are drift-prone (they're the next plan's cascade-shadowing bug). Audit other `var(--brand-ember)` foreground sites (Plan 15 candidate) for the same pattern.

**Race-condition-via-suite-isolation lesson (D8).** `ingest-drag-drop.spec.ts` passed in isolation but failed in the full Playwright suite — the classic "flake from shared state" symptom. Plan 13 Task 7 closure noted it as a flake; Plan 14 Task 6 diagnosed it as a *deterministic race*: the drag-drop event posts an upload, then the test reads the inbox list — but `inbox-store.loadRecent` (which a sibling spec triggers earlier) overwrites the optimistic addition with a stale server response on the next render. The fix is `waitForResponse` on the upload completion (deterministic; the response IS the synchronization point), NOT a retry-based `test.fixme()` (which masks the race rather than fixes it). The production race in `inbox-store.loadRecent` — `set({ sources: items })` unconditionally replaces store contents at line 158, racing `addOptimistic` at line 163 — is a real production bug; documented for Plan 15 with the suggested fix (merge that preserves optimistic rows whose id is not in server response). **Rule:** flakes that pass in isolation are race conditions, not Playwright bugs. The fix should be *deterministic* (wait on the actual synchronization point in production semantics), not *retry-based*. Production races that surface under test conditions are real production bugs that need separate fixes — the test arm and production fix are independent commitments.

**CI-as-structural-gate lesson (C1).** Plan 09 close said "Plan 09 should not relax it" about the axe-core a11y gate, but Plan 09 didn't add Playwright to CI either. The local "should not relax it" turned out to be wishful: the v4 brand-skin.css drop on 2026-04-24 cascade-shadowed Plan 07 Task 25C's tokens.css nudges (Plan 13 Task 6's diagnosis) and shipped 9 a11y violations silently because nobody ran Playwright before merge. The structural fix is *CI enforcement* — Playwright on every push + every PR, both Mac and Windows, the entire e2e suite. Plan 14 D1 + D2 locked it; Tasks 7 + 8 implemented it. The cascade-shadowing class of regression cannot ship silently anymore. **Rule:** when a closure paragraph asserts "we should not weaken X" about a quality gate, that's wishful unless the gate is *structurally enforced* (CI, pre-commit hook, blocking review check). Local discipline decays over time; CI doesn't.

**Sibling-step pattern lesson (Task 8).** When a workflow step is OS-conditional via `if: runner.os == 'X'`, an unconditioned matrix entry will silently skip — the matrix runs on Windows but every step that's gated on `runner.os == 'macOS'` is no-op'd, and CI goes green with zero coverage on the unintended OS. Plan 14 Task 8's review I1 trap closure: the chflags step needed to be Mac-only (chflags is BSD/macOS syscall); naively marking it `if: runner.os == 'macOS'` left Windows with NO Playwright invocation — the windows-2022 matrix entry would pass without running any test. Fix: sibling-step pattern — explicitly add a `Playwright e2e (full suite, Windows)` step gated on `if: runner.os == 'Windows'` with `shell: pwsh` and `;`-separated PYTHONPATH. **Rule:** when an OS-conditional step is part of a multi-step pipeline whose downstream depends on it, ALWAYS pair it with a sibling step gated on the OTHER OS (mutually-exclusive `if:` conditions). Workflow file shape gate in the demo script (Plan 14 Gate 9) parses the YAML and asserts the sibling-step shape — regression guard against re-collapsing.

**chflags + PYTHONPATH escape hatch held across Plan 14 (no new uv quirks).** Lessons 341 + Plan 11/12/13 refinements held. The canonical recipe is `chflags 0 .../_editable_impl_*.pth 2>/dev/null && PYTHONPATH=packages/.../src .venv/bin/python ...` in the SAME command line. Spotlight re-hides sub-second under `~/Documents/Code/...`; a separate `chflags` step before the python step doesn't last. The PYTHONPATH escape hatch makes the test runner immune to .pth shadowing regardless. CI workflow encodes the same pattern explicitly: a Mac step that does `chflags 0 ... && npx playwright test` in a single `run:` block (multi-line shell), and a Windows step that uses absolute PYTHONPATH with `;` separator (Windows `os.pathsep`). **Rule:** the chflags + same-line + PYTHONPATH recipe is now ubiquitous — Plan 11/12/13/14 all relied on it. The CI workflow file is the canonical written form; future implementers should grep it as the reference, not re-derive from lessons.md.

**Handoff to Plan 15.** Plan 15 candidate scope captured in `tasks/todo.md`. Substantial backlog accumulated from Plan 14 task reviews — themes: (a) production race in `inbox-store.loadRecent` overwrite (Plan 14 Task 6 deferred work — deletes the test waitForResponse arm once production is fixed); (b) repair-config dialog UI surface + autonomy modal (Plan 14 Task 3 deferrals); (c) Browse → file-preview dedicated overlay + WikilinkHover tooltip a11y + per-message Fork dialog (Plan 14 Task 4 deviations); (d) hover-state token unification (`--tt-cyan-hover`) + audit other `var(--brand-ember)` foreground sites + stylelint "no hardcoded hex outside :root blocks" rule + `.prose` / `.msg-body` selector convention doc (Plan 14 Task 5 follow-throughs); (e) CI workflow caching + composite action / DRY for chflags + PYTHONPATH + `pnpm install --frozen-lockfile --filter brain_web...` consistency + Defender SmartScreen pre-step under feature-flag + PowerShell line-ending discipline lesson + CI duration observability per-job summary (Plan 14 Tasks 7 + 8 follow-throughs); (f) Plan 13 architectural follow-throughs still open — `bulk-screen.tsx` + `file-to-wiki-dialog.tsx` migration to `useDomains()`; `removeDomainOptimistic` action + `useDomainsStore.error` inline banner; `domainsLoaded` → `loaded` naming alignment; cross-domain-gate `error` field disposition; cross-tab `BroadcastChannel` pubsub; `setAcknowledgedOptimistic` early-return; `panel-domains.tsx` 3-file split; (g) brain_api hardening — pre-existing `_spa_fallback` `Response | None` mypy error at static_ui.py (overload with @overload discriminating on raise_on_miss); (h) Plan 12 small cleanups carried forward — plan-text "topbar scope chip" inaccuracy drift watch (lesson, not code); `brain start` CLI chflags handling; modal "private" vs Settings "Privacy-railed" jargon split; active-domain dropdown toast CTA wording on transport failures; `pushToast` outside try-block defensive cleanup; `pendingSendRef.mode` dead field; (i) Plan 13 test-quality follow-throughs still open — `_NO_CONFIG_MESSAGE` extraction to `tools/_errors.py`; `_mk_ctx` signature alignment; `apply_patch._resolve_config` Plan 07 Task 5 deferral docstring; (j) bigger architectural moves still open — per-domain budget caps; per-domain rate limits; repair-config UI; cross-process hot-reload; `validate_assignment=True` on Config; per-domain autonomy categories; "Set as default" topbar button; per-thread cross-domain confirmation; generic ctx.config lint rule; migration tool for old config.json files; generic zustand promotion across other hooks. Also Plan 14 Task 4 a11y test-quality items — `waitForToolResponse` helper for mount-time tool fetch races; `waitForTimeout` removal across `a11y-populated.spec.ts` (~11 sleep calls); test cleanup contract (`test.afterEach` for state-mutating tests); helper extraction once 5th caller appears (`seedBrainMd` / `seedScope`); `text-[var(--bg)]` → `text-[var(--accent-foreground)]` in `patch-card.tsx:117`.

## Plan 15 — CI green + polish pass (closed 2026-05-06, tag `plan-15-ci-and-polish`)

**Closure summary.** All 10 tasks landed against the locked D1–D11 decisions. Two threads in one cohesive plan: (#A1) ruff debt clear — 76 violations on main (67 auto-fixable + 9 manual: 3 raw-string regex escapes in test files + 6 `×` → `x` in scripts/demo-plan-{10,11,13}.py) cleared in a single commit; first green whole-repo `ruff check .` in 4 plans (Plan 11/12/13/14 implementer recipes ran `ruff check packages/<pkg>/` only and silently masked the debt). (#A2) Windows Playwright chat composer fix — Plan 14 Task 9 deferred verification closed: Task 2 diagnosed the root cause as `_spa_fallback`'s `path.split("/", 1)[0]` segment extraction running on Starlette's `os.path.normpath`-transformed path (which converts `/` → `\` on Windows via ntpath); Task 3 landed the one-line `path = path.replace("\\", "/")` normalization at the top of `_spa_fallback`. (#B1) `brain start` supervisor drops `uv run` — direct `<install>/.venv/(bin|Scripts)/python(.exe) -m uvicorn` invocation via new `_resolve_venv_python(install_dir)` helper. (#B2) Modal + Settings jargon alignment — "Privacy-railed" everywhere (mirrors `Config.privacy_railed: list[str]`); new `PrivacyRailedGlossaryTooltip` component supplies the glossary tooltip both surfaces share. (#B3) Active-domain dropdown toast — conditional CTA (validator-error → "Pick a different domain"; transport-error → "Try again") + pushToast moved outside catch (defensive scoping). (#B4) `pendingSendRef.mode` used explicitly — Task 7's first cut threaded `pending.mode` to `dispatchSend` but left a stale-mode race + throw-leak window across the `setCrossDomainWarningAcknowledged` await; Task 7 review fix-up locked the canonical pattern: capture `pendingSendRef.current` into a local synchronously, clear the ref BEFORE the await, dispatch from the local. (#B5) `raise_if_no_config(ctx, tool_name)` helper extracted to `brain_core.tools._errors` — three brain_core call sites (config_get, list_domains, config_set) refactor to call it; SPAStaticFiles non-http guard stays separate per D7 (different package, different scope-type contract). (#B6) `_mk_ctx` test fixture alignment — all three fixtures (`test_list_domains.py`, `test_list_domains_active.py`, `test_config_set.py`) require explicit `config: Config` (no Optional, no `= None` default); mirrors Plan 13 Task 1's None-policy strictness. (#B7) `apply_patch._resolve_config` docstring — drops the Plan 07 Task 5 forward-looking reference; describes the function's actual current behavior (snapshot Config with vault_root overlay). 12-gate demo (`scripts/demo-plan-15.py`) walks the sequence (ruff clean → brain_api full pytest → brain_web vitest → local Playwright subset → Windows Playwright CI conclusion → `brain start` `/healthz` end-to-end → modal + Settings jargon grep → active-domain toast conditional CTA spec → pendingSendRef.mode capture-into-local pattern → raise_if_no_config helper + 3 callers → _mk_ctx required config grep → sentinel) and prints `PLAN 15 DEMO OK`. Tool count unchanged. Schema unchanged. New module: `brain_core.tools._errors`. New component: `PrivacyRailedGlossaryTooltip`. brain_cli supervisor switches from `uv run uvicorn` to direct venv-python. Final test totals: **181 brain_api pytest passed + 3 skipped** (rises from Plan 14's 178 by +3 raise_if_no_config contract tests) + **343 brain_web vitest passed + 1 skipped** (rises from Plan 14's 334 by +9 across Tasks 5/6/7) + **32 Playwright e2e tests** (unchanged from Plan 14; Task 3 fix lands without spec changes). Plan 16 candidate scope refreshed in `tasks/todo.md`.

**Per-package vs whole-repo ruff gap (lesson).** Four plans of "ruff clean" reports masked 76 violations because the implementer recipe ran `ruff check packages/<pkg>/` (current package only) but CI runs `ruff check .` (whole repo). Plan 11/12/13/14 closures all reported clean ruff, all on per-package scope; the whole-repo CI gate caught nothing because CI was already red on those violations and the Playwright workflow (Plan 14 Task 7+8) was the only green gate. **Rule:** the per-task self-review checklist now has an explicit `uv run ruff check .` (whole repo, no path argument) item that all future plans must run. Per-package ruff is a smoke test, not a gate.

**Drop `uv run` from supervisor (lesson).** Lesson 341 (chflags + same-line + PYTHONPATH escape hatch) was already first-class for the test runner, but `brain start` still went through `uv run uvicorn`. `uv run` re-syncs the venv and re-hides the editable .pth files mid-bootstrap on macOS (Spotlight re-applies `UF_HIDDEN` sub-second after chflags clears it; uv's wheel-unpack triggers Spotlight re-indexing). Plan 15 D3 made the rule first-class in the supervisor: `<install>/.venv/(bin|Scripts)/python(.exe) -m uvicorn` directly, never `uv run`. **Rule:** any future supervisor / launcher / orchestration tool that spawns Python should default to direct venv-python invocation. `uv run` is for interactive developer shells, not production process startup.

**Microcopy + tooltip is a designer/engineer split (lesson).** Plan 15 Task 5 (Modal + Settings jargon alignment) needed two distinct subagent invocations: brain-ui-designer authored the verbatim glossary tooltip copy ("domains marked as private; never appear in default queries") + the modal/Settings replacement strings; brain-frontend-engineer applied them, atomic-commits with the new TSX component. Combining the two would have produced either copy-quality regressions (engineer's copy) or implementation drift (designer guessing component shapes). **Rule:** when a task changes user-facing strings beyond a single token, dispatch the microcopy half to the designer first; pass the locked copy to the engineer as input. Mirrors Plan 06 → Plan 07 design → frontend handoff at the task scale.

**Refs that span an await boundary (lesson).** Task 7's first cut threaded `pendingSendRef.current.mode` into `dispatchSend` — strictly an improvement over reading live-closure `mode`, but the ref read still sat AFTER the `await setCrossDomainWarningAcknowledged(true)` boundary. Two failure modes leaked through: (1) throw-leak — if `dispatchSend` threw (e.g., a WS `send()` failure), the ref kept the stale capture for the next click; (2) stale-mode race — if the user toggled mode + clicked Send during the ack-write window, the second click overwrote the ref before this turn dispatched. Task 7 review locked the canonical pattern: capture into a local synchronously, clear the ref BEFORE the await, dispatch from the local. **Rule:** when a ref's value is meant to capture click-time intent, the read MUST be synchronous with the click handler entry — any await between ref-read and dispatch is a stale-capture trap. Treat `pendingFooRef.current` reads as load-bearing for ordering: read once, store in a local, clear the ref, then await.

**Test-fixture None-config defaults hide intent (lesson).** Plan 13 Task 1 review M3 flagged that `_mk_ctx` defaults of `config=None` made it ambiguous whether a test was exercising the None-config raise path or the canonical happy path. Plan 15 Task 9 D8 removed the default and made `config: Config` required across all three fixtures. Every test call site now declares its config shape, and the None-config raise is exercised exclusively through `test_errors_raise_if_no_config.py` (where the raise IS the test subject). **Rule:** test-fixture parameters with `None` defaults that are never expected to receive `None` in normal use are an anti-pattern — they make happy-path tests look like they tolerate `None` when they don't, and they hide the narrowing the helper actually depends on. Required parameters at the fixture surface enforce intent at every call site.

**Forward-looking deferral comments accumulate as debt (lesson).** Plan 15 Task 10 closed the `apply_patch._resolve_config` docstring's "Plan 07 Task 5 will replace the body with a real loader call" comment — a forward-looking deferral that had carried for 8 plans. The resolution: drop the Plan-N reference, describe the function's actual current behavior, file the "real loader call" deferral as Plan 16+ candidate scope. The lesson: forward-looking "Plan N Task M will…" comments age into stale debt when the deferral becomes implausible (5+ plans without action). **Rule:** in code comments, prefer "current behavior is X (deliberate)" over "will be replaced by Plan N Task M"; track architectural deferrals in `tasks/todo.md` candidate-scope sections, not in source comments. If a forward reference must live in code, it should be re-validated at every plan close.

**Combined spec + code-quality review per task (process observation).** Plan 11 + 12 used a two-stage review split (separate spec-pass + separate code-quality-pass per task). Plan 13 + 14 combined them. Plan 15 continued the combined cadence. The combined cadence is sufficient when task scope is < 50 lines diff (which is true for most polish tasks) — the reviewer holds the whole task-and-context in one head and catches both classes of issue in one read. Two-stage review still has its place for larger architectural moves (Plan 11 Task 7 brain_api Config wiring, Plan 13 Task 5 SPAStaticFiles mount flag) where the spec-correctness reading and the code-quality reading need different mental modes. **Rule:** scale review depth to task scope. < 50 LOC + no architectural decisions → combined review; > 50 LOC or any contract change → two-stage. Plan 15's 10 tasks averaged ~30 LOC and combined-review caught all M-class issues that surfaced.

**Handoff to Plan 16.** Plan 16 candidate scope refreshed in `tasks/todo.md` — spans (a) the production race in `inbox-store.loadRecent` overwrite (Plan 14 Task 6 deferred work); (b) `_spa_fallback Response | None` mypy `@overload` fix (Plan 14 Task 5 review M2); (c) Plan 13 architectural follow-throughs still open (orphan `listDomains` consumers, `removeDomainOptimistic` action, `useDomainsStore.error` inline banner, `domainsLoaded` → `loaded` naming, cross-tab `BroadcastChannel` pubsub, `panel-domains.tsx` split); (d) Plan 14 a11y deferrals (repair-config dialog, autonomy modal, file-preview overlay, WikilinkHover tooltip, per-message Fork dialog smoke); (e) CSS structural cleanup (`--tt-cyan-hover` token, color-contrast audit, stylelint hardcoded-hex rule, `.prose` / `.msg-body` convention doc); (f) CI follow-throughs from Plan 14 (workflow caching, composite action DRY, `gh workflow run --validate`, `pnpm install --filter` consistency, Defender SmartScreen pre-step, PowerShell line-ending discipline lesson, CI duration observability); (g) test-quality follow-throughs (`waitForToolResponse` helper, `waitForTimeout` removal, test cleanup contract, helper extraction at 5th caller, `text-[var(--bg)]` → `text-[var(--accent-foreground)]`); (h) bigger architectural moves (per-domain budget caps, per-domain rate limits, repair-config UI, cross-process hot-reload, `validate_assignment=True`, per-domain autonomy categories, "Set as default" topbar button, per-thread cross-domain confirmation, generic ctx.config lint rule, migration tool, generic zustand promotion, **`pendingSendRef`-as-local refactor across other handlers**); (i) Plan 15 review residuals (microcopy doc SVG mockups still show "private" copy in state-1-initial.svg + state-2-settings-after-toggle.svg; 3 pre-existing TS errors in `tests/e2e/cross-domain-modal.spec.ts`; act() warnings in chat-screen.test.tsx; `test_config_get.py` `_mk_ctx` not yet aligned to Path A; toast detail-vs-CTA period normalization; Plan 07 Task 5 forward-looking deferrals in `config_set.py:81/90` + `schema.py:101`; positive unit test for `PrivacyRailedGlossaryTooltip`).

## Plan 16 — Comprehensive carry-forward (closed 2026-05-07, tag `plan-16-comprehensive-carry-forward`)

**Workflow YAML structural gate via actionlint, NOT `gh workflow run --validate` (lesson).** Plan 16 Task 16 / D16 added a pre-commit hook to catch malformed `.github/workflows/*.yml` before push — the spec wording said "gh workflow run --dry-run --validate" but that flag does not exist on `gh` (verified 2026-05-06): `gh workflow run` triggers a `workflow_dispatch` event and has no local-syntax validator; `gh workflow view` queries the API, not local files. The canonical local validator is **actionlint** (`https://github.com/rhysd/actionlint`) — it parses workflow YAML, validates `uses:` references, type-checks `${{ ... }}` expressions, and shells out to shellcheck for `run:` blocks. The hook is wired via the official `rhysd/actionlint` pre-commit repo at `v1.7.7`. Verified: clean baseline on existing workflows; deliberately deleting `runs-on: ${{ matrix.os }}` from `playwright.yml` causes `actionlint` to exit 1 with `"runs-on" section is missing in job "e2e" [syntax-check]`. Caveat: actionlint v1.7.x **does not** parse composite-action manifests (`.github/actions/*/action.yml`) standalone — it expects the workflow shape (`on:` + `jobs:`) and reports spurious "missing jobs section". Composite actions are validated transitively at their `uses:` site in the calling workflow, but property access and `run:` blocks inside the composite are NOT linted by this gate. **Rule:** when a spec quotes a CLI flag, verify the flag exists before implementing — `gh`, `pnpm`, `uv`, and `actionlint` all have evolving surfaces and stale spec wording is a real failure mode. The pre-commit gate is the local fast-feedback layer; CI does NOT run pre-commit (CI has its own actionlint-equivalent in the form of GitHub Actions' own server-side parser, which fails the workflow at push if YAML is malformed).

**`pnpm install --filter brain_web...` is functionally a no-op TODAY but locks intent (lesson).** Plan 16 Task 16 narrowed `pnpm install --frozen-lockfile` → `pnpm install --frozen-lockfile --filter brain_web...` in the `setup-brain-test-env` composite action. The `pnpm-workspace.yaml` lists `apps/* + packages/js/*`, but `packages/js/` does not yet exist and the root `package.json` has no `dependencies`/`devDependencies` — so the effective workspace is `{root, apps/brain_web}` and `pnpm install --frozen-lockfile` already only resolves brain_web's tree. The filter changes nothing observable today (verified: `pnpm install --frozen-lockfile --filter brain_web...` finishes in 448ms, same shape as before). What it buys: (a) explicit intent — Playwright only needs brain_web; (b) forward-protection — if a future `packages/js/<thing>/` lands without Playwright relevance, the workflow does not balloon. The trailing `...` selects brain_web AND every workspace package brain_web depends on. **Rule:** prefer explicit `--filter <pkg>...` in CI install commands even when the workspace is single-package today — single-package workspaces drift to multi-package over time, and adding the filter retroactively is a coordinated workflow edit, not a free change.

**PowerShell line-ending discipline — UTF-8-BOM-on-PS5.1 vs UTF-8-no-BOM-on-pwsh (preventive lesson).** Plan 16 Task 17 / D17 captures this even though no current workflow exercises the bug. PowerShell 5.1 (Windows-built-in, Desktop edition) and pwsh 7+ (cross-platform Core edition) handle script encodings differently: PS5.1 expects scripts as UTF-8 **with** BOM — UTF-8 without BOM may be misread as ANSI/Windows-1252 and corrupt non-ASCII characters silently. pwsh 7+ defaults to UTF-8 **without** BOM. Today's GitHub Actions workflows use `shell: pwsh` exclusively (verified: `playwright.yml` Windows e2e step + `start-backend-for-e2e.ps1` invocation), so this hasn't bit yet. But the foot-guns are clear: (a) if a workflow ever switches to `shell: powershell` (PS5.1) — for example to call a Windows-built-in cmdlet that pwsh 7 doesn't ship — every `.ps1` file the step touches must be saved as UTF-8 **with** BOM; (b) if a `.ps1` file is committed via a Mac editor that defaults to UTF-8-no-BOM and a future workflow runs it under PS5.1, characters above 0x7F (em-dash, smart-quote, accented letters) will silently mangle; (c) `git autocrlf` interacts: CRLF in the working tree + UTF-8-no-BOM is the worst combo for PS5.1. Recommended `.gitattributes` directive if PS5.1 ever enters the picture: `*.ps1 text eol=crlf working-tree-encoding=UTF-8-BOM`. **Rule:** when adding a Windows shell step, prefer `shell: pwsh` over `shell: powershell` (PS5.1 is in maintenance mode anyway), and if a `.ps1` file is committed for workflow consumption, audit its encoding (`file <path>` on Mac/Linux, `Get-Content -Encoding Byte <path> | Select-Object -First 3` on Windows — UTF-8-BOM starts with `0xEF 0xBB 0xBF`). This lesson is preventive — captured per Plan 16 D17 even though no current workflow exercises the bug.

**Closure summary.** All 47 substantive tasks landed against the locked D1–D36 decisions (plus 4 carry-forward add-ons: T28.5 + T31.5 + T34.5 + T39.5 — see "Foundation + wiring is two tasks" lesson below). Plan 16 closes the entire 50+-item Plan 15 candidate-scope tail block in a single plan: every theme listed under that handoff is shipped as production code, not deferred or "scaffolded for later." Theme breakdown: (T1) production correctness — `inbox-store.loadRecent` id-keyed merge; (T2) brain_api hardening — `_spa_fallback @overload`; (T3-T8) Plan 13 follow-throughs — `useDomains()` migration of orphan consumers + `removeDomainOptimistic` + `useDomainsStore.error` banner + `domainsLoaded → loaded` rename + cross-domain-gate `error` + `BroadcastChannel` cross-tab pubsub + `panel-domains.tsx` 4-file split; (T9-T11) Plan 14 a11y deferrals — `repair-config-dialog`, `autonomy-modal`, `file-preview-overlay`; (T12-T13) CSS structural cleanup — `--tt-cyan-hover` token + stylelint `color-no-hex` rule; (T14-T18) CI follow-throughs — workflow caching + `setup-brain-test-env` composite action + actionlint pre-commit + Defender SmartScreen feature-flag + `$GITHUB_STEP_SUMMARY` duration writeback; (T19-T20) test-quality — `waitForToolResponse` helper + `afterEach` cleanup contract + `--accent-foreground` token; (T21-T25) Plan 15 review residuals — SVG mockups + cross-domain-modal.spec.ts TS errors + chat-screen.test.tsx act() warnings + test_config_get _mk_ctx alignment + Plan 15 trio sweep. **Theme 10 full production (T26-T46)** at the locked 1.B interpretation: per-domain budget caps (T26-T29), per-domain rate limits (T30-T32) including `LeakyBucket`, repair-config dialog full polish (T33), config invalidation + cross-process hot-reload (T34-T35) via symmetric `ConfigWatcher`, `validate_assignment=True` on all 12 ConfigDicts (T36), per-domain × per-category autonomy with HYBRID gate (T37-T40 with D-1 + D-2 + D-3 + D-4 sign-offs), `brain config migrate` CLI (T41), "Set as default" topbar (T42), zustand promotion of budget + domain-overrides stores (T43), `pendingSendRef`-as-local audit (T44), and `BRN001` ruff custom rule (T45-T46) wired to a CI hard-fail gate with zero violators across the codebase. T47 (this closure) ships the 47-gate demo + this lessons section + `tasks/todo.md` Plan 17 carry-forward + three §3 / §6 spec footnotes. Final test counts: brain_core **982 passed + 5 skipped**; brain_api **183 + 3 skipped**; brain_mcp **137 + 3 skipped**; brain_cli **129**; vitest **460 + 1 skipped** (77 test files). 59 commits over Plan 16. Plan-author drift adjudicated 8+ times (paths drifted between authoring and execution, ruff plugin-API turned out to be Rust-only, file names diverged from spec wording, etc.). The `BRN001` zero-violator audit confirmed the architecture's read-the-config discipline is structurally clean — no ad-hoc `ctx.config` reads outside the allowlisted entry-points.

**Production race fix (T1; lesson).** `inbox-store.loadRecent`'s `set({ sources: items })` unconditionally overwrote the store every time the server responded, racing `addOptimistic` writes that hadn't yet been confirmed by a subsequent refresh. The fix is an id-keyed merge: build a `Set<string>` of server-side ids, then prepend the optimistic rows whose ids do NOT appear in that set. The Plan 14 Task 6 `waitForResponse` band-aid in `ingest-drag-drop.spec.ts` is now deletable — the production race that caused the test flake is closed at the source. **Rule:** production races that surface under test conditions are real bugs, not test infrastructure problems; fix production first, delete the test arm second. Carrying both is debt.

**Full-production interpretation of carry-forward at scale (T26-T46 + 1.B; lesson).** Plan 16's 47-task shape was sustainable because (a) most tasks shipped < 50 LOC + combined review held the cadence; (b) Theme 10's expansion from 7 SCAFFOLD tasks to 21 production tasks split each schema/enforcement/UI as a discrete reviewable unit (T26 schema → T27 ledger query → T28 guard → T28.5 wiring → T29 UI for budget caps; same shape for rate limits, autonomy categories, hot-reload). **Rule:** when the user picks "ship the feature working end-to-end" over "ship the data shape and defer", the natural decomposition is one task per layer (schema → migration → enforcement → wiring → UI), not one task per concern. The wiring layer is the load-bearing one — without it, the schema and guard ship as no-ops in production.

**Per-domain × per-category schema reshape (T37-T41; lesson).** The autonomy schema went from `Config.autonomous: AutonomousConfig` (flat 5-bool BaseModel) to `Config.autonomous: dict[str, AutonomyCategoryFlags]` (per-domain × per-category, 5 flags per domain) in one plan because (a) T37 brainstormed and locked the shape with HYBRID gate semantics + read-time backwards-compat as named deliverables; (b) T38 landed the schema migration with the read-time fallback; (c) T39 reshaped the gate (`should_auto_apply(*, domain=...)` keyword-only); (d) T40 shipped the UI; (e) T41 added an explicit `brain config migrate` CLI for users who want to roll over their on-disk config. **Rule:** schema-shape changes that touch many call sites are tractable IF the migration is explicit (CLI tool) AND the read-time fallback covers users who never run the CLI. Both are required — neither alone is enough.

**Leaky-bucket vs sliding-window for rate limiting (T31; lesson).** Locked leaky-bucket recommendation upheld at implementation time. The decision rationale ("queue smooths bursty traffic; sliding-window is harder to reason about correctness for; Anthropic's own limiter is bucket-shaped") proved load-bearing — the implementer routed back zero times, the design held under review, and the per-domain registry is a clean module-level dict keyed by `(provider, domain)`. **Rule:** when a brainstorm produces a recommendation backed by three concrete reasons, that's signal-of-quality — implementers will likely not need to revisit. Sliding-window's "what counts as 'last 60 seconds'" question alone would have produced 3+ implementation-time decisions; leaky-bucket's "tokens per second + bucket capacity" surface is a single-decision gate.

**Cross-process hot-reload via symmetric watchdog (T35; lesson).** The original plan-doc text described an asymmetric setup where brain_api manages the brain_mcp PID and signals it via SIGHUP. Reality is that brain_api and brain_mcp are independent processes with independent lifecycles — neither owns the other. The symmetric `ConfigWatcher` abstraction (each process runs its own watcher in its lifespan, watches the same on-disk file, calls its own `_reset_ctx_cache` callback when the file mtime/version changes) is cleaner: no IPC, no PID management, no signal-handler portability footgun. **Rule:** when a spec proposes process-A-signals-process-B, audit whether the two processes have a clear ownership relationship. If they don't (independent supervisors, independent `__main__`s), the right shape is "each process watches the shared resource" — Unix watchdog + Windows watchdog both work via filesystem events; no cross-process IPC is needed.

**`validate_assignment=True` enabled despite perf overhead (T36 / 1.B; lesson).** Plan 16 Task 36 measured the cost: 200-260% relative overhead, but only 0.24μs absolute per assignment (1000-iteration mirror-BaseModel benchmark). Per locked 1.B + 3.A, the flag ships unconditionally across all 12 ConfigDict instances. The Pydantic v2 `model_validator(mode="after")` no-rollback quirk (cross-field validator failures don't roll back the triggering field mutation) was discovered + documented at 4 seams during implementation; CLAUDE.md "What NOT to do" gained an entry. **Rule:** "ship the correctness improvement" beats "preserve a perf budget that may not matter in practice" when (a) absolute cost is microsecond-scale; (b) call cadence is human-interaction (Settings UI saves), not LLM-call hot-path. Document both relative AND absolute numbers in the commit body — relative-only ("260% overhead!") looks alarming and triggers reflexive rollback discussions; absolute ("0.24μs/call") immediately recontextualizes.

**BroadcastChannel cross-tab pubsub (T6; lesson).** Plan 13's `domains-store` introduced cross-instance pubsub via `BroadcastChannel` to close the optimistic-clobber race when the user has Settings open in two tabs and toggles a domain. Plan 16 T6 audited sibling stores for the same race shape and found `cross-domain-gate-store` had it too — the privacy-rail acknowledgment toggle. Both stores now share `_broadcast.ts` and synchronize via `domain-state` / `cross-domain-gate-state` channels. **Rule:** when one store has a known race shape, audit ALL sibling stores that mutate persisted Config fields for the same shape. The second one is usually waiting in the wings — don't ship the fix as a one-off; ship the helper + audit pass.

**Stylelint as structural enforcement (T13; lesson).** Plan 14 lesson C3 (theme-aware tokens beat hardcoded `[data-theme="dark"]` overrides) was a code-review discipline. Plan 16 T13 turned it into structural enforcement via `stylelint --rule color-no-hex`. The rule fails on any hex literal outside `:root` blocks — `--brand-ember`, `--tt-cyan`, etc. all stay; ad-hoc `#E06A4A` site-shadowing fails CI. **Rule:** when a discipline relies on "remember to use the token", structural enforcement (lint rule, pre-commit hook, CI gate) is the only durable fix. Code-review catches drift only when the reviewer sees the diff; the rule catches drift on every commit.

**CI duration observability (T18; lesson).** Plan 14 Task 7+8 added Mac+Windows Playwright matrix runs; Plan 16 T18 surfaces per-job wall-clock to `$GITHUB_STEP_SUMMARY` so reviewers can scan step-level cost without clicking into each job. The summary is a markdown table, two columns (step name, duration), written by the LAST step in each job (so its OWN duration won't appear — accepted footnote in the workflow comment). **Rule:** the act of measuring CI duration creates pressure to keep it down. Caching (T14) and the composite action (T15) follow naturally once duration is visible — implementers see "oh, this step is 2 minutes; let me cache it" rather than "is CI fast or slow?"

**Ruff custom rule BRN001 (T45-T46; lesson).** The plan-doc proposed BRN001 as a Ruff custom rule. Implementation discovery: Ruff has NO Python plugin API — Astral's plugin path is Rust-only (verified 2026-05). The rule landed instead as a standalone AST checker module (`brain_core._lint.brn001`) wired via pre-commit, matching the actionlint hook shape. The CI gate runs `python -m brain_core._lint.brn001` over the codebase as a hard-fail step. The zero-violator audit (T46) confirmed clean architecture — `ctx.config` reads happen only at the 5 allowlisted tool entry-points. **Rule:** when a spec quotes a tool's plugin API, verify the API exists before sizing the work — Ruff's marketing positioning ("extensible") doesn't mean Python-extensible. The standalone-AST-checker fallback is a clean abstraction anyway, and pre-commit + CI is the same enforcement surface as a "real" Ruff rule.

**`pendingSendRef`-as-local audit + apply (T44 / 1.B; lesson).** Plan 16's v1 (pre-1.B) had T44 as audit-only; under 1.B the audit was supposed to be followed by an apply pass. The audit found ZERO needs-fix sites — Plan 15 Task 7 had already addressed the only at-risk ref (`pendingSendRef.current` capture-into-local pattern). The closure shipped as audit-only with a thorough doc explaining why no fixes were needed. **Rule:** "file an audit doc" is rarely the right ending — audits without follow-up rot into wishlists. But if the audit's honest answer is "zero sites need the fix", shipping audit-only with the rationale IS the right ending. The middle state (audit doc + ambiguous "we should fix this someday") is the trap.

**Combined review held across 47 tasks (process observation).** Plan 15 lesson 5 said combined spec + code-quality review is sufficient for < 50 LOC tasks; Plan 16 stress-tested it at 47 tasks. Plan 16's tasks averaged ~50 LOC for the architectural moves and ~30 LOC for the polish. Combined-review caught all M-class issues that surfaced; only T33 + T36 came back APPROVED_WITH_NITS rather than fully approved, and both nits were < 1-line follow-up commits. **Rule:** combined review scales with plan size IF per-task PR shape stays narrow. Plan 16 confirms the upper-bound claim from Plan 15 — combined cadence works at 47 tasks when the average task scope holds at < 50 LOC.

**Foundation + wiring is two tasks, not one (T28.5 / T31.5 / T34.5 / T39.5; NEW lesson).** Schema-first features need an explicit wiring task. The foundation task ships pin tests that cover the new gate / schema field / loader function, but the gate is a no-op in production until call sites thread the right context. This pattern recurred FOUR times in Plan 16: T28 PerDomainBudgetGuard pinned but no LLM entry-point called `guard.check_for(...)` until T28.5; T31 LeakyBucket pinned but no `LLMRequest.domain` was set in production until T31.5; T34 `resolve_config` landed but production callers (brain_api lifespan, brain_cli chat, brain_mcp server) still called `load_config` directly until T34.5; T39 reshaped `should_auto_apply` but `apply_patch._resolve_config` still returned a defaults-only Config, brain_api lifespan never built an `AnthropicProvider`, and brain_mcp's `_cached_ctx` had no invalidation hook until T39.5. **Rule:** when authoring a plan, explicitly enumerate the production wiring sites for any new gate / schema / loader. The foundation-task PR is incomplete until a follow-up "wire X at every production call site" task lands. Auto-memory `feedback_foundation_plus_wiring.md` codifies the heuristic for future plans.

**Pydantic v2 `model_validator(mode="after")` no-rollback quirk (T36; NEW lesson + CLAUDE.md addition).** Pydantic v2's field-level validators DO roll back on raise — the bad value never reaches the live model. But cross-field validators decorated `@model_validator(mode="after")` do NOT — the triggering field mutation persists, and the validator just raises. With `validate_assignment=True` enabled, this matters: a bad assignment to `Config.active_domain` that violates a cross-field invariant (e.g., active_domain not in domains list) leaves `active_domain` mutated to the bad value AND raises. Pattern to mitigate: pre-check the invariant before assignment + use a `persist_config_or_revert` snapshot that bypasses validation on the revert path (`__dict__` writes since the snapshot was already valid). Now in CLAUDE.md "What NOT to do." **Rule:** future schema changes that introduce cross-field validators must audit every assignment site with `validate_assignment=True`. A pre-check + snapshot pattern is the canonical shape; ad-hoc try/except is fragile.

**iCloud-induced .pth duplication blocks chflags (NEW lesson; test recipe shift).** The original Lesson 341 chflags recipe (`chflags 0 .../uv-editable-*.pth && pytest`) FAILS on this repo because `Documents/Code/...` is iCloud-synced and Spotlight produces duplicate `_editable_impl_brain_core 2.pth` / `3.pth` / ... up to `7.pth` files with file-flags that prevent `chflags 0` ("Operation not permitted"). The PYTHONPATH workaround (`unset VIRTUAL_ENV && PYTHONPATH=packages/brain_core/src:... uv run --package <pkg> pytest`) bypasses the .pth indirection entirely and is now the primary recipe. Auto-memory `feedback_uv_uf_hidden.md` captures the corrected recipe. **Rule:** when a "canonical recipe" depends on a filesystem operation that requires elevated permissions on the user's specific setup, the recipe is fragile. Provide a fallback that bypasses the failing layer (here: PYTHONPATH bypasses the .pth file entirely).

**Plan-author drift at scale (NEW lesson; 8+ instances over Plan 16).** When a 47-task plan is written quickly, file paths and tool surfaces drift between authoring and execution. Plan 16 hit this 8+ times: ruff custom-rule API turned out to be Rust-only (T45 spec said "Ruff custom rule BRN001"); plan-doc paths for components drifted (`patch-card.tsx` was assumed at top-level; actually under `pending/`); tool surfaces drifted (`gh workflow run --validate` doesn't exist; actionlint is the canonical local validator); component nomenclature drifted (`brain_api routes/config.py` was assumed; reality uses `tools/*`). **Rule:** implementers MUST grep before assuming a file exists. The "Modify: X" framing in plan docs is a probability claim, not a guarantee — author + implementer drift in the 1-2 weeks between plan-write and plan-execute is normal. Adjudicate by greping the actual repo state, not by treating the plan-doc path as ground truth.

**`validate_assignment=True` perf cost is ~3x at the assignment site, but ~0.24us/call in absolute terms (lesson).** Plan 16 Task 36 / D29 enabled `Config.model_config = ConfigDict(extra="forbid", validate_assignment=True)` unconditionally on `Config` and all 11 sub-configs (`LLMConfig`, `RateLimitOverride`, `ProviderConfig`, `BudgetOverride`, `BudgetConfig`, `URLHandlerConfig`, `TweetHandlerConfig`, `PDFHandlerConfig`, `HandlersConfig`, `AutonomousConfig`, `DomainOverride`). Per locked decisions 1.B + 3.A, the flag ships regardless of measurement outcome. Measured (1000 random valid field assignments, fixed PRNG seed `0xBEEF`, mirror-shape `BaseModel` for both arms): **baseline ~0.10ms total / guarded ~0.32ms total → ~0.22ms delta → ~200-260% relative overhead → ~0.24us per call**. The relative number looks alarming but the absolute is trivial: `Config` writes happen at human-interaction cadence (Settings UI saves, `brain_config_set` calls), not at LLM-call cadence. Two correctness wins land alongside: (a) bad values never reach the live `Config` at all instead of persisting silently and only failing on the next process boot; (b) the Pydantic v2 quirk where field-level validators roll back on raise but `model_validator(mode="after")` does NOT — so cross-field violations leave the field mutated to the bad value — is documented at every seam that depends on it (`config_set._check_active_domain_membership` keeps its pre-check; `persist_config_or_revert`'s revert path bypasses validation via `__dict__` writes since the snapshot was already valid). The KNOWN-LIMITATION pin tests (`test_invalid_value_currently_persists_without_validation` for `budget.daily_usd`; `test_privacy_railed_removing_personal_load_rejects` for the privacy rail) flipped to positive validation pins (`test_validate_assignment_enforcement`, `test_privacy_railed_removing_personal_raises_on_assignment`). **Rule:** when "ship the correctness improvement" beats "preserve a perf budget that may not matter in practice", measure-and-document beats measure-and-gate — but document the absolute cost (us/call), not just the relative percentage, because a 200% overhead on a microsecond operation is a different decision from a 200% overhead on a millisecond operation. Consider revisiting if `Config`-instantiation or field-assignment hot-paths emerge (none today; the assignment site is bounded by Settings-UI write cadence).

## Plan 17 — Residuals + spec annotations (closed 2026-05-11, tag `plan-17-residuals-and-spec-annotations`)

**Closure summary.** All 18 substantive tasks landed in 28 commits across 4 themes: production wiring closure (T1-T8), mypy + test debt closure (T9-T11), spec annotations (T12-T15), and misc tidy-ups (T16-T18). Final demo: `scripts/demo-plan-17.py` → `PLAN 17 DEMO OK`. Test counts unchanged at the plan-level baseline (brain_api 187 passed / 4 skipped after T11's mypy-driven test annotations; brain_core suite ~990+; brain_mcp 137 + 3 skipped; vitest unchanged plus T3/T4/T5/T6/T18 added ~30 unit tests). Three spec footnotes added inline (T12 §3 HYBRID algorithm; T13 §10 multi-domain enforcement; T14 §10 resolve_config canonical entry). One CI gate added (brain_api mypy --strict hard-fail step). Five frontend components migrated to zustand stores (panel-budget, BudgetCapsSubsection, domain-override-form). One audit closed as already-done (T10) and one as audit-only with docstring (T9). No schema changes, no new dependencies.

**Audits that close as already-done are valid (T10; lesson).** Plan 17 T10 was sized for "migrate test_mcp_session_list_domains to raise_if_no_config." The implementer correctly stopped before fabricating a migration when the audit revealed that the test passes today and the migration was already shipped in Plan 15 Task 8 (`packages/brain_core/tests/tools/test_errors_raise_if_no_config.py::test_list_domains_uses_helper` is the existing pin). Closing the task as ALREADY-DONE with a plan-doc note pointing at the existing coverage IS the right move — fabricating a migration would have just been busywork. **Rule:** when a task's premise turns out to be stale (e.g., the migration shipped already in a prior plan), the correct close is ALREADY-DONE with a pointer to existing coverage, not a fabricated equivalent. Plan-author drift across 4-5 weeks is normal; verify premises before implementing.

**Audit-only with docstring is a valid task outcome (T9; lesson).** T9 specifically permitted "audit-only with a doc note enforcing the read-only contract." The audit found zero mutations across all `_resolve_config` consumers, and shipped as a 6-line docstring addition + a plan-doc findings section. This is the right shape when the audit verdict is "no mutation found" — write the contract into the docstring so future readers can't drift, document the audit so future readers don't re-do it. **Rule:** the "audit" deliverable shape should be specified up front (docstring? plan-doc section? regression test?) AND the "what if the audit finds nothing" branch should be specified. Plan 17 T9's spec did both, so the implementer didn't have to invent the closure shape.

**`-> Any` is a soft suppression even in tests (T11; lesson).** Plan 17 T11 closed brain_api mypy --strict debt. The implementer's first pass added `-> Any` to 3 monkeypatch async-generator stubs (replacing `ChatSession.turn`'s return signature). The code-quality reviewer caught it: `ChatSession.turn` IS `AsyncIterator[ChatEvent]`, so `-> Any` is exactly the soft suppression style the debt-closure task was meant to eliminate. The proper return type compiles cleanly under `--strict`. **Rule:** in a debt-closure PR, "make mypy stop complaining" is the WRONG framing. The correct framing is "give every symbol its most-precise type." `Any` is acceptable for boundaries (json, pickle, untyped third-party returns); inside the codebase, when the actual type is known, use it. Test stubs aren't exempt — if a future refactor renames `ChatEvent.kind`, the typed stubs catch the drift; `Any` stubs don't.

**Monkeypatch target vs import-binding lookup site (T17; NEW lesson).** When monkeypatching a function that's called inside a helper, the patch must land at the namespace the helper *looks up* the function from at call time — not where the call site lexically appears. Pre-Plan-17, `repair_config_apply` imported `save_config` directly (`from brain_core.config.writer import save_config`); the imported binding is a snapshot reference, so a `monkeypatch.setattr("brain_core.config.writer.save_config", boom)` would NOT have intercepted the handler's call. After T17, `repair_config_apply` calls `save_config` via the `persist_config_or_revert` context manager, which lives in `brain_core.config.writer` and calls its module-local `save_config` — so the patch at `brain_core.config.writer.save_config` now fires. The T17 senior review flagged that the regression test's RED-before / GREEN-after demonstration was a *false-positive RED*: pre-refactor the patch missed because the handler's direct binding hid `save_config` from the patch target, then post-refactor the patch finally lands because the indirection through `persist_config_or_revert` re-routes the lookup through `writer`'s namespace. Production correctness was unaffected (the helper IS correct; the bug being fixed IS real), but the test pattern was brittle. Fix shipped as `test_apply_save_failure_reverts_under_handler_local_patch` — patches BOTH `brain_core.config.writer.save_config` (today's call site) AND the handler's own namespace via `setattr(handler_mod, "save_config", boom, raising=False)` (future-proof against re-introducing a direct binding inside the handler). **Rule:** when monkeypatching a helper's internal call, patch BOTH the helper's namespace AND any caller-local namespace that might shadow it. `raising=False` makes the caller-local patch a no-op today while locking in protection against a future refactor. Without this discipline, a regression test can pass for the wrong reason (the patch never fires) and a real reintroduction of the bug would NOT cause the test to fail.

## Plan 18 — Typed-surface drift closure (closed 2026-05-11, tag `plan-18-typed-surface-drift-closure`)

**Closure summary.** All 5 tasks landed in ~16 commits across 2 themes: typed-surface drift closure (T1-T3) + Plan 17 residual close-out (T4) + closure (T5). T1 closed the surfaced `recent()` bug + the MEDIUM-severity `doc-picker-dialog.tsx` production bug. T2 audited every typed wrapper in `apps/brain_web/src/lib/api/tools.ts` against backend `ToolResult.data` shapes (38 wrappers, 15 DRIFT rows found — far above the "expected 0-or-few" the plan author predicted). T3 split into 11 sub-fixes (T3.1-T3.11) covering all T1-class DRIFTs; deferred 4 informational extra-in-backend DRIFTs to Plan 19. T4 confirmed T36 stale-docstring close-as-ALREADY-DONE via live grep. T5 closure landed `scripts/demo-plan-18.py` → `PLAN 18 DEMO OK`. Three live production consumer bugs closed (Inbox silently empty, undo toast always "Reverted 0 file(s).", budget-override toast computed from prop default). 11 new Python key-set pin tests added. Zero new dependencies, zero schema changes.

**Typed-wrapper-vs-runtime drift class lesson (T1-T3.11; NEW lesson).** When one typed-surface drift surfaces (T1's `recent()` bug), audit ALL sibling typed wrappers for the same shape (T2's `tools.ts` audit). Plan 16 T6's rule — "when one store has a known race shape, audit ALL sibling stores for the same shape" — generalizes directly: a typed wrapper IS a sibling store in the type system. The audit returned 15 DRIFT rows from a single pass — far above the "expected 0-or-few" the plan author predicted when authoring T3 as a conditional branch (zero-findings vs 1+ findings). 3 of those 15 were LIVE consumer bugs already shipped to production. **Rule:** typed-wrapper-vs-runtime drift is a *class* of defect, not an *instance*. When one drifts, audit the rest; sample size of one is misleading. Plan 18's audit took ~30 minutes and surfaced 3 live bugs the unit tests couldn't catch because the mocks were also wrong-shape.

**Audit findings exceeding planned scope: user adjudication, not auto-expansion (T2 → T3 tiered; NEW lesson).** When an audit returns far more findings than the plan author predicted, the right move is to surface the count to the user with explicit options (fix all / fix subset / defer the rest), not silently expand the plan to absorb every finding. T2 found 15 DRIFTs against an "expected 0" plan; the user's tiered decision (fix 11 T1-class inline; defer 4 informational extras to Plan 19) kept Plan 18 finite while still closing every real bug. The "informational" DRIFTs aren't bugs (extra-in-backend with no consumer reading them) — punting them is correct. **Rule:** plans size the work, the work doesn't size the plan. When the audit blows past the plan's predicted shape, AskUserQuestion with options; don't quietly grow the plan to 20+ tasks. The four deferred informational DRIFTs in Plan 19 will land in their natural-sized plan, not as bonus tasks on Plan 18.

**Three live consumer bugs from a single audit pass (T3.1, T3.7, T3.11; NEW lesson).** Type-level drift IS user-visible drift the moment a consumer reads the missing field. T3.1: `recentIngests` declared `{items, ...}` but backend emits `{ingests, ...}` AND inner row used `it.at` but backend emits `classified_at` — Inbox UI silently empty. T3.7: `undoLast` declared `{reverted_files: string[]}` but backend never emits that field — toast always read `data.reverted_files?.length ?? 0` → always "Reverted 0 file(s).". T3.11: `budgetOverride` declared `{amount_usd, duration_hours, expires_at}` but backend emits `{override_until, override_delta_usd, note}` — toast computed from prop default instead of backend response. Each was silently shipped to production because the TS-side test mocks were *also* wrong-shape (mocked the declared type, not the real backend). **Rule:** test mocks that don't match the real backend shape mask consumer bugs. The cure: real-shape regression pins (the T3.1-T3.11 pattern: Python key-set test of the actual handler's `ToolResult.data.keys()`, paired with TS narrow that forces compilation to match).

**Discriminated unions for multi-branch backends (T3.5, T3.7, T3.9; NEW lesson).** Three sub-fixes adopted the same pattern: when a backend handler has multiple `data` shapes discriminated by a literal-string `status` field, model the TS side as a discriminated union (`type X = | { status: "a"; ... } | { status: "b"; ... }`). The type system then enforces narrowing at each consumer (`if (data.status === "a") { ... }`). T3.5 ingest had 3 branches (applied/pending/error). T3.7 undoLast had 2 branches (reverted/nothing_to_undo) — its live consumer was the toast that conflated them. T3.9 bulkImport had 3 branches (refused/planned/applied) and even had a consumer workaround cast acknowledging the drift. **Rule:** when the backend has N branches keyed on a literal `status` field, the TS wrapper SHOULD be a discriminated union of N members. The alternative (permissive `as`-cast + nullable reads) silently passes for the failure cases and surfaces wrong-shape reads only in production. Cost: ~20 LOC per backend; benefit: every missed branch is a compile error at every consumer.

**Key-set pin tests as canonical drift-protection (T3.2-T3.11; NEW lesson).** The smallest, most durable drift-pin pattern is `assert set(result.data.keys()) == {"key1", "key2", ...}` (strict equality, not subset). Costs ~10-20 LOC per handler; fails RED on any key add/remove/rename. Multi-branch handlers get one pin per branch (T3.5 / T3.7 / T3.9). The pattern was adopted on all 11 T3 sub-fixes — a uniform shape future implementers can find with `grep -rn test_data_keys_pin` and extend to new handlers without re-inventing. **Rule:** when a typed wrapper exists, pin its backend shape with a strict key-set equality test. The TS narrow + Python key-set pin is a two-sided fence: future refactors must update both sides simultaneously or one side fails.

**No-consumer narrow fixes are cheap (T3.2 / T3.3 / T3.4 / T3.8 pattern; NEW lesson).** When a typed wrapper has no live consumer in the frontend, the fix is pure TS narrow + Python key-set pin — no UI test, no consumer chase, no follow-up. ~30-50 LOC total per sub-fix. Adopted 4× in Plan 18 (`getIndex` / `getBrainMd` / `lint` / `costReport`). **Rule:** narrow-only fixes are fast and risk-free; don't lump them with live-consumer-bug fixes (which need additional regression tests against the bug's actual symptom). Tier the sub-fixes by consumer presence before estimating cost.

**`upload.ts` UploadResult is a parallel REST drift (Plan 19 candidate; lesson on audit boundary).** T3.5's review surfaced that `apps/brain_web/src/lib/ingest/upload.ts:24-30` declares an `UploadResult` interface for the `/api/upload` HTTP endpoint that doesn't match `packages/brain_api/src/brain_api/endpoints/upload.py:91-95`'s actual `UploadResponse = {patch_id: str}` return. T2's audit was scoped to MCP-tool wrappers only — the REST-endpoint surface needs its own audit pass to catch the same drift class on a different transport. **Rule:** when auditing a typed-surface drift class, define the audit boundary explicitly. T2's "all typed wrappers in `tools.ts`" was clear; but the REST-endpoint surface was outside that boundary. Plan 19 candidate — and a general lesson: a typed-drift audit should enumerate its surface-of-interest before counting findings.

**Cost-badge UX consideration (T3.1 fallout; lesson on backend-default semantics).** Backend always emits `cost_usd` (defaulting to `0.0` for cached / zero-token rows). Post-T3.1, the Inbox row badge renders `$0.000` for these rows where pre-fix the badge was incorrectly hidden for ALL rows (because the wrong-shape adapter ignored the field). A future UX decision is whether to suppress the badge specifically when `cost === 0` (cleaner) or render `$0.000` (more accurate / honest). Plan 19 candidate. **Rule:** when a typed-surface fix exposes a previously-hidden field, the immediate UX question is "what does the default value mean to the user?" — `0.0` for a cost field is ambiguous (free? unknown? error?). The fix isn't always to hide; sometimes it's to render a more meaningful placeholder. Defer to the user.

## Plan 19 — REST-endpoint drift + typed-surface polish closure (closed 2026-05-12, tag `plan-19-rest-drift-and-typed-polish`)

**Closure summary.** All 5 tasks landed in 12 commits across 4 themes: Theme A REST-endpoint drift (T1 audit + T2 fixes), Theme B cost-badge UX (T3), Theme C 4 informational extras bundled (T4), Theme D `planToFiles` type-tighten (T5), plus T6 closure. T1 audited 3 REST endpoints in `packages/brain_api/src/brain_api/endpoints/*` (2 OK / 1 DRIFT — drift rate 33% vs Plan 18 T2's 39% on MCP-tool wrappers). T2 closed the `/api/upload` `UploadResult` drift end-to-end: TS narrowed from `{patch_id|null, applied, domain, [extra]}` → `{patch_id: string}` exactly mirroring `UploadResponse(BaseModel)`; two live consumer bugs at `drop-zone.tsx:64` + `app-shell.tsx:178` removed (both wrote `res.domain` which was always `undefined`/`null`); the dead-code envelope fallback at `upload.ts:91-95` replaced with a defensive `ApiError("upload_envelope_invalid")` tripwire; Python pin test `test_endpoint_upload_shape.py` asserts `set(UploadResponse.model_fields.keys()) == {"patch_id"}` via Pydantic model_fields introspection. T3 suppressed the Inbox cost badge when `source.cost > 0` (strict comparison defensive against `NaN`/`undefined`/`null`) at `source-row.tsx:109-115`; updated the `inbox-store.ts:160` breadcrumb from "Plan 19 candidate" to "Plan 19 T3 (D4) resolved" so future readers see the closure cross-reference. T4 bundled 4 cosmetic-severity extra-in-backend DRIFTs: widened `recent()` to `{items, limit_used}`, `proposeNote()` to `{status, patch_id, target_path}`, `listPendingPatches()` to `{count, patches}`, and `configSet()` to a named `ConfigSetData` interface `{status, key, value, persisted, note}`; 7 downstream `configSet`-routed wrappers (`setDomainOverride`, `setPrivacyRailed`, `setDomainBudget`, `setDomainRateLimit`, `setDomainAutonomy`, `setActiveDomain`, `setCrossDomainWarningAcknowledged`) explicitly retyped to `Promise<ToolResponse<ConfigSetData>>` because each had a hardcoded narrow return type that TS covariance silently accepted; 4 Python key-set pin tests added (`test_recent_outer_shape_pin.py`, `test_propose_note_data_shape_pin.py`, `test_list_pending_patches_outer_shape_pin.py`, `test_config_set_data_shape_pin.py`) — 7 test cases total covering empty/populated/branch variants. T5 tightened `planToFiles` (a module-local helper inside `step-pick-folder.tsx`, not in `tools.ts` as the plan-doc named) to `readonly BulkImportPlannedItem[]`; the `as unknown as Array<Record<string, unknown>>` cast at the call site removed; the helper body pruned from ~40 LOC to ~17 LOC by deleting 6 dead field probes (`source`, `classified`, `domain`, `size`, `duplicate`, `skip`) that mock fixtures had carried but the real backend `bulk_import.py:196-204` `planned`-branch never emits. Zero new dependencies, zero schema changes, zero spec text changes. Test counts: brain_api +2 pin tests in `test_endpoint_upload_shape.py`; brain_core +7 pin test cases across 4 files in `tests/tools/`; brain_web +3 source-row tests + 1 drop-zone test. Final demo `scripts/demo-plan-19.py` 16-gate run prints `PLAN 19 DEMO OK`.

**REST-endpoint drift class lesson (T1; NEW lesson).** Typed-wrapper-vs-runtime drift class from Plan 18 T2 (MCP-tool transport) generalizes directly to the REST transport. Plan 19 T1's audit of 3 REST endpoints found 1 DRIFT (33% rate vs Plan 18 T2's 39%) — proving the class is real but the rate varies by surface. The drift the audit caught was the highest-impact instance in the Plan 18 lessons block: 2 live consumer bugs at `drop-zone.tsx:64` + `app-shell.tsx:178` silently wrote `undefined`/`null` into every inbox row's `domain` field on every successful upload — same Plan 18 T1-class shape as `recentIngests` (silently-empty Inbox), `undoLast` (always 0), `budgetOverride` (stale prop default). **Rule:** typed-surface drift is transport-agnostic. Future plans introducing a new transport (e.g., GraphQL, gRPC, file-based) must audit its typed wrappers against the actual handler shape — same pattern as Plan 18 T2 (`tools.ts`) and Plan 19 T1 (`endpoints/*.py`). Define the audit surface-of-interest before counting findings.

**FastAPI `response_model` as drift-pin source (T2.b; NEW lesson).** Plan 18 pins were shaped `set(result.data.keys()) == {...}` against the handler's actual return — required a fixture, a `ToolContext`, and a handler invocation (~30-60 LOC per pin). FastAPI's `response_model` is itself a contract; pinning `set(UploadResponse.model_fields.keys()) == {"patch_id"}` reads `model_fields` via Pydantic introspection — no fixture, no handler call (~10-15 LOC). The Plan 19 T2 pin lives at `packages/brain_api/tests/test_endpoint_upload_shape.py` and also pins the non-null direction (`field.annotation is str` + `field.is_required()`). **Rule:** lighter pattern when the response is a Pydantic `BaseModel`. Use this pattern for REST-endpoint pins; use the heavier handler-invocation pattern for MCP tools. The two patterns are complementary, not exclusive — both fail RED on backend drift, just at different layers (model schema vs runtime return).

**T4 4-sub-fix bundle precedent (T4; NEW lesson).** Plan 18 T3.1-T3.11 split sub-fixes by live-consumer-presence — each live consumer bug got its own sub-task with a regression test. Plan 19 T4 bundled 4 cosmetic-severity DRIFTs under one task because (a) all 4 had zero live consumers (verified via grep before bundling), (b) all 4 used the same TS-widen + Python-pin pattern, (c) total LOC ~80-150 stayed within a single-task review budget. The bundle had its own RED-GREEN demo per the plan doc (one of the pin tests sanity-checked by deliberately mutating the handler emit). **Rule:** the bundle is the right cadence when no live consumers exist; split is the right cadence when live consumers need per-bug regression coverage. Tier the sub-fixes by consumer presence before estimating cost (Plan 18 lessons already noted this; Plan 19 T4 was the explicit precedent).

**`tsc --noEmit` strictness vs vitest transformer lenience (T2 follow-up `0ac02a9`; NEW lesson).** Plan 18 verification recipe used `pnpm vitest run` only. Plan 19 T2 caught an `activeTab: "all"` literal that vitest's vite-transformer accepted but `pnpm tsc --noEmit` rejected (the `InboxTab` union is `"progress" | "failed" | "recent"`). Vitest is lighter on type-strictness because it runs against transformed output, not raw types. The `0ac02a9` commit fixed the leak. **Rule:** `pnpm tsc --noEmit` should run alongside vitest in per-task verification, not just at plan-close. Per-task recipe is now: vitest + tsc --noEmit + ESLint (if applicable). Vitest passing IS NOT proof of type correctness.

**`resolve_out_dir` `parents[4]` iCloud/uv-editable degradation (T2 surprise #2; NEW lesson).** `packages/brain_api/src/brain_api/static_ui.py:96` uses `Path(__file__).parents[4]` as a dev-fallback for `<repo_root>/apps/brain_web/out`. Under uv's editable install + iCloud's `.pth` masking (cross-reference: auto-memory `feedback_uv_uf_hidden.md` + lesson 341), `parents[4]` does NOT resolve to the repo root; the SPA mount silently degrades to API-only mode. `BRAIN_WEB_OUT_DIR` env override works. T2 supplemental visual verification (commit `1b148a7`) used the env override successfully — recipe documented inline in the plan doc's `### Supplemental visual verification` section. **Rule:** for any future visual-QA invocation under uv editable install, set `BRAIN_WEB_OUT_DIR=apps/brain_web/out` explicitly. Future Plan candidate: harden `resolve_out_dir` against iCloud-masked parents (out of Plan 19 scope; Plan 13 + Plan 15 cross-platform sweeps own this seam).

**`configSet`'s 7 downstream wrappers had hardcoded types, not type-propagation (T4 surprise #1; NEW lesson).** Plan-doc framing said "single root fix; 7 wrappers inherit automatically." Reality: each wrapper had a hardcoded `Promise<ToolResponse<{key: string; value: unknown}>>` return type. TS covariance let them compile against the widened `configSet` (the narrower `{key, value}` is assignable from the wider `ConfigSetData`), but on-hover doc + IDE auto-complete at each wrapper still showed the OLD shape. The implementer explicitly updated all 7 wrappers (right call — preserves developer-experience surface). **Rule:** "automatic inheritance via type propagation" assumes wrappers use `ReturnType<typeof X>` or re-export; hardcoded explicit return types break the propagation. Audit at narrow-time whether downstream wrappers use propagation or hardcoded types; if hardcoded, update each one explicitly. Future widen-style tasks should grep the downstream-wrapper return-type style (`grep -E "Promise<ToolResponse<\{"`) before estimating.

**`planToFiles` lives in step-pick-folder.tsx, not tools.ts (T5 surprise #1; NEW lesson).** Plan-author drift instance — plan-doc named `tools.ts` for the `planToFiles` signature change but the function is a module-local helper inside `step-pick-folder.tsx:57`. `tools.ts` only owns the `BulkImportPlannedItem` interface (line 288). Matches Plan 16 lesson "Plan-author drift at scale — implementers MUST grep before assuming a file exists." The implementer adapted correctly by editing both files per the spec's intent (type-tighten at the helper definition; cast-removal + interface-import at the call site). **Rule:** when the plan-doc names a file for a change, the first implementer action is `grep -rn <symbol> apps/brain_web/` to confirm the symbol lives where the plan author thought it did. Plan-author drift at the file-resolution layer is normal across 4-5 weeks; the cure is grep, not blame.

**Body cleanup at narrow-time — defensive probes that mocks shipped but backend never emits (T5; NEW lesson).** Pre-T5 `planToFiles` body probed 6 fields (`source`, `classified`, `domain`, `size`, `duplicate`, `skip`) that the real backend `packages/brain_core/src/brain_core/tools/bulk_import.py:196-204` `planned`-branch never emits. These were defensive cruft from mock fixtures that wrote a wider shape than the real envelope. Plan 19 T5 pruned them, shrinking the helper from ~40 LOC to ~17 LOC. Per Plan 18 T2 lesson "test mocks that don't match real backend shape MASK consumer bugs" — the cleanup also affects production code shape, not just test surface. **Rule:** when narrowing a type, audit the body for field probes that the narrowed type doesn't cover; mock-driven probes often outlive the mocks. Pruning dead probes IS the lift — don't widen the interface to accommodate them.

**T2 supplemental visual verification recipe (uvicorn + curl; NEW lesson).** When real visual verification is blocked by a missing API key (Anthropic), the curl + uvicorn recipe verifies the structural contract end-to-end via the error branch + the response envelope shape. T2 supplemental verification commit `1b148a7` documents this pattern. Recipe: (1) `find .venv -name "*.pth" -exec chflags 0 {} \;` (iCloud .pth masking workaround); (2) start uvicorn with `--factory` + explicit `BRAIN_VAULT_ROOT` + `BRAIN_WEB_OUT_DIR`; (3) `curl /api/setup-status` → confirm 200; (4) `curl /api/token` → fetch token; (5) `POST` a test file to `/api/upload` with `X-Brain-Token`; (6) verify error envelope shape `{error, message, detail}`; (7) `POST /api/tools/brain_recent_ingests` → confirm row shape includes `domain` (proves polling fills the field). Useful for future plans where the implementer's environment lacks an LLM key. **Rule:** structural correctness can be proved via the error branch + envelope shape even when the success branch needs a real API key — don't block visual QA on the success branch when the contract is pinned at the schema layer (Pydantic `model_fields`).

**Handoff to Plan 20.** Plan 20 candidate scope per the tail block of `tasks/todo.md` — 4 preserved NOT-DOING carry-forwards from Plan 18 (`seedBrainMd` / `seedScope` rule-of-three; per-thread cross-domain confirmation = architectural NO per spec §3 + Plan 16 D36; topbar scope chip drift watch = lesson-only per Plan 12; PEP 703 `_cached_ctx` 3.14 trigger), plus three new candidates surfaced during Plan 19 execution: (a) `resolve_out_dir` hardening against iCloud-masked `parents[4]` (Plan 13 / Plan 15 cross-platform sweeps own this seam), (b) `configSet`-routed wrapper-shape audit pattern (if a future widen-style task hits the same hardcoded-types reality), (c) FastAPI `response_model` field-set pin pattern adoption across all REST endpoints (lightweight pin for any new endpoint with a `BaseModel` response).

## Plan 20 — Response-model pins + tools.ts wrapper audit closure (closed 2026-05-12, tag `plan-20-response-model-pins-and-wrapper-audit`)

**Closure summary.** All 4 tasks landed in 4 commits across 2 themes: Theme A `response_model` pin adoption (T1 4-BaseModel pin bundle), Theme B `tools.ts` wrapper-shape audit + tiered fix (T2 audit + T3 zero-fix closure), plus T4 closure. T1 closed the Plan 19 T1 audit-OK-but-unpinned gap for all 4 cited BaseModels (`SetupStatusResponse`, `TokenResponse`, `ToolResponse`, `ErrorResponse`) via the canonical Plan 19 T2 two-tier shape (strict-equality field-set pin + per-field `field.annotation` + `field.is_required()` pin for each single-typed field; heterogeneous `dict[str, Any] | None` fields pinned only as non-required defaulting per the responses.py module docstring rationale). 3 pin test files, 228 LOC, 14 tests, all PASSED in 0.02s. T2 audited all wrapper functions in `apps/brain_web/src/lib/api/tools.ts` against backend reality. Authoring-time single-line grep returned 21 matches; exec-time re-derivation per D4 surfaced the true count: **45 wrappers** (38 root + 7 wrapper-fanout) — the 21-count was a single-line-grep artefact because multi-line `Promise<\n  ToolResponse<...>>` return-type declarations span 2-3 lines. Verdict breakdown: 7 OK-PROPAGATES (the 7 `configSet`-routed wrappers from Plan 19 T4, all sharing the explicit `ConfigSetData` named alias) + 38 OK-NARROW-INTENTIONAL (every root wrapper, explicitly narrow per the "wrapper IS the typed surface" design) + **0 DRIFT-HARDCODED**. Plan 19 T4's `7cd2b72` commit is fully holding; the broader 38-root surface is drift-free by design. T3 collapsed to **zero-fix closure** per the user's tiered AskUserQuestion at exec time (Q3=3.A) — adding regression pins for a non-existent drift class would be over-engineering per CLAUDE.md "engineered enough" principle. Regression check: vitest 496 passed / 1 skipped; tsc --noEmit clean. Zero source modifications. Zero new dependencies, zero schema changes, zero spec text changes. Final demo `scripts/demo-plan-20.py` 7-gate run prints `PLAN 20 DEMO OK`.

**FastAPI `response_model` pin pattern scaled cleanly to 4 BaseModels (T1; NEW lesson).** Plan 19 T2's two-tier shape (field-set strict equality + per-field type+required pins via Pydantic introspection) replicated cleanly across 4 BaseModels for ~228 LOC across 3 test files / 14 test functions. Each pin ~10-15 LOC. No fixture, no handler invocation. Heterogeneous envelope fields (`ToolResponse.data` / `ErrorResponse.detail`, both `dict[str, Any] | None`) correctly skipped the type-annotation pin and only pinned `not field.is_required()` — preserves the intentional per-tool / per-error variation documented in `responses.py`'s module docstring (cross-reference: `RateLimitDetail` + `ValidationDetail` documentation-only models). **Rule:** future REST endpoints introducing a new Pydantic `response_model` get a Plan 19 T2 two-tier pin test by default — single-typed fields get `field.annotation is X` + `field.is_required()` pins; heterogeneous `dict[str, Any] | None` fields skip type-annotation pin and pin only nullability.

**Plan-author drift caught at AUTHORING time, not execution time (D12; NEW lesson).** Plan 20 authoring-time recommendation said "routes/* has BaseModel returns — that's the natural #6 surface." Pre-execution grep revealed the OPPOSITE: routes/* had no `response_model` surface (`chat.py`=WebSocket; `health.py`=`dict[str, str]` literal; `tools.py`=generic `ToolResponse` envelope already covered by Plan 18 T2's per-tool data-shape audit). The natural surface was `endpoints/*` unpinned BaseModels + the shared envelopes. Plan 20 caught it via `AskUserQuestion` BEFORE T1 dispatch (corrected to 6B). Plan 16 / Plan 19 T5 lesson "implementers MUST grep before assuming file/symbol locations" fires at BOTH layers: implementers grep before assuming, AND plan authors should grep before recommending. Plan 20 paid the audit cost at authoring (5 minutes); the alternative would have been T1 implementer surfacing the drift after dispatch (30+ minutes of wasted context + re-dispatch). **Rule:** when authoring a plan-doc that names files/symbols/shapes, run the grep BEFORE writing the recommendation. Prefer "I verified `<symbol>` exists at `<file:line>`" over "I think `<symbol>` is in `<file>`." The plan-author-drift watch should fire AT authoring, not just at execution.

**Single-line grep artefacts undercount wrapper surface (T2; NEW lesson).** Plan 20 authoring-time grep `grep -nE "Promise<ToolResponse<" tools.ts` returned 21 matches. Exec-time audit found **45 wrappers** total (38 root + 7 fanout). Discrepancy: multi-line return-type declarations (e.g., `: Promise<\n  ToolResponse<{...}>\n>`) span 2-3 lines; single-line grep catches only the line containing `Promise<ToolResponse<`. The audit subject was actually 45 wrappers, not 21. Per D4 the implementer documented this inline in `## T2 audit findings` header and proceeded against the true count. **Rule:** when grepping for a TypeScript pattern that could span multiple lines (return-type annotations, JSX prop lists, parameter destructuring), use a multi-line-aware tool: `grep -Pzo '(?s)pattern'` OR `rg --multiline` OR open the file and visually scan. Single-line grep counts are a LOWER BOUND, not an exact count.

**Zero-DRIFT audit IS the success receipt (T2 + T3; NEW lesson).** Plan 20 T2 audited 45 wrappers expecting some DRIFT (Plan 19 T4 found 7 in a subset; the broader 38-root surface was unexplored). Result: 0 DRIFT-HARDCODED rows. Plan 19 T4's `7cd2b72` fix was comprehensive AND the broader surface is drift-free by design (every root wrapper is OK-NARROW-INTENTIONAL — the wrapper IS the typed surface). T3 collapsed to zero-fix closure with regression check + outcome doc + pointer to T2 findings. **Rule:** an audit returning zero findings is a legitimate outcome — the audit's job is to verify the drift class is closed, and "closed" is the goal. Don't manufacture work to justify the audit (e.g., adding regression-pin tests for a drift class that doesn't currently exist = over-engineering per CLAUDE.md "engineered enough" principle). The audit findings table IS the receipt.

**`tools.ts` wrapper convention — explicit shared named alias (T2; NEW lesson).** The codebase's established pattern for wrapper-fanout helpers is explicit shared named interface alias (e.g., `ConfigSetData` at `tools.ts:628-634`, referenced by `configSet` root at l. 639 + each of the 7 fanout wrappers at their respective return types). Future wrapper-fanout helpers SHOULD adopt this same shape: declare a named interface for the data payload, reference it via `Promise<ToolResponse<Foo>>` at both root and wrappers. Plan 20 T2 confirmed this pattern is the de-facto convention across the 7 fanout wrappers + applies as the default for any future fanout. **Rule:** when adding a new wrapper-fanout helper to `tools.ts`, follow the `ConfigSetData` pattern — named interface + explicit shared alias at root + wrapper return types. Don't use `ReturnType<typeof <root>>` (less readable on hover); don't hardcode inline `{...}` shapes (Plan 19 T4 drift class).

**Handoff to Plan 21.** Plan 21 candidate scope per the tail block of `tasks/todo.md` — 4 preserved NOT-DOING carry-forwards (unchanged since Plan 17 / 18 / 19: `seedBrainMd` / `seedScope` rule-of-three; per-thread cross-domain confirmation = architectural NO per spec §3 + Plan 16 D36; topbar scope chip drift watch = lesson-only per Plan 12; PEP 703 `_cached_ctx` 3.14 trigger), plus 1 deferred from Plan 20 per user 1.A scope-locking decision ("Small Plan 20 = #6 + #7", NOT #5): `resolve_out_dir` `parents[4]` hardening against iCloud-masked workspaces (Plan 19 T2 surprise #2; `BRAIN_WEB_OUT_DIR` env override is the durable workaround; cross-platform sweep territory per Plan 13 + Plan 15). The new Plan-20-surfaced observations (multi-line-grep artefact / plan-author-grep / wrapper-convention) are LESSON-level captures (not Plan 21 candidates unless surfaced via real-world need).

## Plan 21 — `resolve_out_dir` + silent-degrade hardening closure (closed 2026-05-12, tag `plan-21-resolve-out-dir-hardening`)

**Closure summary.** All 3 substantive tasks landed across 2 themes + closure. **Theme A `resolve_out_dir` walk-up hardening (T1).** Replaced the depth-based `Path(__file__).resolve().parents[4]` dev-fallback in `packages/brain_api/src/brain_api/static_ui.py:96` (Plan 19 T2 surprise #2) with a content-based `_find_repo_root(start, *, marker=".git")` walk-up at `static_ui.py:70-89`. The walk-up is pure pathlib (no `os.path`, no `shell=True`), defaults to `.git` marker per D4 adjudication, returns `None` if `/` reached without a hit, and accepts both directory and file starting paths. `resolve_out_dir`'s dev-fallback block (`static_ui.py:115-129`) calls walk-up as primary AND retains `here.parents[4]` as secondary inside `try: ... except IndexError: pass` (preserves tarball-extracted source-dir behavior when `.git` is absent). Env-override (`BRAIN_WEB_OUT_DIR`) + install-layout (`BRAIN_INSTALL_DIR/web/out`) candidates unchanged. 5 unit tests in `packages/brain_api/tests/test_static_ui_find_repo_root.py` covering marker-at-start, intermediate first-hit-wins, missing-marker-returns-None, starting-path-as-directory, starting-path-as-file — all PASSED in 0.01s. Tests use unique sentinel names so the test runner's own `.git` ancestor chain cannot contaminate assertions. In-process visual-QA receipt confirms `_find_repo_root(.venv/lib/python3.12/site-packages/brain_api/static_ui.py)` AND `_find_repo_root(packages/brain_api/src/brain_api/static_ui.py)` both resolve to the iCloud repo root — the env-override workaround documented in the auto-memory is now unnecessary in dev. **Theme B silent-degrade observability (T2).** Replaced bare `try: ... except RuntimeError: pass` (pre-T2 lines 284-286 in `packages/brain_api/src/brain_api/app.py`) with a structured `_lifespan_logger.warning("spa_mount_skipped", error=str(exc), hint=...)` at `app.py:284-302`. The hint string points at both documented workarounds (`BRAIN_WEB_OUT_DIR=apps/brain_web/out` env-override AND `pnpm --dir apps/brain_web build`). Mirrors brain_api's existing structlog convention (verified via `grep -rn "structlog\|get_logger" packages/brain_api/src/brain_api/`): `app.py:40` binds `_lifespan_logger = structlog.get_logger(__name__)` and two prior warnings in the same module use the same `("event_name", error=str(exc), key=value)` shape. 3 unit tests in `packages/brain_api/tests/test_app_silent_degrade_warning.py`: warning fires when `mount_static_ui=True` AND `resolve_out_dir` raises; warning does NOT fire when `mount_static_ui=False` (CI/contract surface); warning does NOT fire on successful mount. Tests patch BOTH `brain_api.app.resolve_out_dir` (today's import-bound reference) AND `brain_api.static_ui.resolve_out_dir` (`raising=False` belt-and-suspenders for a future refactor that inlines the import — Plan 17 T17 monkeypatch-binding-vs-import lesson applied). All 3 PASSED in 0.02s. brain_api full-suite: 211 passed / 4 skipped (up from T1's 208/4; +3 delta = exactly the new silent-degrade tests). **Closure (T3).** `scripts/demo-plan-21.py` 6-gate demo prints `PLAN 21 DEMO OK`. Zero new dependencies, zero schema changes, zero spec text changes. Auto-memory `feedback_uv_uf_hidden.md` gained a 2026-05-12 update section documenting two NEW failure modes surfaced during Plan 21 dev iteration (install-as-copy stale site-packages + .venv duplication beyond `.pth` masking).

**Content-based walk-up beats depth-based index (T1; NEW lesson).** Plan 21 T1 replaced `Path(__file__).resolve().parents[4]` (a brittle depth assumption) with `_find_repo_root(start, marker=".git")` (content-based ancestor lookup). The walk-up is robust regardless of how `Path(__file__).resolve()` arrived at its current location — uv editable install, symlinks, iCloud `.pth` masking, future tooling quirks. Walk-up returns `None` when the marker is absent, allowing the secondary `parents[4]` fallback to handle tarball-extracted source dirs without a `.git` directory. **Rule:** when code needs a workspace root or sibling directory derived from `__file__`, prefer content markers (`.git`, `pyproject.toml`, dedicated sentinel files) over `parents[N]` indices. The walk-up pattern is cross-platform (pathlib-only), small (~20 LOC), and adapts to install-mode quirks without re-derivation. The depth-based form is an anti-pattern at workspace-root resolution time even though it ships free with `pathlib` — the implicit assumption ("brain_api source lives EXACTLY 4 levels below the repo root") only holds in the dev source tree and silently breaks under install-mode changes.

**D4 marker adjudication: sub-pyprojects break `pyproject.toml` first-hit semantics (T1; NEW lesson).** Plan-doc cited `pyproject.toml` as a candidate marker. T1 implementer verified at exec time: 5 `pyproject.toml` files exist in this repo (root + 4 sub-packages: `brain_api` / `brain_cli` / `brain_core` / `brain_mcp`), so a `pyproject.toml`-based walk-up would short-circuit at the first sub-package level (`parents[3]`), NOT reach the workspace root (`parents[4]`). `.git` is unambiguous — `find . -name .git` returned exactly 1 hit at the repo root, with no git-submodule edge cases on this repo. **Rule:** when picking a workspace root marker for `_find_repo_root`-style helpers, verify the marker is unambiguous in the target repo at exec time. `.git` is the safest default for git-managed repos; `pyproject.toml` is fine for single-package repos but breaks under uv workspaces or any monorepo with sub-package pyprojects. Document the choice + edge cases in the helper's docstring so a future maintainer doesn't swap the marker without re-deriving.

**Silent-degrade-by-design needs observability (T2; NEW lesson).** Plan 21 T2 replaced `try: ... except RuntimeError: pass` (silent-degrade for headless mode) with a structured `_lifespan_logger.warning("spa_mount_skipped", error=..., hint=...)`. The pattern is correct in intent (skip the SPA mount when `out_dir` resolution fails — required for the API-only / CI / contract test surface) but its silence hides UNINTENTIONAL failures (e.g., uv editable install + iCloud `.pth` masking → developer sees a silent `GET /` 404 with no diagnostic). The branch only fires when the caller asks for the SPA mount (`mount_static_ui=True`) AND `resolve_out_dir` raised — CI/contract tests use `mount_static_ui=False` so the warning is silent there. The hint string includes BOTH documented workarounds so the developer fixes their environment, not the code. **Rule:** when a `try: ... except: pass` pattern is intentionally silent in one mode (CI/headless) and would mask an unintentional failure in another (dev/visual-QA), differentiate the two via a control flag (e.g., `mount_static_ui` boolean) and emit a structured warning in the unintentional-failure branch. Silent-degrade-by-design is fine; silent-degrade-by-accident is a bug-finding tax that compounds over time.

**Grep-before-assuming generalizes to runtime code-content assumptions (T1; NEW lesson).** The "grep before assuming a symbol exists" lesson (Plan 16) → "grep before assuming a file/symbol location" (Plan 19 T5 / Plan 20 D12) → now "code MUST verify content/markers, not assume depth" (Plan 21 T1). Same principle, applied at three layers: plan authoring, implementation, AND runtime code. The Plan 19 T2 author's `parents[4]` assumption was correct at authoring time AND correct in the dev source tree AND correct under editable install on a non-iCloud filesystem — but the assumption was implicit and silently broke when one of those three preconditions changed. A `.git` walk-up makes the assumption explicit in the code (the repo root is wherever `.git` lives) and verifiable at runtime. **Rule:** the grep-before-assuming discipline applies wherever code or docs make a positional assumption about another part of the system. Content-based assertions (string match, file exists, key set equality) beat positional assertions (line N, depth N, index N).

**iCloud + uv install-as-copy is a NEW failure-mode class beyond `.pth` masking (T1+T2; NEW lesson + auto-memory update).** `feedback_uv_uf_hidden.md` describes the `.pth`-hidden-flag class where editable installs work but `.pth` files get `UF_HIDDEN`'d. Plan 21 T1 + T2 implementers hit TWO new shapes: **(A) install-as-copy stale site-packages.** Workspace pyproject pins `brain_api = { workspace = true, editable = false }` (and likely `brain_mcp` / `brain_cli` too), so the package is installed as a non-editable COPY in `.venv/lib/python3.12/site-packages/brain_api/`. Source edits in `packages/brain_api/src/brain_api/` do NOT propagate to site-packages — `inspect.getsource(brain_api.app)` returned the stale pre-T2 `except RuntimeError: pass` while the on-disk source had the new `_lifespan_logger.warning(...)`. Diagnostic signal: the `__file__` attribute points at `.venv/.../site-packages/`, not `packages/<pkg>/src/`. Recipe: PYTHONPATH bypass for one-shot test runs (`unset VIRTUAL_ENV && PYTHONPATH=packages/brain_core/src:packages/brain_api/src:packages/brain_mcp/src:packages/brain_cli/src uv run --package brain_api pytest`) OR `uv pip install --reinstall-package brain_api -e packages/brain_api` for persistent dev-loop. **(B) .venv file duplication (extreme iCloud corruption).** T1 implementer encountered every file in `.venv/lib/python3.12/site-packages/<pkg>/` duplicated with ` 2.py` / ` 3.py` suffixes AND the canonical files (`__init__.py`, `static_ui.py`) missing — imports failed with `ModuleNotFoundError` because the actual module files were gone, not because of `.pth` masking. Recovery: `rm -rf .venv && uv sync --all-packages && uv pip install --reinstall-package brain_api -e packages/brain_api`. Auto-memory `feedback_uv_uf_hidden.md` updated at Plan 21 T3 close with both shapes. **Rule:** when a test run produces ImportError pointing at site-packages (not the editable source path), the package may be install-as-copy rather than editable. When the directory contains ` 2.py`-suffixed files with no canonical counterpart, iCloud has corrupted the .venv beyond chflags recovery — full rebuild is the only path. Plan 22 candidate: flip workspace pyproject pins to `editable = true` for the 3 wrapper packages (brain_api / brain_mcp / brain_cli) to close the dev-loop friction class — trade-off is the `.pth` UF_HIDDEN class returns to scope; mixed-mode editable workspace is the likely path forward.

**Handoff to Plan 22.** Plan 22 candidate scope per the tail block of `tasks/todo.md` — 4 preserved NOT-DOING carry-forwards (unchanged since Plan 17 / 18 / 19 / 20: `seedBrainMd` / `seedScope` rule-of-three; per-thread cross-domain confirmation = architectural NO per spec §3 + Plan 16 D36; topbar scope chip drift watch = lesson-only per Plan 12; PEP 703 `_cached_ctx` 3.14 trigger), plus 1 NEW candidate surfaced during Plan 21 dev iteration: workspace `editable = true` flip for `brain_api` / `brain_mcp` / `brain_cli` (excluding `brain_core` which already works). Trade-off: editable mode brings the `.pth` UF_HIDDEN masking class back into scope (auto-memory `feedback_uv_uf_hidden.md` 2026-05-12 update section); the path forward is likely mixed-mode (editable wrappers + non-editable brain_core, OR a Makefile target that wraps the chflags + reinstall ceremony so contributors don't re-discover it).

## Plan 22 — Watched-folders sync (closed 2026-05-12, tag `plan-22-watched-folders-sync`)

**Closure summary.** Plan 22 is the first "real new subsystem" plan since Plan 09 (v0.1.0). The 17 substantive tasks + 3 mini-fixes (T10.5 lookup-gap, T12 fix-up resync wiring, T14.5 dry_run mode) landed across 4 themes in 39 commits. **Theme A — Foundation (T0-T1).** Spec §4 gained 4 new optional frontmatter fields (`source_path` / `orphaned` / `orphaned_at` / `watched_folder_id`); spec §5 gained a "Watched folders" subsection documenting the lifecycle + D1 overwrite contract + 7-tool surface; spec §10 gained a safety-rails bullet pinning the `pre_watched_folder_sync` backup trigger + `include_orphans` default-filter. `WatchedFolder` Pydantic model + `Config.watched_folders: list[WatchedFolder]` field locked the schema; 4 frontmatter fields default to optional/None so existing notes load unchanged. **Theme B — Backend (T2-T10).** `IngestPipeline.update_source` (re-ingest preserving slug + frontmatter `domain`) + `IngestPipeline.mark_orphaned` (non-destructive `orphaned: true` + `orphaned_at: <today>` via VaultWriter with undo log) implemented per D1/D2. `scope_guard(path, *, allowed_domains, include_orphans=False)` extended with the keyword-only flag — default `False` excludes orphans from chat/recent/search contexts. 7 watched-folder tools registered (`brain_watch_folder` / `brain_unwatch_folder` / `brain_list_watched_folders` / `brain_list_orphans` / `brain_resync_folder` / `brain_restore_orphan` / `brain_delete_orphan`). `WatchedFolderWatcher` core in `brain_core.watch.folder_watcher` mirrors `ConfigWatcher` shape (start / stop / debounce / event handler); brain_api lifespan + brain_mcp `_cached_ctx` both register watchers per D7 symmetric pattern with parallel `_build_watched_folder_watcher` + `_restart_watched_folder_watcher` helpers (cross-package duplication acknowledged as Plan 23 candidate). `pre_watched_folder_sync` joined `_VALID_TRIGGERS` in `backup_create.py`. **T10.5 (mini-fix; CORRECTNESS GATE).** T10 integration tests passed via pre-seeded fixtures that BYPASSED the live-sync watcher path's lookup mechanism; T10.5 added a "create-then-modify" lifecycle test that failed pre-fix (2 duplicate notes) and passed post-fix (1 note with refreshed content_hash), then wired `source_path` + `watched_folder_id` kwargs through `IngestPipeline.ingest()` end-to-end. **Theme C — Frontend (T11-T15).** brain-ui-designer delivered 6 ASCII-wireframe mockups + README at `docs/design/plan-22/`; mockups specified verbatim microcopy + shadcn primitives + accessibility annotations. `panel-watched-folders.tsx` (Settings → Watched folders), `panel-orphans.tsx` (Settings → Orphans), `watched-folders-topbar-indicator.tsx` (status chip with state table — self-hides on empty vault, prefers high-attention orphan-route on click), 3 confirmation modals (`watch-enable-modal.tsx` / `watch-disable-modal.tsx` / `orphan-delete-modal.tsx`), and Bulk Import → "Watch this folder" CTA in `step-apply.tsx`. T12 fix-up wired the Resync button (T12 implementer falsely claimed `brain_resync_folder` didn't ship in T5; verification proved 7 tools were registered). T14.5 added `dry_run` mode to `brain_watch_folder` for the watch-enable modal cost-preview flow (7 pin tests). **Theme D — Closure (T16-T17).** Playwright e2e `watched-folders.spec.ts` covers enable / disable / orphan / bulk flows with axe gates. `scripts/demo-plan-22.py` 21-gate demo prints `PLAN 22 DEMO OK`. `packages/brain_api/tests/test_tools_listing.py` constant cleared from 38 (Plan 16 baseline) to 45 (Plan 22 +7 tools) + spot-check assertions for all 7 new tool names + test function renamed `test_lists_thirty_six_tools_after_issue_17` → `test_lists_all_registered_tools` with a closing note that the constant updates per plan. Zero spec rollback risk; zero schema breaks; zero new external dependencies (watchdog was already in scope from Plan 16 T35 ConfigWatcher).

**First "real feature" plan since v0.1.0 (CLOSURE-level lesson).** Plans 10-21 were polish / feature-completion / audit / fix. Plan 22 introduced a NEW SUBSYSTEM (watched folders) with its own spec section, schema delta, pipeline paths, tool surface, watcher infrastructure, frontend screens, and e2e coverage. The CLAUDE.md non-negotiable "spec update first" rule fired on T0 — implementation could not start until §4 / §5 / §10 had the contract locked. The plan grew to 17 substantive + 3 mini-fixes = 20 total, second-largest since Plan 16's 47. **Rule:** when a plan introduces a new subsystem (not a feature, not a polish), the spec update is T0 and cannot be deferred. The downstream tasks each have an unambiguous contract to honor; correctness gates (T10.5, T14.5) become possible because the spec defines what "correct" means. The pause cadence (D14: every ~5 tasks) is the safety rail for an integration-heavy plan with cross-package surface.

**T10.5 lookup-gap discovery proves the pause-cadence value (T10.5; NEW lesson).** T10's integration tests passed via fixtures that pre-seeded VaultWriter with `source_path` + `watched_folder_id` in frontmatter, BYPASSING the live-sync watcher path's lookup mechanism. The watcher's "find the note by source_path, fall back to creating one" logic was never exercised by the pre-seeded tests — both branches returned the same note slug. T10.5 added a "create-then-modify" lifecycle test that drove the watcher path end-to-end: watcher fires `create`, pipeline ingests + writes frontmatter with `source_path` + `watched_folder_id`; watcher fires `modify`, pipeline looks up by `source_path`, finds the existing note, calls `update_source` (preserves slug, refreshes content_hash + body). The test failed pre-fix with 2 duplicate notes (lookup found nothing → ingest path created a second note); passed post-fix with 1 note + refreshed content. Without the T10 pause + controller review, the broken watcher would have shipped through Theme C without being detected at the unit / per-task level. **Rule:** integration tests that pre-seed state can mask end-to-end correctness gaps. The "create-then-modify" lifecycle shape is the canonical verification for any sync / lookup mechanism — pre-seeded fixtures verify "given X, do Y" but lifecycle tests verify "the state transition produces X correctly." Both shapes are needed; pre-seeded alone is insufficient.

**T12 fix-up false-negative — controller verifies before accepting implementer claims (T12; NEW lesson).** T12 implementer claimed `brain_resync_folder` didn't exist in the T5 deliverable and shipped a disabled "Resync now" button with a "Coming soon" tooltip as a workaround. Controller verification via `list_tools()` returned all 7 watched-folder tools registered correctly, including `brain_resync_folder`. T12 fix-up commit wired the button to the actual tool and removed the tooltip. The root cause was the implementer's grep query missed the tool because it ran against a stale `.venv/.../site-packages/` copy of `brain_core/tools/__init__.py` (the install-as-copy class documented in `feedback_uv_uf_hidden.md` 2026-05-12 update section). **Rule:** when an implementer flags a dependency as missing, controller verifies before accepting the workaround. Ask for the exact grep / check command the implementer used; reproduce; confirm. The cost of one verification beat the cost of shipping disabled UX + a follow-up fix-up commit. This generalizes the Plan 19 T5 grep-before-assuming discipline to "implementer claim + controller verification" — a two-step check that catches stale environment + plan-author drift in a single audit.

**brain-ui-designer ASCII wireframes worked for v1 (T11; NEW lesson).** Plan 06 used an external design tool (Claude design tool with Figma-quality output). Plan 22 T11 used the brain-ui-designer subagent with ASCII wireframes in markdown — 6 mockup files (`watched-folders-settings.md` / `orphan-management.md` / `topbar-status.md` / 3 modals + README). Mockups specified: verbatim microcopy (every button label, error string, empty-state copy, glossary tooltip text), shadcn primitives (`<Dialog>` vs `<AlertDialog>` vs `<Card>` with explicit usage rationale), accessibility annotations (focus order, keyboard shortcuts, axe-checkable color tokens, aria-labelledby contracts, screen-reader announcement copy), state tables (topbar indicator's 4 states × 2 routes × 3 tooltip variants). T12-T15 implementers consumed mockups directly; pin-by-pin microcopy assertions in unit tests caught regression risk at PR time. Manual chrome verification at T15 pause confirmed verbatim microcopy on the watch-enable modal matched the mockup. **Rule:** ASCII wireframes in markdown are acceptable for v1 UI design when verbatim microcopy + accessibility annotations + primitive specs are present. Pixel-perfect Figma is deferred until a design problem actually requires it (visual hierarchy / dense layout / new component library). The win is the design-implementer feedback loop tightens — mockups are reviewable and editable in the same pipeline as code, no external-tool gating.

**Symmetric watcher pattern from Plan 16 T35 scaled cleanly (T7+T8; NEW lesson).** Plan 16 T35 established the ConfigWatcher symmetric pattern: brain_api lifespan + brain_mcp `_cached_ctx` both register the watcher; both expose parallel `_build_*` + `_restart_*` helpers; both apply Plan 17 T17's monkeypatch-vs-import-binding lesson in tests. Plan 22 T7 + T8 mirrored that shape for `WatchedFolderWatcher` — same threading model, same hot-reload bridge from `ConfigWatcher` to `WatchedFolderWatcher` restart, same belt-and-suspenders test patching. The implementation parallel made T7 + T8 a 1-day total scope despite touching both wrapper packages. The duplication concern (`_watched_folders_changed` helper exists in both packages; `_build_watched_folder_watcher` + `_restart_watched_folder_watcher` mirror across the two packages) is acknowledged as Plan 23 candidate — also the brain_api + brain_mcp parallel-watchers fire question (VaultWriter filelock prevents corruption; coordination needed for clean undo-log). **Rule:** when a cross-process pattern works once, reuse it. The duplication is acceptable in the short term; the lift-and-share refactor is the right Plan 23 candidate once the pattern stabilizes across watchers.

**Plan-author drift continues (T15; recurring lesson).** Plan 22 plan-doc named `step-apply-complete.tsx` for the Bulk Import → "Watch this folder" CTA target file; reality at exec time was `step-apply.tsx`. T15 implementer applied Plan 19 T5's grep-before-assuming discipline + adapted the file path. Plan 16 / 19 / 20 / 21 each surfaced one or two plan-author drift instances; the discipline is now reflexive. **Rule:** plan-doc file paths are advisory. Implementer runs grep / `find` to verify the target file exists with the named symbol before editing. When the file/symbol is missing, surface the drift in the outcome receipt + Plan 23 candidate scope so the next plan-author doesn't repeat the error.

**iCloud install-as-copy + .venv duplication ongoing dev-loop friction (carry-over; auto-memory).** Plan 22 implementers across T7 / T8 / T12 hit the install-as-copy class documented in `feedback_uv_uf_hidden.md` 2026-05-12 update section: source edits in `packages/brain_api/src/` or `packages/brain_mcp/src/` don't propagate to `.venv/lib/python3.12/site-packages/` without explicit `uv pip install --reinstall-package <pkg> -e packages/<pkg>` reinstall. Recoveries used in Plan 22: PYTHONPATH bypass (one-shot recipe per auto-memory) OR `rm -rf .venv && uv sync --frozen` (full rebuild when iCloud duplication kicks in). The root cause is `brain_api` / `brain_mcp` / `brain_cli` pinning `editable = false` in workspace pyproject. **Rule:** dev-loop friction surfaces consistently across the 3 wrapper packages; the editable-flip candidate forwarded to Plan 23 is the structural fix. Until then, contributors recover via the documented recipes — none of the Plan 22 implementers re-discovered the failure mode from scratch (the auto-memory + recipes worked).

**Manual chrome verification at T15 pause caught microcopy / UX gaps early (T15; NEW lesson).** Controller (main loop) opened chrome via `mcp__claude-in-chrome__*` tools at the T15 pause, navigated to Settings → Watched folders, clicked through the watch-enable modal, verified the D1 overwrite-contract microcopy verbatim against the T11 mockup, and tested keyboard navigation + focus order. The verification caught T15 implementer concern #2 (domain dropdown defaulted to `domains[0] = "personal"` not `activeDomain`) and surfaced 2 additional one-line UX items (AbortController on confirm in-flight; native OS folder picker limitations) as Plan 23 candidates. T16 e2e tests are valuable but lag the controller's smoke test by hours — by the time e2e RED would appear, T15 had already been committed + tagged. **Rule:** at major frontend pause cadences (T12 + T15 + plan-close per D14), controller does a brief chrome verification before dispatching the next task. The cost is ~10 minutes; the win is catching microcopy / UX correctness in the same edit cycle as the implementation. Smoke tests > e2e tests for closing-the-loop on UX correctness; both are needed.

**Handoff to Plan 23.** Plan 23 candidate scope is substantial — the largest carry-forward block since Plan 16. Organized in 4 sections per `tasks/todo.md` tail:

- **Critical (from execution):** `list_watched_folders._walk_watched_folder_counts` catches `FrontmatterError` but NOT `pydantic_core.ValidationError` (T16 surfaced a real backend bug — partial frontmatter triggers ValidationError from `Note.model_validate(...)` which is not caught; the orphan walk silently aborts).
- **UX polish (from Theme C concerns; ~9 items):** native OS folder picker (webkitdirectory limitations + permission UX); domain dropdown defaults to `activeDomain` not `domains[0]` (one-line); AbortController on confirm in-flight; cross-store reconcile (`watched_folders.orphan_count` stays stale after restore/delete — needs orphans-store subscription); orphans-only topbar state coverage gap; "Open in Finder" backend helper (genuinely doesn't exist); bulk action progress feedback (running bulk-bar text); `OrphanEntry` lacks `last_synced_at` + `title`; topbar indicator 200ms entrance transition (currently instantaneous).
- **Architectural (from Theme B carry-forwards):** `_build_pipeline` 4-site duplication; `_watched_folders_changed` cross-package duplication; brain_api + brain_mcp parallel watchers → duplicate fires (filelock prevents corruption; coordination needed for clean undo-log); Stage-4 archive deferral (handler API extract-without-archive split).
- **Dev-loop friction:** workspace `editable = true` flip for brain_api / brain_mcp / brain_cli (carry-over from Plan 21 closure; resolves install-as-copy class but reintroduces `.pth` UF_HIDDEN masking — mixed-mode or Makefile-wrapped ceremony likely the path forward).
- **Preserved 4 NOT-DOING (unchanged since Plan 17):** seedBrainMd / seedScope rule-of-three (3/5 callers); per-thread cross-domain confirmation (architectural NO per Plan 16 D36 spec footnote); topbar scope chip drift watch (lesson-only); PEP 703 free-threaded Python `_cached_ctx` (3.14 trigger).

These are NOT a Plan 23 commitment — Plan 23 will be authored just-in-time once the user reviews this carry-forward block and locks scope. The 4 sections are listed in approximate priority order per CLAUDE.md "thoughtfulness > speed" — backend correctness first, UX polish second, architectural cleanup third, dev-loop friction fourth, preserved NOT-DOINGs last.

## Plan 23 — Watched-folders hot-fix + UX one-liners (closed 2026-05-12, tag `plan-23-watched-folders-hotfix-ux`)

**Closure summary.** Plan 23 is the smallest plan since Plan 21 (3 tasks). Plan 22 closed 2026-05-12 with 19 carry-forward candidates; honest re-assessment surfaced exactly 1 truly critical (the `list_watched_folders` `ValidationError` catch gap surfaced at T16) + 2 cheap UX one-liner wins flagged at T15 implementer + controller-verification pauses. Bundled into a 3-task plan instead of folding all 19 carry-forwards into a 20-task megaplan. **Theme A — Critical hot-fix (T1).** `packages/brain_core/src/brain_core/tools/list_watched_folders.py` `_walk_watched_folder_counts` helper extended to catch `pydantic.ValidationError` alongside the existing `FrontmatterError`. Pre-T1 a single malformed-frontmatter watched-folder note in the vault crashed the Settings → Watched folders tab + Topbar indicator (Plan 22 T16 surfaced this via Playwright `purgeWatchedFolderPollution` cleanup helper — the defensive workaround was evidence the failure mode was real). T1 outcome split the existing single `except (OSError, UnicodeDecodeError, FrontmatterError):` block into two: silent skip for transient I/O (`OSError` / `UnicodeDecodeError`) + warn + skip for content corruption (`FrontmatterError` / `ValidationError`). +13 LOC source, -1 LOC, +72 LOC test. Regression-pin test `test_validation_error_note_skipped_without_crash` at `packages/brain_core/tests/tools/test_list_watched_folders.py:153` constructs a vault with valid + malformed frontmatter notes, asserts no crash + bad note skipped + exactly one structlog warning captured (with `path` + `error` fields populated + `log_level == "warning"`). RED verified by stashing the source change; test fails RED with uncaught `pydantic_core._pydantic_core.ValidationError`. brain_core full-suite: 1156 → 1157 passed (+1), 5 skipped (unchanged). **Theme B — Bundled UX one-liners (T2).** T2.a — `apps/brain_web/src/components/dialogs/watch-enable-modal.tsx` domain dropdown initial value changed from `domains[0]?.slug ?? "research"` to `activeDomain || domains[0]?.slug ?? "research"`. Pulled `activeDomain` from existing `useDomains()` hook (Plan 11 T6 / Plan 12 T5 selector). Applied the fix to BOTH the `useState` lazy initializer AND the post-resolve hydration `useEffect` so domains-hydrating-after-modal-open paths land on activeDomain too. T2.b — `apps/brain_web/src/components/shell/watched-folders-topbar-indicator.tsx` added a `loaded` selector + a `React.useEffect` block that fires `void refresh()` when `!loaded`. Matches the `useDomains()` first-mount auto-refresh pattern at `use-domains.ts:117-121`. Chose `!loaded` gate over the plan-doc's literal `folders.length === 0 && !in-flight` phrasing: `!loaded` correctly handles legitimately-empty vaults (loaded=true, folders=[] → no re-fetch loop); the literal would re-fetch on every mount for a user with zero watched folders. T2 tests: +3 unit tests at `apps/brain_web/tests/unit/watch-modals.test.tsx` (activeDomain default + defensive fallback + prefilledDomain-overrides) + +3 unit tests at `apps/brain_web/tests/unit/topbar-watched-status.test.tsx` (refresh fires when `!loaded` + doesn't re-fire when `loaded` + doesn't re-fire when `loaded` with empty folders). Two pre-existing tests in topbar-watched-status.test.tsx repaired in place (preconditions changed by the new mount-fetch). RED verified by stashing both source files; both new-behavior tests failed RED. **Closure (T3).** `scripts/demo-plan-23.py` 7-gate demo prints `PLAN 23 DEMO OK`. Zero spec text changes, zero schema changes, zero new dependencies.

**Honest re-assessment of "critical" works (CLOSURE-level lesson; NEW).** Plan 22 closed with 19 Plan 23 candidates. Plan 23 brainstorm narrowed to 1 truly critical (ValidationError catch — a real user-facing crash) + 2 cheap UX wins (activeDomain default — privacy footgun; topbar mount-fetch — stale UI on non-Settings landing). Resisting the temptation to bundle all 19 kept Plan 23 shippable in 3 tasks. The remaining 16 carry forwards into Plan 24 candidate scope at Plan 22-end priority levels — nothing got lost; everything just-in-time. **Rule:** at plan close, carry-forward lists need a "what's actually critical?" re-assessment at the next plan's brainstorm. Polish + nice-to-have items are NOT critical; user-facing crash risks ARE. Use the next plan to ship the smallest-thing-that-matters; defer the rest. The win is two-fold: Plan 23 closes fast + Plan 24 candidate scope stays honest (16 items, all still genuinely candidate at Plan 22 close priority).

**Pydantic v2 strict validation propagates to user-facing tools (T1; NEW lesson).** T1 closes the gap where a single malformed-frontmatter watched-folder note crashed the entire watched-folders Settings tab + Topbar indicator. Pydantic v2's strict-validation behavior (via `extra="forbid"` + `Literal` enforcement) raises `ValidationError` on per-field type mismatch, which propagates out of `Frontmatter.from_dict` unless the caller catches it. The existing `FrontmatterError` catch handled YAML-parse-level failures but NOT Pydantic-schema-level failures. **Rule:** tool dispatchers that walk the vault and parse per-note frontmatter MUST catch both `FrontmatterError` (YAML parse failures) AND `pydantic.ValidationError` (Pydantic schema failures). The walk skips bad notes + structlog-warns; the user sees a partial result instead of a crash. Pattern: split the except into `(OSError, UnicodeDecodeError)` for silent-skip (transient I/O) and `(FrontmatterError, ValidationError)` for structlog-warn (content corruption diagnostic).

**T1 added observability for the existing FrontmatterError path (T1; NEW lesson).** Pre-T1, `FrontmatterError` was caught + silently skipped. Post-T1, it's caught + structlog-warned (alongside the new `ValidationError` branch). This is a v1.5 observability improvement bundled with the v1 crash fix. The cost is one structlog call; the benefit is that ops can diagnose vault corruption ("which notes have broken frontmatter? when did it break?") without a code change to add the warning. **Rule:** when extending an except clause to handle a new exception class, audit whether the existing branch's silent-skip should be upgraded to warn-and-skip. The new class will get the warn treatment (because that's what motivated the extension); leaving the existing class silent creates two-tier diagnostic behavior for the same conceptual failure mode (corrupted content).

**`!loaded` gate is the right shape for mount-fetch (T2.b; NEW lesson).** T2.b implementer caught a subtle improvement on the plan-doc spec. The plan-doc said `folders.length === 0 AND not in-flight`. That wording re-fetches every mount for legitimately-empty vaults (a user who has not yet enabled any watched folders), creating an infinite re-fetch loop. The `!loaded` gate (mirrors the `useDomains()` first-mount precedent at `apps/brain_web/src/lib/hooks/use-domains.ts:117-121`) tracks "has this component ever loaded?" instead of "is the store currently empty?" — fires once per session per indicator instance, then stops. **Rule:** mount-fetch gates should track "has this component ever loaded?" not "is the store currently empty?" — the former handles empty-state correctly; the latter creates infinite-refetch loops. When the plan-doc spec lands the literal wrong shape (because the plan-author didn't notice the empty-state edge case), the implementer's correct response is to surface the better shape + explain why + ship the better shape, not blindly implement the literal text.

**T2.a applied to BOTH useState init AND hydration useEffect (T2.a; NEW lesson).** T2.a implementer noticed that the watch-enable modal had TWO places that defaulted the domain dropdown: the `useState` lazy initializer (first render path) AND a post-resolve `useEffect` that re-set the domain when `domains.length > 0 && !domain` (the hydration path). If only the initializer was fixed, a rare ordering — domains hydrating AFTER modal open — would still land the modal on `domains[0]`. Fix was applied to both paths so activeDomain wins regardless of data-loading order. **Rule:** when fixing a "default value" bug, audit BOTH the initial-mount path AND any post-mount hydration / re-initialization paths in the component. Otherwise the fix is conditional on data-loading order — works in the common case, fails silently in the race-condition case. Cheap to audit; cheap to fix; expensive to surface as a re-opened bug after closure.

**Plan 22 controller chrome verification paid off (T2.a; CARRY-OVER lesson reinforced).** The activeDomain default bug was flagged at Plan 22 T15 implementer concern #2. Controller's manual chrome session at the Plan 22 T15 pause confirmed it ("Scope: Research" in topbar but modal defaulted to "personal"). T2.a closed it inside Plan 23 instead of carrying forward to Plan 24+. The Plan 22 closure section already captured the broader lesson ("manual chrome verification at T15 pause caught microcopy / UX gaps early"); Plan 23 T2.a is the practical follow-through that proves the lesson's value. **Rule:** at major frontend pause cadences, controller does a brief chrome verification (~10 minutes). The bugs surfaced at the pause are cheap to fix in the next plan; the bugs that surface only at e2e test time or in production are 10× more expensive. Plan 21 / 22 / 23 each strengthened this pattern; it's now reflexive.

**Handoff to Plan 24.** Plan 24 candidate scope per the tail block of `tasks/todo.md` — 16 items remaining from Plan 22's 19-item carry-forward block (Plan 23 closed 3: T1 critical + T2.a activeDomain + T2.b topbar mount-fetch). Breakdown: 7 UX polish (native folder picker, AbortController on confirm, cross-store reconcile, orphans-only topbar coverage gap, Open-in-Finder backend, bulk action progress, OrphanEntry fields, topbar entrance transition); 4 architectural (`_build_pipeline` 4-site dup, `_watched_folders_changed` cross-package dup, brain_api + brain_mcp parallel watchers, Stage-4 archive deferral); 1 dev-loop friction (workspace editable flip); 4 preserved NOT-DOING (seedBrainMd rule-of-three, per-thread cross-domain spec NO, topbar scope chip drift watch, PEP 703 `_cached_ctx`). These are NOT a Plan 24 commitment — Plan 24 will be authored just-in-time when triggers fire (e.g., a real ValidationError-class crash in a different tool dispatcher would re-prioritize the catch-extension pattern across the tool registry; a 4th `_build_pipeline` call site would re-prioritize the factory lift).

