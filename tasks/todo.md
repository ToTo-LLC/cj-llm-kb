# brain — Master Plan Index

> Master tracking board for the `brain` implementation. Each sub-plan is a self-contained, demoable unit. Plans are written **just-in-time**: plan N+1 is authored only after plan N is approved and reviewed, so lessons from earlier execution shape later plans.

**Spec:** [`docs/superpowers/specs/2026-04-13-cj-llm-kb-design.md`](../docs/superpowers/specs/2026-04-13-cj-llm-kb-design.md)
**Orchestration:** [`.claude/ORCHESTRATION_GUIDE.md`](../.claude/ORCHESTRATION_GUIDE.md)
**Lessons:** [`lessons.md`](./lessons.md)

## Status

| # | Plan | Status | Demoable deliverable | Primary subagent(s) |
|---|---|---|---|---|
| 01 | [Foundation](./plans/01-foundation.md) | ✅ Complete (2026-04-13, tag `plan-01-foundation`) | Tested `brain_core` library (config, vault, llm, cost) green on Mac + Windows CI | brain-core-engineer, brain-test-engineer |
| 02 | [Ingestion](./plans/02-ingestion.md) | ✅ Complete (2026-04-14, tag `plan-02-ingestion`) | 9-stage ingest pipeline with 5-source demo passing (`PLAN 02 DEMO OK`); Tasks 21–22 VCR cassettes deferred until API key available | brain-core-engineer, brain-prompt-engineer |
| 03 | [Chat](./plans/03-chat.md) | ✅ Complete (2026-04-15, tag `plan-03-chat`) | `brain chat` Ask/Brainstorm/Draft modes working in terminal with 366 tests across brain_core + brain_cli; 7-gate demo passing (`PLAN 03 DEMO OK`); VCR chat cassettes deferred per D7a | brain-core-engineer, brain-prompt-engineer |
| 04 | [MCP](./plans/04-mcp.md) | ✅ Complete (2026-04-17, tag `plan-04-mcp`) | brain_mcp stdio server with 18 tools + 3 resource URIs; brain mcp install/uninstall/selftest/status CLI; 14-gate demo passing (`PLAN 04 DEMO OK`); VCR MCP cassettes deferred per D9a | brain-mcp-engineer, brain-core-engineer |
| 05 | [API](./plans/05-api.md) | ✅ Complete (2026-04-21, tag `plan-05-api`) | brain_api FastAPI REST (18 tool endpoints) + WebSocket chat (ChatSession-bridged, schema_version=1); Origin/Host/token auth; 14-gate demo passing (`PLAN 05 DEMO OK`); VCR chat cassettes deferred per D9a | brain-api-engineer (brain-mcp-engineer role-overloaded), brain-core-engineer |
| 06 | UI Design | ✅ Complete (2026-04-21, external design tool) | Design brief + 3 delta passes + v3 design artifacts at `docs/design/` (design-brief.md, design-delta.md, design-delta-v2.md, CJ Knowledge LLM v3 zip); 5 pre-flight backend decisions pinned at `docs/design/plan-07-preflight.md` (D1a/D2a/D3a/D4a/D5a) | external (brain-ui-designer replaced with Claude design tool) |
| 07 | [Frontend](./plans/07-frontend.md) | ✅ Complete (2026-04-21, tag `plan-07-frontend`) | brain_web Next.js 15 with 8 screens + setup wizard + 15+ dialogs/overlays + Playwright e2e (5 flows + axe-core AA) + 14-gate demo passing (`PLAN 07 DEMO OK`); tool surface 18 → 34 | brain-frontend-engineer, brain-core-engineer, brain-test-engineer |
| 08 | [Install + Packaging](./plans/08-install.md) | ✅ Complete (2026-04-23, tag `plan-08-install`) | Static-export pivot (brain_api serves UI); 10 CLI verbs (start/stop/status/doctor/upgrade/uninstall/backup/chat/mcp/patches); install.sh (Mac) + install.ps1 (Windows 11); launcher icons; 11-gate demo passing (`PLAN 08 DEMO OK`); VM dry-run harness ready (clean-Mac + clean-Windows receipts deferred to pre-release sweep) | brain-installer-engineer, brain-core-engineer, brain-frontend-engineer, brain-test-engineer |
| 09 | [Ship](./plans/09-ship.md) | ✅ Complete (2026-04-24, tags `plan-09-ship` + `v0.1.0`) | v0.1.0 GitHub release live (universal tarball, SHA256 `657f9fea…`); install.sh/.ps1 flipped to real URLs; update-check nudge shipped; README + LICENSE + CONTRIBUTING + privacy; 17/17-section QA sweep receipt (`docs/testing/v0.1.0-qa-receipt.md`); 12-gate demo passing (`PLAN 09 DEMO OK`); 947+18 Python + 231+1-skip frontend unit + 14/14 Playwright; Tasks 9+10 clean-VM dry runs harnessed-deferred to pre-release validation | brain-test-engineer, brain-installer-engineer |
| 10 | [Configurable Domains](./plans/10-configurable-domains.md) | ✅ Complete (2026-04-27, tag `plan-10-configurable-domains`) | `Config.domains: list[str]` replaces v0.1 `Domain` Literal; `scope_guard` widened; classify prompt templated; Settings → Domains panel + topbar + Browse + setup wizard all consume live `useDomains()`. 9 tasks all green; demo gate (`scripts/demo-plan-10.py`) passes 7/7 (`PLAN 10 DEMO OK`). 1022 backend tests + 247 frontend vitest cases + Playwright `domains.spec.ts` ready. Closes `docs/v0.1.0-known-issues.md` item #21. | brain-core-engineer, brain-prompt-engineer, brain-frontend-engineer, brain-test-engineer |
| 11 | [Persistent Config + Per-Domain Overrides + Privacy Rail Generalization](./plans/11-persistent-config.md) | ✅ Complete (2026-04-28, tag `plan-11-persistent-config`) | Atomic+locked+backup `save_config()` shipped (closes `#27`); five mutation tools (`brain_config_set`, `brain_create_domain`, `brain_rename_domain`, `brain_delete_domain`, `brain_budget_override`) persist to `<vault>/.brain/config.json`; loader gains `config.json → .bak → defaults` fallback chain with structlog warnings. `Config.privacy_railed: list[str]` generalizes the rail (validator requires `personal`); `Config.domain_overrides: dict[str, DomainOverride]` + `resolve_llm_config()` / `resolve_autonomous_mode()` resolvers thread per-domain LLM/autonomy overrides. Frontend: `panel-domains.tsx` grows per-row override editor + privacy-rail toggle; `app-store.scopeInitialized` + `useDomains().activeDomain` give first-mount scope hydration. 10 tasks all green; 8-gate demo (`scripts/demo-plan-11.py`) prints `PLAN 11 DEMO OK`. 1134 backend pytest + 273 brain_web vitest cases + Playwright `persistence.spec.ts` (1 test, 595 ms). Plan 12 candidate scope: 4 deferred items added below. | brain-core-engineer, brain-frontend-engineer, brain-test-engineer |
| 12 | [Settings UX completion + Plan 11 correctness cleanup](./plans/12-settings-ux-and-cleanup.md) | ✅ Complete (2026-04-28, tag `plan-12-settings-ux-and-cleanup`) | `Config.cross_domain_warning_acknowledged: bool` lands as the new persisted field; `DomainOverride.autonomous_mode` and `brain_core.llm.resolve_autonomous_mode` both DELETE'd as dead code (closes Plan 11 lesson 351 user-guide drift). `brain_mcp/server.py:_build_ctx` ports the Plan 11 brain_api Config-wiring fix + production-shape stdio integration test guards the regression. Read-tool audit lands `_READ_TOOLS_THAT_THREAD_CTX_CONFIG` regression-pin contract test + fixes `brain_config_get`'s `Config()` snapshot drift. `useDomains()` promoted to a zustand-backed selector via `domains-store.ts`; cross-instance pubsub eliminates `domains.spec.ts`'s `page.reload()` workaround. `_SETTABLE_KEYS` extended for `active_domain` + `cross_domain_warning_acknowledged`; `panel-domains.tsx` grows an "Active domain" dropdown above the per-domain rows + a "Show cross-domain warning" toggle. Cross-domain confirmation modal ships at `dialogs/cross-domain-modal.tsx` with `useCrossDomainGate()` + `shouldFireCrossDomainModal()` trigger; chat-screen send is gated on the modal when scope contains ≥2 domains AND ≥1 is in `Config.privacy_railed`. 10 tasks all green; 7-gate demo (`scripts/demo-plan-12.py`) prints `PLAN 12 DEMO OK`. 1140 backend pytest + 316 brain_web vitest passed + 11/11 Playwright specs (modulo 9 pre-existing failures from clean main HEAD, not caused by Plan 12). Plan 13 candidate scope: 11 deferred items added below. | brain-core-engineer, brain-mcp-engineer, brain-frontend-engineer, brain-ui-designer, brain-test-engineer |
| 13 | [Cross-instance cleanup + pre-existing test debt closure](./plans/13-cross-instance-cleanup-and-test-debt.md) | ✅ Complete (2026-04-29, tag `plan-13-cross-instance-cleanup-and-test-debt`) | None-policy strictness lands on `list_domains` + `config_set.py:317-327` matching `config_get`'s lifecycle-violation wording (closes #A1; scope-adjacent `apply_patch.py:_resolve_config` docstring sweep landed). `panel-domains.tsx` drops parallel local `domains: string[]` state and reads from `useDomainsStore` directly — single source of truth (closes #A2; `removeDomainOptimistic` failure-mode regression captured in Plan 14). `cross-domain-gate-store.ts` lands as a zustand store mirroring Plan 12 D4's `domains-store.ts` split; `useCrossDomainGate()` becomes a selector; cross-instance pubsub between Settings toggle + chat-screen gate works without `page.reload()` (closes #A3). brain_api 13-failure root cause confirmed (Task 4): NOT the OriginHostMiddleware drift Plan 12 closure hypothesized — it's Plan 08 Task 1's `SPAStaticFiles` mount shadowing synthetic test routes when `apps/brain_web/out/` exists. Task 5 lands `mount_static_ui: bool = True` keyword-only flag on `create_app`; conftest passes `False` for the API-only test surface. All 13 tests pass; envelope shape parity regression-pin test added (5 tests; closes #B1). a11y color-contrast root cause (Task 6): NOT the gate weakening Plan 12 closure suspected — it's the v4 brand-skin.css drop cascade-shadowing Plan 07 Task 25C's tokens.css nudges. Task 6 token sweep cleared 9 violations across 8 routes + setup-wizard welcome step (closes #B2). 7 tasks all green; 7-gate demo (`scripts/demo-plan-13.py`) prints `PLAN 13 DEMO OK`. 1164 backend pytest + 11 skipped + 334 brain_web vitest + 1 skipped + 20/21 Playwright e2e (1 pre-existing `ingest-drag-drop` flake passes in isolation). Plan 14 candidate scope: see tail block below. | brain-core-engineer, brain-frontend-engineer, brain-test-engineer, brain-mcp-engineer (role-overloaded as brain-api-engineer) |
| 14 | [Hardening + CI restoration](./plans/14-hardening-and-ci.md) | ✅ Complete (2026-04-28, tag `plan-14-hardening-and-ci`) | `SPAStaticFiles.__call__` overridden to return 404 for non-http (WS / lifespan) scopes — defense-in-depth against route-ordering changes (closes #B1). `request_id` pin added to 500-envelope `detail` slot — `'request_id' in body['detail']` AND `len(...) > 0` (D4 wording-shape, not UUID-shape; closes #B2). `apps/brain_web/tests/e2e/a11y-populated.spec.ts` lands with 11 populated-state cases — 6 dialogs (rename-domain, delete-domain, fork-thread, backup-restore, cross-domain modal, patch-card edit-approve) + 2 menus (topbar scope picker, Settings tabs walk × 8) + 3 overlays (search overlay ⌘K, drop-zone overlay, toast notifications); 2 deferrals (repair-config dialog UI surface, autonomy modal) filed for Plan 15 with explicit grep-receipts (closes #C2.a + #C2.b). `.prose a` routed through `var(--tt-cyan)` in brand-skin.css — single source of truth; theme-aware (light = `#C64B2E`, dark = `#E06A4A`); hardcoded `[data-theme="dark"] .prose a:hover { #E06A4A }` removed (closes #C3). `ingest-drag-drop.spec.ts` flake closed via test-arm `waitForResponse` — production race in `inbox-store.loadRecent` separately documented for Plan 15 (closes #D8). `.github/workflows/playwright.yml` lands with macOS-14 + windows-2022 matrix; sibling-step pattern (Mac chflags step gated `runner.os == 'macOS'`, Windows pwsh step gated `runner.os == 'Windows'`); full e2e suite runs on every push + every PR — the structural fix for the cascade-shadowing class of regression (closes #C1.a + #C1.b). 9 tasks all green; 12-gate demo (`scripts/demo-plan-14.py`) prints `PLAN 14 DEMO OK`. **1169 backend pytest + 11 skipped** (rises from Plan 13's 1164 by +5 pin tests = Task 1 ws_guard + Task 2 request_id sub-test) + **334 brain_web vitest + 1 skipped** (matches Plan 13 baseline; populated-state coverage is e2e, not unit) + **32 Playwright e2e tests across 11 spec files** (rises from Plan 13's 21 by +11 `a11y-populated.spec.ts` cases). Spec footnote landed in §8 (Web UI) noting the new CI gate. Plan 15 candidate scope: see tail block below. | brain-mcp-engineer (role-overloaded), brain-frontend-engineer, brain-test-engineer, brain-installer-engineer |
| 15 | [CI green + polish pass](./plans/15-ci-and-polish.md) | ✅ Complete (2026-05-06, tag `plan-15-ci-and-polish`) | 76 ruff violations on main cleared in a single commit — first green whole-repo `ruff check .` in 4 plans (Plan 11/12/13/14 implementer recipes ran per-package only and silently masked the debt; closes #A1). Windows Playwright chat composer fix landed: Task 2 diagnosed `_spa_fallback`'s `path.split("/", 1)[0]` running on Starlette's `os.path.normpath`-transformed path (which converts `/` → `\` on Windows via ntpath); Task 3 landed `path = path.replace("\\", "/")` normalization at the top of `_spa_fallback` (closes #A2). `brain start` supervisor drops `uv run` — direct `<install>/.venv/(bin\|Scripts)/python(.exe) -m uvicorn` via new `_resolve_venv_python(install_dir)` helper (closes #B1). Modal + Settings jargon aligned to "Privacy-railed" everywhere (mirrors `Config.privacy_railed: list[str]`); new `PrivacyRailedGlossaryTooltip` component supplies the glossary tooltip both surfaces share (closes #B2). Active-domain dropdown toast — conditional CTA (validator-error → "Pick a different domain"; transport-error → "Try again") + `pushToast` moved outside catch (closes #B3). `pendingSendRef.mode` used explicitly + Task 7 review fix-up locked the canonical pattern: capture `pendingSendRef.current` into a local synchronously, clear the ref BEFORE the await, dispatch from the local (closes throw-leak + stale-mode race; closes #B4). `raise_if_no_config(ctx, tool_name)` helper extracted to `brain_core.tools._errors`; three brain_core call sites (config_get, list_domains, config_set) refactor to call it; SPAStaticFiles non-http guard stays separate per D7 (closes #B5). `_mk_ctx` test fixture alignment — all three fixtures (`test_list_domains.py`, `test_list_domains_active.py`, `test_config_set.py`) require explicit `config: Config` (closes #B6). `apply_patch._resolve_config` docstring drops the Plan 07 Task 5 forward-looking reference; describes current behavior (closes #B7). 10 tasks all green; 12-gate demo (`scripts/demo-plan-15.py`) prints `PLAN 15 DEMO OK`. **181 brain_api pytest + 3 skipped** (rises from Plan 14's 178 by +3 raise_if_no_config contract tests) + **343 brain_web vitest + 1 skipped** (rises from Plan 14's 334 by +9 across Tasks 5/6/7) + **32 Playwright e2e tests** (unchanged from Plan 14). Per D11, NO spec text changes. Plan 16 candidate scope: see tail block below. | brain-test-engineer, brain-mcp-engineer (role-overloaded as brain-api-engineer), brain-installer-engineer, brain-ui-designer, brain-frontend-engineer, brain-core-engineer |
| 17 | [Residuals + spec annotations](./plans/17-residuals-and-spec-annotations.md) | ✅ Complete (2026-05-11, tag `plan-17-residuals-and-spec-annotations`) | All 18 substantive tasks landed in 28 commits across 4 themes: (T1) brain_api → AnthropicProvider production e2e integration test gated on `ANTHROPIC_E2E=1` (CI step + 5min timeout); (T2) brain_api ConfigWatcher live `ctx.config` swap on `config.json` change via module-level `_on_config_change` callback + `object.__setattr__` on the frozen ToolContext + 4 pin tests; (T3-T5) three frontend components migrated to zustand stores — `panel-budget.tsx` via `useBudget()` + `useBudgetStore.getState().setDailyCap`; `panel-domains-row.tsx` BudgetCapsSubsection via `setDomainCap`; `domain-override-form.tsx` LLM fields via `setOverrideField`, `autonomous_mode` stays direct `configSet` (intentionally excluded from `DomainOverrideEntry` per Plan 12 D1); (T6) AutonomyCategory drift gate via shared `apps/brain_web/tests/fixtures/autonomy-categories.json` checked by both Python (`AutonomyCategoryFlags.model_fields`) + TS (`AUTONOMY_CATEGORIES`); (T7) stale `autonomous.ingest` doc/comment refs in `config_set.py:9` + `ingest.py:192` replaced with `autonomous.<slug>.new_files=true` (T40 per-domain × per-category shape); (T8) `_on_config_change` lifted from `_run` closure to module-level in `brain_mcp/__main__.py`; tests import the lifted symbol directly; (T9) `apply_patch._resolve_config` read-only contract docstring added + plan-doc audit findings appended (audit found zero mutations across all consumers); (T10) closed as **already-done** — `raise_if_no_config` migration for `brain_list_domains` shipped in Plan 15 Task 8; the named test was never failing; (T11) brain_api `mypy --strict src tests` closure (21 → 0 errors across 9 files: auth.py no-any-return real bug fix; schema.py stale `valid-type` ignore drop; 19 test annotations: `Iterator[ApiClient]`, `pytest.MonkeyPatch`, `AsyncIterator[ChatEvent]` for async-gen stubs) + new CI step `Type-check brain_api` with `timeout-minutes: 5` mirroring the brain_core step; (T12) spec §3 footnote — 5-step HYBRID gate algorithm description (member-field rule + CONCEPTS/DRAFT category rule + ANY-False rule); (T13) spec §10 sub-bullet — multi-domain per-call enforcement no-op policy (Plan 17 D4: `len(Config.domains) > 1` → `domain=None` → `PerDomainBudgetGuard.check_for` short-circuits; global caps still apply; re-litigation requires spec amendment); (T14) spec §10 — canonical `resolve_config` contract pinned (long-running consumers each hold one live shared Config ref: `app_state.ctx.tool_ctx.config` / `session.app_config` / `_cached_ctx.config`); (T15) `_lifespan` comment-debt consolidated — ~50 lines of inline tutorial comments collapsed into a single structured function docstring with named sections (Startup sequence / Shutdown sequence / Hot-reload contract / History); net -20 LOC, 187 pytest passes, mypy clean; (T16) `brain_recent` aligned on canonical `items` field across backend (`recent.py`) + browse-screen.tsx adapter shim removed; 3 test files flipped; flagged the pre-existing `RecentEntry` typed-wrapper-vs-runtime drift in `tools.ts` as Plan 18 candidate; (T17) `repair_config_apply` mutation wrapped in `persist_config_or_revert` for atomic mutation+save with snapshot revert on disk-write failure + 2 pin tests (primary patches `writer.save_config`; belt-and-suspenders companion patches the handler's local namespace via `raising=False`, captured in lessons as "monkeypatch-vs-import-binding"); (T18) three repair-config-dialog T33 nits bundled — footer DOM reordered to Re-run → Re-apply → Cancel (escape-hatch convention); hex literals in `ICON_BY_STATUS` + inline `text-[var(--danger)]` style replaced with `var(--ok/warn/danger)` tokens (confirmed at `tokens.css:121-123`); `mountedRef` guard added around 3 post-await `setState` calls in `handleRun` + 1 in `handleApply` catch (success branch only fires parent-owned callbacks); 11 → 16 unit tests (+5 for footer order, tab walk, token assertion, 2 unmount-during-await). 18-gate demo `scripts/demo-plan-17.py` prints `PLAN 17 DEMO OK`. Test count snapshot: brain_api 187 passed / 4 skipped (rises from Plan 16 baseline 183 + 3 by +4 lifespan hot-reload + e2e tests); brain_mcp 137 + 3 skipped; brain_core ~990+ (Plan 16 was 982 + 5; T6 + T17 + T18 added pin tests); vitest +~30 (5 panel-budget + 11 panel-domains-row + 14 domain-override-form + 1 autonomy drift). Three spec footnotes landed inline (§3 HYBRID algorithm, §10 multi-domain no-op, §10 resolve_config contract). One CI gate added (brain_api mypy --strict hard-fail). No schema changes, no new dependencies. Plan 18 candidate scope: see tail block below. | brain-core-engineer, brain-frontend-engineer, brain-mcp-engineer, brain-ui-designer (T18a not needed — code/comment reconciliation only) |
| 16 | [Comprehensive carry-forward](./plans/16-comprehensive-carry-forward.md) | ✅ Complete (2026-05-07, tag `plan-16-comprehensive-carry-forward`) | All 47 substantive tasks + 4 carry-forward add-ons (T28.5 / T31.5 / T34.5 / T39.5) landed against locked D1–D36 decisions (1.B full production, 2.A panel-domains 4-file split, 3.A validate_assignment 10% perf threshold/ENABLE always, 4.A ctx.config lint rule via custom AST checker). Closes the entire Plan 15 carry-forward block in a single plan: (T1) inbox-store id-keyed merge production race fix; (T2) `_spa_fallback @overload`; (T3-T8) Plan 13 follow-throughs — `useDomains()` migration of orphan consumers + `removeDomainOptimistic` + `useDomainsStore.error` banner + `domainsLoaded → loaded` rename + `BroadcastChannel` cross-tab pubsub + `panel-domains.tsx` 4-file split; (T9-T11) Plan 14 a11y deferrals — `repair-config-dialog` + `autonomy-modal` + `file-preview-overlay`; (T12-T13) CSS structural cleanup — `--tt-cyan-hover` token + stylelint `color-no-hex` rule; (T14-T18) CI follow-throughs — workflow caching + `setup-brain-test-env` composite action + actionlint pre-commit + Defender SmartScreen feature-flag + `$GITHUB_STEP_SUMMARY` duration writeback; (T19-T20) test-quality — `waitForToolResponse` helper + `afterEach` cleanup contract + `--accent-foreground` token; (T21-T25) Plan 15 review residuals trio sweep. **Theme 10 full production (T26-T46)**: per-domain budget caps (T26-T29), per-domain rate limits via `LeakyBucket` (T30-T32), repair-config dialog full polish (T33), `Config.config_version` + `loader.resolve_config` cache invalidation (T34), symmetric `ConfigWatcher` cross-process hot-reload (T35), `validate_assignment=True` on all 12 ConfigDicts (T36), per-domain × per-category autonomy with HYBRID gate + read-time backwards-compat (T37-T40), `brain config migrate` CLI (T41), "Set as default" topbar (T42), zustand promotion of budget + domain-overrides (T43), `pendingSendRef` audit (T44), `BRN001` standalone AST checker + CI hard-fail gate with zero violators (T45-T46). 47-gate demo (`scripts/demo-plan-16.py`) prints `PLAN 16 DEMO OK`. Schema gains 4 new fields (`Config.budget.per_domain`, `Config.providers[*].rate_limit_per_domain`, `Config.config_version`, `Config.autonomous` reshape from `bool` → `dict[str, AutonomyCategoryFlags]`) + `validate_assignment=True` flag. New components: `RepairConfigDialog` (full polish), `AutonomyModal`, `FilePreviewOverlay`, `PanelDomainsRow` + `PanelDomainsAdd` + `PanelDomainsActive`, `PanelAutonomous`, `BudgetStore`, `DomainOverridesStore`, `PrivacyRailedGlossaryTooltip`. New CLI: `brain config migrate`. New infrastructure: `watchdog` symmetric file-watcher, `BRN001` AST checker, leaky-bucket rate-limit enforcement, stylelint `color-no-hex` rule, actionlint pre-commit hook, composite GH action, BRN001 CI hard-fail gate, CI duration writeback. Final test counts: brain_core **982 + 5 skipped**; brain_api **183 + 3 skipped**; brain_mcp **137 + 3 skipped**; brain_cli **129**; vitest **460 + 1 skipped** (77 test files). 59 commits over Plan 16. Three spec footnotes landed (§3 Vault per-domain autonomy, §3 Vault privacy clause strengthening, §10 Safety rails per-domain budget caps + rate limits — plan doc said "§6 Cost" but §6 is "Chat and brainstorm loop"; budget content lives in §10). Plan 17 candidate scope: see tail block below — RESIDUALS ONLY (under 1.B, the v1 "bigger architectural moves" carry-forward block is empty). | brain-core-engineer, brain-mcp-engineer, brain-frontend-engineer, brain-ui-designer, brain-test-engineer, brain-installer-engineer |
| 18 | [Typed-surface drift closure](./plans/18-typed-surface-drift-closure.md) | ✅ Complete (2026-05-11, tag `plan-18-typed-surface-drift-closure`) | Closes the Plan 17 T16 typed-wrapper-vs-runtime drift carry-forward + the class of drift it surfaced. (T1) `RecentEntry` narrowed in `tools.ts` to backend reality `{path, modified_at}`; `doc-picker-dialog.tsx` scope-filter switched to path-prefix matching (`it.path.startsWith(s + "/")`) and the redundant `it.domain.toLowerCase()` search-clause dropped (closes the silent-drop / `TypeError` MEDIUM-severity production bug surfaced in Plan 17 T16 review). (T2) Sibling-drift audit of all 38 typed wrappers in `tools.ts` against backend handler `ToolResult.data` shapes appended inline to plan doc as `## T2 audit findings` (38 rows: 19 OK / 4 MINOR / 15 DRIFT). Drift-class breakdown: 11 T1-class (TS-required fields missing in backend — silently `undefined` at runtime) + 4 extra-in-backend (backend keys TS can't read without cast — harmless to current callers). 3 of the 15 DRIFT rows were LIVE consumer bugs: `recentIngests` (Inbox silently empty), `undoLast` (toast always "Reverted 0 file(s)."), `budgetOverride` (toast computed from prop default not backend value). (T3) User adjudicated tiered scope: fix all 11 T1-class DRIFTs inline; defer 4 informational extras to Plan 19. **T3.1** recentIngests narrowed to `{ingests: RecentIngestEntry[]}` + `classified_at` field + live Inbox bug closed. **T3.2** getIndex narrowed to `{domain, frontmatter, body}` + Python key-set pin. **T3.3** getBrainMd narrowed to `{exists, body}` + Python key-set pin. **T3.4** lint narrowed to Plan 09 stub `{status, message}` contract + Python key-set pin. **T3.5** ingest discriminated-union over backend `status` (3 branches: applied/pending/error) + 3 per-branch Python pins. **T3.6** rejectPatch narrowed to `{status: "rejected", patch_id, reason}` literal + Python key-set pin. **T3.7** undoLast `UndoLastData` discriminated-union (`{status: "reverted", undo_id}` vs `{status: "nothing_to_undo"}`) + 2 per-branch pins + `pending-screen.tsx` consumer narrowed via `data.status === "nothing_to_undo"` discrimination (closes live "Reverted 0 file(s)." toast bug). **T3.8** costReport narrowed to `{today_usd, month_usd, by_domain, by_mode}` + Python key-set pin. **T3.9** bulkImport `BulkImportData` discriminated-union (3 branches: refused/planned/applied) + 3 per-branch pins; eliminated consumer workaround cast at `step-pick-folder.tsx`. **T3.10** createDomain narrowed to `{status: "created", domain: {slug, name, accent_color}, note}` with fields nested under `domain` key + Python key-set pin. **T3.11** budgetOverride narrowed to `{status: "override_set", override_until, override_delta_usd, note}` + Python key-set pin + live toast bug closed. (T4) T36 stale-docstring close-as-ALREADY-DONE verified at exec time (grep across 4 phrase variants returns zero hits across `packages/brain_core/tests/`; the docstring at `test_config_set_persists.py:445` correctly describes `validate_assignment=True` as enabled per Plan 16 T36). (T5) Closure: `scripts/demo-plan-18.py` 16-gate demo prints `PLAN 18 DEMO OK`. **11 new Python key-set pin tests** added across `packages/brain_core/tests/tools/` (single + multi-branch variants depending on backend shape); zero new dependencies, zero schema changes. Plan 19 candidate scope: see tail block below (Track A REST drift / Track B UX fallout / Track C 4 deferred extras / Track D `planToFiles` type-tighten / preserved Plan 17 carry-forwards). | brain-frontend-engineer, brain-core-engineer |
| 20 | [Response-model pins + tools.ts wrapper audit](./plans/20-response-model-pins-and-wrapper-audit.md) | ✅ Complete (2026-05-12, tag `plan-20-response-model-pins-and-wrapper-audit`) | Closes Plan 19's two carry-forward candidates (#6 + #7 per user 1.A scope locking) in 4 tasks + 7 commits across 2 themes. **Theme A `response_model` pin adoption (T1).** Closed the Plan 19 T1 audit-OK-but-unpinned gap for all 4 cited BaseModels via the canonical Plan 19 T2 two-tier shape (field-set strict equality + per-field `field.annotation` + `field.is_required()` pins; heterogeneous `dict[str, Any] | None` fields skip type-annotation pin per responses.py module docstring rationale). 3 pin test files in `packages/brain_api/tests/` (test_endpoint_setup_status_shape.py — 70 LOC / 5 tests; test_endpoint_token_shape.py — 41 LOC / 2 tests; test_response_envelope_shapes.py — 117 LOC / 7 tests covering `ToolResponse` + `ErrorResponse`). 14 tests total, all PASSED in 0.02s. No drift surfaced; plan-doc citations matched main HEAD exactly. **Theme B `tools.ts` wrapper-shape audit (T2 + T3).** Audited all wrappers in `apps/brain_web/src/lib/api/tools.ts` against backend reality. Authoring-time single-line grep returned 21 matches; exec-time re-derivation per D4 surfaced **45 wrappers** (38 root + 7 fanout) — the discrepancy is single-line-grep undercount on multi-line `Promise<\n  ToolResponse<...>>` return-type declarations. Verdict breakdown: 7 OK-PROPAGATES (the 7 `configSet`-routed wrappers from Plan 19 T4, sharing explicit `ConfigSetData` named alias) + 38 OK-NARROW-INTENTIONAL (every root wrapper is intentionally narrow per the "wrapper IS the typed surface" design) + **0 DRIFT-HARDCODED**. Plan 19 T4's `7cd2b72` 7-wrapper fix is fully holding; the broader 38-root surface is drift-free by design. (T3) Collapsed to **zero-fix closure** per user's tiered AskUserQuestion at exec time (Q3=3.A). Regression check green: vitest 496 passed / 1 skipped; tsc --noEmit clean. Zero source modifications — manufacturing regression pins for a non-existent drift class would be over-engineering per CLAUDE.md "engineered enough" principle. **Closure (T4).** `scripts/demo-plan-20.py` 7-gate demo prints `PLAN 20 DEMO OK`. Zero new dependencies, zero schema changes, zero spec text changes. Test counts: brain_api +14 pin tests across 3 new files. Plan 21 candidate scope: see tail block below (4 preserved NOT-DOING + 1 deferred from Plan 20 per user 1.A `resolve_out_dir` hardening). | brain-core-engineer, brain-frontend-engineer |
| 19 | [REST-endpoint drift + typed-surface polish closure](./plans/19-rest-drift-and-typed-polish.md) | ✅ Complete (2026-05-12, tag `plan-19-rest-drift-and-typed-polish`) | Closes the Plan 18 tail-block carry-forward across 4 themes in 5 tasks + 14 commits. **Theme A REST-endpoint drift.** (T1) Audited 3 REST endpoints in `packages/brain_api/src/brain_api/endpoints/*` against TS-side consumers: 2 OK (`/api/setup-status`, `/api/token`) + 1 DRIFT (`/api/upload`, drift rate 33% vs Plan 18 T2's 39% on the MCP-tool surface; the audit-class generalizes across transports). (T2) Closed the `/api/upload` `UploadResult` drift end-to-end: TS narrowed from `{patch_id|null, applied, domain, [extra]}` → `{patch_id: string}` exactly mirroring backend `UploadResponse(BaseModel)`; two live consumer bugs at `drop-zone.tsx:64` + `app-shell.tsx:178` removed (both wrote `res.domain` which was always `undefined`/`null`); dead-code envelope fallback at `upload.ts:91-95` replaced with a defensive `ApiError("upload_envelope_invalid")` tripwire; Python pin `test_endpoint_upload_shape.py` asserts `set(UploadResponse.model_fields.keys()) == {"patch_id"}` via Pydantic introspection (lighter pattern than the handler-invocation pins Plan 18 used). T2 caught a vitest-vs-tsc lenience leak (`activeTab: "all"` literal accepted by vitest, rejected by tsc — `InboxTab` union is `"progress"|"failed"|"recent"`; fixed in `0ac02a9`) and a supplemental visual-verification recipe captured (commit `1b148a7`: uvicorn + curl proves the error-branch envelope shape even without an Anthropic key). **Theme B cost-badge UX.** (T3) Suppressed the Inbox cost badge when `source.cost > 0` at `source-row.tsx:109-115` (strict comparison defensive against `NaN`/`undefined`/`null`); breadcrumb at `inbox-store.ts:160` updated from "Plan 19 candidate" to "Plan 19 T3 (D4) resolved" so future readers see the closure cross-reference; 3 new vitest cases in `source-row.test.tsx` cover positive / `cost === 0` / `cost === undefined` branches. **Theme C 4 informational DRIFTs bundled.** (T4) Widened 4 cosmetic-severity wrappers in `tools.ts`: `recent()` → `{items, limit_used}`, `proposeNote()` → `{status, patch_id, target_path}`, `listPendingPatches()` → `{count, patches}`, `configSet()` → named `ConfigSetData` interface `{status, key, value, persisted, note}`. 7 downstream `configSet`-routed wrappers (`setDomainOverride`, `setPrivacyRailed`, `setDomainBudget`, `setDomainRateLimit`, `setDomainAutonomy`, `setActiveDomain`, `setCrossDomainWarningAcknowledged`) explicitly retyped to `Promise<ToolResponse<ConfigSetData>>` because TS covariance silently accepted the hardcoded narrow return types but IDE hover/auto-complete at each call site still showed the old shape. 4 Python key-set pin tests added in `packages/brain_core/tests/tools/` (7 test cases total covering empty/populated/branch variants). **Theme D `planToFiles` tighten.** (T5) Tightened the local helper at `step-pick-folder.tsx:57` from `Array<Record<string, unknown>>` to `readonly BulkImportPlannedItem[]`; `as unknown as Array<Record<string, unknown>>` cast at the call site removed; helper body pruned from ~40 LOC to ~17 LOC by deleting 6 dead field probes (`source`, `classified`, `domain`, `size`, `duplicate`, `skip`) that mock fixtures had carried but the real backend `bulk_import.py:196-204` `planned`-branch never emits. Plan-author drift surprise: plan-doc named `tools.ts` for the signature change but `planToFiles` is module-local inside `step-pick-folder.tsx`; implementer adapted correctly. **Closure (T6).** `scripts/demo-plan-19.py` 16-gate demo prints `PLAN 19 DEMO OK`. Zero new dependencies, zero schema changes, zero spec text changes. Test counts: brain_api +2 pin tests in `test_endpoint_upload_shape.py`; brain_core +7 pin test cases across 4 files in `tests/tools/`; brain_web +3 source-row tests + 1 drop-zone test. Plan 20 candidate scope: see tail block below (4 preserved NOT-DOING + 3 new from Plan 19 execution). | brain-frontend-engineer, brain-mcp-engineer, brain-core-engineer |

Legend: ⏸ not yet written · 📝 ready for execution · 🚧 in progress · ✅ complete

## Gate discipline

- Every plan has an explicit demo gate. No plan is marked ✅ without a proof artifact (screenshot, recording, or test-run receipt) per the "Verification Before Done" rule in `CLAUDE.md`.
- Plan 07 is hard-blocked on plan 06 approval. All other sequencing is soft — earlier plans inform later ones but can overlap where contracts are stable.
- Lessons learned during plan N feed into [`lessons.md`](./lessons.md) and influence the authoring of plan N+1.
- After every plan completion: pause for user review before starting the next.

## Workflow per plan

1. Main loop authors (or refines) the plan file under `tasks/plans/`.
2. Execution via `superpowers:subagent-driven-development` — one fresh subagent per task, two-stage review between tasks.
3. Each step marked complete only with verification proof per the `CLAUDE.md` rule.
4. On plan completion: demo artifact captured → user review → mark ✅ here → update [`lessons.md`](./lessons.md) → author next plan.

## Review cadence

- **Section-by-section** feedback within a plan (per `CLAUDE.md` plan-mode directives).
- **Plan-by-plan** feedback at demo gates.
- Decisions surfaced as `AskUserQuestion` with ≤4 labeled options, recommended first, per the user's preference format (NUMBER.LETTER).

## Plan 21 candidate scope (forwarded from Plan 20)

Plan 20 closed 2026-05-12 with 4 tasks (T1 4-BaseModel pin bundle + T2 tools.ts wrapper audit + T3 zero-fix closure + T4 closure) across 2 themes. T2 audit found 0 DRIFT-HARDCODED across 45 wrappers — Plan 19 T4's 7-wrapper fix is fully holding and the broader 38-root surface is drift-free by design. The remaining candidates are 4 preserved NOT-DOING carry-forwards (unchanged since Plan 17 / 18 / 19) plus 1 deferred from Plan 20 per user 1.A scope-locking ("Small Plan 20 = #6 + #7", NOT #5).

### Preserved Plan 17 / earlier NOT-DOING carry-forwards (unchanged)

1. **`seedBrainMd` / `seedScope` helper extraction** — rule-of-three threshold not yet met (current count = 3; threshold = 5). Lift when 5th caller appears.
2. **Per-thread cross-domain confirmation** — STAYS NOT-DOING per Plan 16 D36 spec footnote (architectural NO at the spec level, not a "we didn't get to it" deferral). Re-litigating requires a spec amendment first.
3. **Plan-text "topbar scope chip" inaccuracy drift watch** — lesson-only, captured in `tasks/lessons.md` Plan 12 closure section. The drift watch IS the deferral.
4. **Free-threaded Python (PEP 703) consideration for the brain_mcp `_cached_ctx` invalidation hook** — Plan 16 T39.5 made the no-lock decision based on the GIL guarantee. PEP 703 is opt-in in 3.13 but may become default in 3.14+; revisit when free-threaded becomes the default. No urgency.

### Deferred from Plan 20 per user 1.A scope locking

5. **`resolve_out_dir` `parents[4]` hardening against iCloud-masked workspaces** — Plan 19 T2 surprise #2 documented `packages/brain_api/src/brain_api/static_ui.py:96` silently degrading to API-only mode under uv editable install + iCloud `.pth` masking. `BRAIN_WEB_OUT_DIR` env override works as the durable workaround. Plan 13 + Plan 15 cross-platform sweeps own this seam structurally. Plan 20 explicitly deferred this candidate per user 1.A "Small Plan 20 = #6 + #7" scoping decision.

These are NOT a Plan 21 commitment — Plan 21 will be authored just-in-time once the user reviews this carry-forward list and locks scope. Plan 20's wrapper-shape audit cleared the broader 38-root surface; if Plan 19 T4's drift class re-emerges in a future plan, the Plan 20 T2 findings table is the canonical reference shape for re-running the audit.
