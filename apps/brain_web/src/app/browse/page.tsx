"use client";

// /browse — default landing (Client Component, Plan 08 Task 2).

import { BrowseScreen } from "@/components/browse/browse-screen";

export default function BrowseIndexPage(): React.ReactElement {
  return <BrowseScreen />;
}
