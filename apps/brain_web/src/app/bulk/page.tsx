"use client";

// /bulk — bulk-import surface (Client Component, Plan 08 Task 2).

import { BulkScreen } from "@/components/bulk/bulk-screen";

export default function BulkPage(): React.ReactElement {
  return <BulkScreen />;
}
