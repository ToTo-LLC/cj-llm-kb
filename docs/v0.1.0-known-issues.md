# v0.1.0 known issues

> Consolidated deferral + sweep-item list from Plans 01–08. Triage tags:
>
> - **BLOCKER** — must resolve before v0.1.0 tag. Fix inline or escalate.
> - **RELEASE_NOTE** — ship v0.1.0 with this caveat; mention in release notes; open a GitHub issue.
> - **DEFER** — post-v0.1.0 work; tracked internally; not user-facing.
>
> Generated by Plan 09 Task 2 on 2026-04-23 from `tasks/lessons.md`, plan Review
> sections (`tasks/plans/{02,03,04,05,07,08}.md`), and commit bodies since
> `plan-01-foundation`.

## Summary

- **Total items: 44** (41 original + 3 from the Plan 09 Task 11 QA sweep)
- **BLOCKER: 0**
- **RELEASE_NOTE: 5**
- **DEFER: 39** (36 original + 2 new Task 11 deferrals + 1 closed false positive)

No blockers surfaced. Plans 01–08 closed green; every deferred item was
either (a) conscious scope trimming documented in `tasks/lessons.md` at the
time of the decision, or (b) a cosmetic or future-release-only concern with
no user-observable failure mode today.

---

## BLOCKER (must resolve before v0.1.0 tag)

_None._

Plan 08 closed green (`PLAN 08 DEMO OK`, 11 gates) with 930 passing Python
tests + 16 skipped + 224 frontend unit + 14 Playwright. No safety-rail
bypass, vault-corruption path, or install-script abort was found during the
Plan 01–08 audit. The `brain doctor` command, scope-guard, VaultWriter,
undo log, and cross-platform install scripts have all been exercised end-
to-end.

---

## RELEASE_NOTE (ship + document in v0.1.0 release notes)

| # | Item | Source | GH issue | Release-note text |
|---|------|--------|----------|-------------------|
| 1 | **PDF upload through the web UI is not yet supported.** Server-side extraction works via the `brain_ingest` tool (the CLI and MCP paths accept PDFs), but the browser's drag-drop / file-picker upload route rejects binaries with a 415 response. | `tasks/lessons.md:271` (Plan 07 deferred items); commit `eaef727` ("'PDFs coming soon' toast — deferred to the Task 25 sweep per plan"); `tasks/plans/07-frontend.md` Review | _to open in Task 13_ | **PDF upload from the web UI is deferred to v0.2.0.** For now, drop PDFs into `~/Documents/brain/inbox/` and run `brain ingest` from the terminal, or use `brain chat` and let Claude ingest the file through the MCP tools. The web uploader currently accepts text, Markdown, HTML, VTT transcripts, DOCX, email (.eml), and URLs. |
| 2 | **Bulk-import folder picking uses a text input rather than a native directory picker.** The browser's `<input webkitdirectory>` reads file metadata only; there is no system-native folder dialog yet. | `tasks/lessons.md:274` (Plan 07 deferred items); `tasks/plans/07-frontend.md` Review | _to open in Task 13_ | **Bulk-import uses a path text input.** Paste or type the absolute folder path; a native folder picker lands in v0.2.0 when the desktop wrapper is added. |
| 3 | **Broken-wikilink detection is not yet surfaced in the UI.** `[[slug]]` links that point at non-existent notes render as plain text with no "unreachable" affordance. The `state.sqlite` note index has the data; a `brain_wikilink_status` tool + UI treatment were deferred. | `tasks/lessons.md:270` (Plan 07 deferred items); commit `c8bd474` ("BROKEN_WIKILINKS is an empty Set — Plan 09's brain_wikilink_status fills it"); `apps/brain_web/src/components/chat/msg-actions.tsx` stub | _to open in Task 13_ | **Broken wikilinks are not highlighted in v0.1.0.** If you reference `[[note]]` and the note does not exist, the link still renders as plain text. A dedicated lint pass + "broken link" chip lands in v0.2.0. |
| 4 | **`brain ingest` in end-to-end (E2E) mode with the bundled fake LLM can stage a patch with an absolute `target_path`.** When `BRAIN_E2E_MODE=1` is set, the canned integrate response returns `new_files=[]` and `brain_ingest` falls back to the absolute `note_path`. A subsequent `brain_apply_patch` rejects with ScopeError because the first path segment is `/`, not a domain. Real-LLM production runs are unaffected because the real integrate response populates `new_files` with a vault-relative path. | `tasks/lessons.md:297` (Plan 08 lesson); `tasks/plans/08-install.md:790` (handoff); demo-plan-08 gate 7 docstring | _to open in Task 13_ | **A known edge case with `BRAIN_E2E_MODE=1` (used only for test harnesses) can stage an ingest patch that the apply step then rejects.** Regular users running with a real API key are not affected. A workaround in `brain_apply_patch` to strip an absolute prefix that resolves under the vault is tracked for v0.1.1. |
| 5 | **Windows file-token lockdown uses a partial permission fallback.** `brain_api.auth.write_token_file` writes the per-run API secret with `os.chmod(0o600)` on POSIX (full protection) and relies on the "read-only" bit on Windows, which does not restrict reads. A `pywin32` ACL hardening pass was deferred to avoid adding the dep surface in v0.1.0. The token still rotates on every `brain start`. | `tasks/lessons.md:243` (Plan 05 deferred — Windows ACL); `packages/brain_api/src/brain_api/auth.py:66` (`TODO(Windows ACL)`); Plan 08 lessons (`tasks/lessons.md:293e`) | _to open in Task 13_ | **On Windows, the local API token file uses basic filesystem permissions rather than full ACLs.** The token still rotates every time `brain start` runs, and brain does not bind to non-loopback interfaces, so a local account on your machine is the only way to read it. Full ACL lockdown is scheduled for v0.2.0. |

---

## DEFER (future release, not user-facing)

Grouped by theme. Source attribution is the commit hash, plan file + line
number, or lessons.md line reference that first raised the item.

### Tooling / DX

| # | Item | Source | Target release |
|---|------|--------|----------------|
| 1 | Flip `editable = true` in `[tool.uv.sources]` once the upstream uv hidden-`.pth` bug is resolved. Current workaround forces `uv sync --reinstall-package <pkg>` on every source edit. | `tasks/lessons.md:98` (Plan 01); `tasks/lessons.md:150` (Plan 03); `tasks/lessons.md:287` (Plan 08) | v0.2.0 |
| 2 | Pytest `importmode=importlib` unification. Would drop the `explicit_package_bases = true` workaround that exists to de-conflict a duplicate `prompts` package between `packages/brain_mcp/tests/prompts/` and `packages/brain_api/tests/prompts/`. | `tasks/lessons.md:238` (Plan 05 Batch C) | v0.2.0 |
| 3 | Record VCR cassettes for chat / classify / summarize / integrate / ingest prompts. Infra landed in Plan 02 Task 20 + Plan 03 Task 21 + Plan 04 Task 22 + Plan 05 Task 22; cassettes themselves require a real `ANTHROPIC_API_KEY` at record time. | `tasks/lessons.md:126` (Plan 02); `tasks/lessons.md:247` (Plan 05) | v0.2.0 |
| 4 | `_TOOL_BY_NAME` dict lookup in `brain_mcp.server`. Current linear scan over all tool modules is fine at 34 tools; revisit if surface grows past ~50. `brain_api` already has `AppContext.tool_by_name`. | `tasks/lessons.md:196` (Plan 04); `tasks/lessons.md:246` (Plan 05) | backlog |
| 5 | `RateLimiter._Bucket` dataclass refactor. Current `list[float]` state works; a dataclass would strengthen mypy narrowing. Unused `self._config` field + defensive `elapsed = max(0.0, now - last)` clock-backwards guard bundled with this item. | `tasks/lessons.md:197` (Plan 04); `tasks/plans/04-mcp.md:5030` | backlog |
| 6 | Consolidate the `theme-provider` onto the Zustand store and delete the provider shim left behind by the Plan 07 Task 10 dual-path migration. | commit `490debc` (Plan 07 Task 10 — "Task 25 sweep should consolidate onto the store and delete the provider") | v0.2.0 |
| 7 | `bulk-import` response shape drift. Backend returns `data.items`; `step-pick-folder.tsx` reads `data.plan ?? data.items`. Next sweep should pick one name and deprecate the other. | `tasks/lessons.md:275` (Plan 07 deferred items) | v0.2.0 |
| 8 | `_build_ctx` in `brain_mcp.server` rebuilds `StateDB` + `BM25VaultIndex` on every call. Profile before optimizing; cache per-server-instance if slow. | `tasks/plans/04-mcp.md:5033` | backlog |
| 9 | `can_handle` Protocol's `async` keyword is vacuous — every call site awaits a pure string/path check. Changing the Protocol affects 7 handlers; defer until the next handler lands. | `tasks/lessons.md:33` (Plan 02 Task 4 review) | backlog |
| 10 | `_SETTABLE_KEYS` + JSON-Schema drift watchdog. `brain_config_set`'s allowlist was out of sync with the real schema in Plan 04; fixed in commit `3b107cd`, but a schema-vs-allowlist regression test would prevent future drift. | `tasks/lessons.md:188` (Plan 04) | v0.2.0 |
| 11 | Pre-existing `rank_bm25` / `webvtt` mypy untyped-import errors. Ignored via module-level suppressions; upstream stubs or local type stubs would clean these up. | `tasks/lessons.md:198` (Plan 04) | backlog |
| 12 | BM25 demo-scaffolding IDF quirk inherited from Plan 04 demo script. Workaround documented inline; not a production code path. | `tasks/lessons.md:199` (Plan 04); `tasks/lessons.md:242` (Plan 05) | backlog |

### Frontend polish

| # | Item | Source | Target release |
|---|------|--------|----------------|
| 13 | Monaco editor prefetch on hover of Edit button (reduce first-open jank). | commit `8cb8e40` (Plan 07 Task 18 — "Task 25 sweep items: Monaco prefetch on hover"); `tasks/lessons.md:270` area | v0.2.0 |
| 14 | Real vault name from `brain_config_get("vault_name")` rather than passing it in as a caller prop (currently defaults to `"brain"`). | commit `8cb8e40` (Plan 07 Task 18) | v0.2.0 |
| 15 | Proper LCS diff renderer for patch review. Current view treats every line as an add; edits use a naive line-by-line compare. | commit `7a59f44` (Plan 07 Task 16 — "proper LCS is a Task 25 sweep item") | v0.2.0 |
| 16 | Quote-message action wires into composer-store with `"> "` prefix. Current stub logs to the console. | commit `c8bd474` (Plan 07 Task 14); `apps/brain_web/src/components/chat/msg-actions.tsx:83` | v0.2.0 |
| 17 | Thread export action wires to a `brain_export_thread` tool (not yet authored). Current stub logs to the console. | `apps/brain_web/src/components/chat/chat-sub-header.tsx:52` | v0.2.0 |
| 18 | `brain_list_threads` tool for the left-nav recent-chats panel (currently client-side state only). | `tasks/lessons.md:272` (Plan 07 deferred items) | v0.2.0 |
| 19 | `edit-approve-dialog` three-step chain wiring (Task 16 intent documented at the top of the file). | `apps/brain_web/src/components/dialogs/edit-approve-dialog.tsx:68` | v0.2.0 |
| 20 | Setup wizard: wire `apiKey` to `configSet` once setup auth lands. Currently the input is captured but not persisted through the config write path. | `apps/brain_web/src/components/setup/wizard.tsx:46`; `apps/brain_web/src/components/setup/steps/api-key.tsx:14` | v0.2.0 |

### Backend / tools surface

| # | Item | Source | Target release |
|---|------|--------|----------------|
| 21 | `ClassifyOutput.domain` is a hardcoded `Literal["research","work","personal"]`. Blocks user-configurable domains. | `tasks/lessons.md:18` (Plan 02 Task 17 review); `tasks/lessons.md:128` (Plan 02 handoff) | v0.2.0 |
| 22 | ~~`failures.record_failure` silently overwrites on slug collision. Adding a `{slug}.{timestamp}.error.json` naming convention would preserve retry history.~~ **Fixed 2026-04-24** — naming is now `{slug}.{ts}.error.json` (compact UTC timestamp, microsecond precision). | `tasks/lessons.md:20` (Plan 02) | ~~backlog~~ shipped |
| 23 | URL handler's `timeout=30.0` and PDF handler's `min_chars=200` are hardcoded magic numbers. Plumb via config once a handler-layer config design is agreed. | `tasks/lessons.md:59` (Plan 02 Task 4); `tasks/lessons.md:128` (Plan 02 handoff) | v0.2.0 |
| 24 | Related-notes retrieval for the ingest `_integrate` step is still stubbed. Lands when retrieval gets a second iteration. | `tasks/lessons.md:128` (Plan 02 handoff); `packages/brain_core/src/brain_core/ingest/pipeline.py:263` | v0.2.0 |
| 25 | ~~UndoLog parser uses `END_PREV` sentinel; should slice by the written `PREV_LEN` byte count. No failure case under current vault content policy.~~ **Fixed 2026-04-24** — parser now consumes exactly `PREV_LEN` characters of content; `END_PREV` is tolerated for backward compatibility with older undo records but no longer required. | `tasks/lessons.md:29` (Plan 01 Task 14 review) | ~~backlog~~ shipped |
| 26 | ~~`VaultWriter` rollback hardening — wrap individual rollback steps in try/except, persist undo records before mutations complete, clear `Receipt.applied_files` on rollback. Non-blocking robustness.~~ **Fixed 2026-04-24** — all three: rollback steps are wrapped (errors attached as exception notes); undo record is now persisted before each mutation; receipt's `applied_files` and `undo_id` are cleared on rollback. | `tasks/lessons.md:31` (Plan 01 Task 13 review) | ~~v0.2.0~~ shipped |
| 27 | `brain_config_set` in-memory-only. Persistence was explicitly deferred to Plan 07, and the Settings panel now writes via `brain_config_set`, but the config-file write path still needs verification end-to-end. | `tasks/plans/04-mcp.md:5035`; `packages/brain_mcp/src/brain_mcp/server.py:130` | v0.2.0 |
| 28 | ~~`BulkImporter.plan()` doesn't expose a `max_files` kwarg. MCP layer truncates post-classify, wasting classifier calls when the caller already knows the cap.~~ **Fixed 2026-04-24** — `max_files` is a first-class kwarg on `plan()` and short-circuits the walk before classify is invoked. The MCP layer's post-classify slice is gone. | `tasks/lessons.md:195` (Plan 04 deferred items) | ~~v0.2.0~~ shipped |
| 29 | ~~Ingest-pricing wiring — stage-tagged cost events exist (Plan 07 Task 3) but the pricing path for ingest specifically is flagged with a `TODO(plan-07+)`.~~ **Fixed 2026-04-24** — `ingest_history.cost_usd` now sums classify + summarize + integrate spend (priced via `BudgetEnforcer.estimate_cost`). Partial-spend exit paths (QUARANTINED, FAILED after partial work) record their accumulated cost too. | `packages/brain_core/src/brain_core/ingest/pipeline.py:356` | ~~v0.2.0~~ shipped |
| 30 | ~~Dedicated `ChatMode.MCP` value so MCP-origin `propose_note` / `ingest` calls can be distinguished from chat-origin calls in transcripts. Currently both reuse `ChatMode.BRAINSTORM`.~~ **Fixed 2026-04-24** — `ChatMode.MCP` is a tagging value (no `MODES` entry, not a session mode); `brain_propose_note` and `brain_ingest` both stamp pending envelopes with it. | `packages/brain_core/src/brain_core/tools/propose_note.py:12`; `packages/brain_core/src/brain_core/tools/ingest.py:154` | ~~v0.2.0~~ shipped |
| 31 | ~~Classify model string pulled from `LLMConfig.classify_model`. Currently hardcoded in two places (`tools/classify.py` + `tools/ingest.py`).~~ **Fixed 2026-04-24** — `ToolContext` now carries an optional `config` field; `classify`, `ingest`, and `bulk_import` resolve `classify_model` (and `default_model` for summarize/integrate) from `ctx.config.llm` when present, falling back to the previous hardcoded constants when None. | `packages/brain_core/src/brain_core/tools/classify.py:47`; `packages/brain_core/src/brain_core/tools/ingest.py:60` | ~~v0.2.0~~ shipped |
| 32 | ~~Request-id correlation on 500 bodies. Plan 07 sketch added the middleware hook; the 500 handler still needs to merge the request-id into `detail`.~~ **Fixed 2026-04-24** — landed `RequestIDMiddleware` (the "sketch" turned out not to exist; built it as a pure-ASGI middleware matching the `OriginHostMiddleware` shape). Every response carries `X-Request-ID`; the 500 catch-all surfaces the same id under `detail.request_id`; caller-supplied `X-Request-ID` headers are honored end-to-end. | `tasks/lessons.md:245` (Plan 05); `packages/brain_api/src/brain_api/errors.py:166` | ~~v0.2.0~~ shipped |
| 33 | ~~IPv6 loopback (`::1`) support in `_LOOPBACK_HOSTS`. Defer until uvicorn exposes IPv6 and the tests exercise it.~~ **Fixed 2026-04-24** — added `"::1"` to `_LOOPBACK_HOSTS` plus a new `_extract_hostname` helper that correctly strips brackets from IPv6 Host headers (`[::1]:4317` → `::1`). Tested via three new HTTP-level cases (with/without port, public-IPv6 rejection) and a unit-test class for the hostname parser. | `tasks/lessons.md:244` (Plan 05); `packages/brain_api/src/brain_api/auth.py:30` | ~~v0.2.0~~ shipped |

### Cross-platform

| # | Item | Source | Target release |
|---|------|--------|----------------|
| 34 | fnm Windows installer trips Defender SmartScreen on some pristine machines. Documented in the VM host instructions; Plan 09 VM dry runs (Tasks 9 + 10) are the verification artifact. | `tasks/lessons.md:285` (Plan 08); `docs/testing/clean-windows-vm-host-instructions.md` | v0.2.0 (signed installer) |
| 35 | Code signing — macOS notarization and Windows Authenticode. Explicitly out of scope for v0.1.0. | `tasks/plans/09-ship.md:64` (out-of-scope declaration) | v0.2.0+ |
| 36 | Native bundles (Electron / Tauri / py2app). Tarball + install script is the v0.1.0 distribution. | `tasks/plans/09-ship.md:65` | roadmap |

### v0.1.0 Task 11 QA sweep deferrals (2026-04-24)

Plan 09 Task 11's live Claude-in-Chrome sweep surfaced 5 fixable findings
(all shipped) + 3 intentional DEFERs. The DEFERs below have no user-facing
failure today; they're cosmetic or scoped to a future design call.

| # | Item | Source | Target release |
|---|------|--------|----------------|
| 42 | **a11y: theme cards + autonomous toggles concatenate title + description into a single accessible name** — e.g. `"Researchreading · papers"`. Needs a design decision on whether to use an explicit `aria-label` per card, restructure the DOM so title + description render as separate `<label>` + `aria-describedby` nodes, or drop the description from the a11y name entirely. Screen-reader impact is minor (all three options are audible); the read is ugly, not wrong. | Plan 09 Task 11 QA sweep finding F6 (2026-04-24) | v0.2.0 |
| 43 | **polish: Backup panel surfaces raw timestamp ID next to human-readable date** — the dual render (`20260421T184213Z` + `Apr 21 2026, 6:42 pm`) is intentional today (ID is the filename on disk, copy-pasteable), but the design may want to hide the ID behind a "Details" toggle so the default view is lighter. Not a bug. | Plan 09 Task 11 QA sweep finding F10 (2026-04-24) | backlog |
| 44 | **(closed — false positive)** F12 mode-button a11y sweep flagged a missing `aria-checked` on the Ask/Brainstorm/Draft mode buttons; re-read confirmed `role="radio"` + `aria-checked` are both present on the `<ModeButton />` component. No action. | Plan 09 Task 11 QA sweep finding F12 (closed false positive, 2026-04-24) | — |

### Documentation

| # | Item | Source | Target release |
|---|------|--------|----------------|
| 37 | Plan-authoring retrospective lessons: `Task 5` fixture-under-threshold spec bug, `Task 10` title case-sensitivity spec bug, `is`-vs-`==` on round-tripped pydantic models, `len == 4` turn-count fragility. Captured as rules in `tasks/lessons.md` but never consolidated into a plan-authoring style guide. | `tasks/lessons.md:37` (Plan 02 Task 5); `tasks/lessons.md:39` (Plan 02 Task 10); `tasks/lessons.md:156`/`:158` (Plan 03); `tasks/lessons.md:221` (Plan 05) | v0.2.0 |
| 38 | `base.py` dead `register()`/`HANDLERS` cleanup. Dispatcher instantiates handlers explicitly via `_default_handlers()`; the legacy registration pattern is unused. | `tasks/lessons.md:65` (Plan 02 Task 3 review) | backlog |
| 39 | `brain_api` vs `brain_mcp` module layering — Plan 05 Task 14 moved `ToolContext` + `RateLimiter` + `scope_guard_path` + `text_result` into `brain_core`; the `brain_mcp` copy is now a re-export shim. The re-export could be deleted if the import sites were fixed up (low priority, compatibility-oriented). | `tasks/lessons.md:213` (Plan 05) | backlog |
| 40 | `anthropic` SDK could be split into a separate `brain_llm` package. Current layering keeps it inside `brain_core` behind the `LLMProvider` abstraction. Revisit when a second provider lands. | `tasks/lessons.md:67` (Plan 01 Task 2 review) | v1.0 |
| 41 | Demo video (Q4c decision). Plan 09 ships with 6–8 stills only (Q4b). A narrated walkthrough video lands with v0.2.0. | `tasks/plans/09-ship.md:68` (out-of-scope declaration) | v0.2.0 |

---

## Appendix — raw source of truth

Each line above traces back to one of the following primary sources so a
future auditor can verify without re-reading the full plan history:

- **`tasks/lessons.md`** — the canonical running log. Plan 01 entries at
  lines 96–112, Plan 02 at 114–128, Plan 03 at 130–164, Plan 04 at
  166–200, Plan 05 at 202–247, Plan 07 at 249–275, Plan 08 at 277–297.
  Every DEFER item in this document cites the lessons.md line where the
  deferral was first recorded.
- **`tasks/plans/*.md` Review sections** — each plan closes with a Review
  heading that pins final stats and handoff bullets. Referenced directly
  where a DEFER item originated in the Review rather than during task
  execution: `03-chat.md:4140`, `04-mcp.md:5122`, `05-api.md:6203`,
  `07-frontend.md:4324`, `08-install.md:680`.
- **`tasks/plans/*.md` Task 25 / hardening-sweep sections** — surface the
  full intake list of deferred items per plan: `03-chat.md:4032` (Task
  24), `04-mcp.md:4994` (Task 25), `05-api.md:5930` (Task 25), `07-
  frontend.md:4226` (Task 25).
- **Commit bodies since `plan-01-foundation`** — several items are
  documented only in the commit message body. Notable:
  - `3c228a3` — Plan 07 Task 25A sweep (10 tools scope-expanded).
  - `5222ee5` — Plan 07 Task 25C — `BRAIN_E2E_MODE` backdoor + color-contrast.
  - `4913a5c` — Plan 04 Task 25 — `VaultWriter.apply` absolutize fix.
  - `c07d119` — Plan 04 Task 25 — selftest env-from-config fix.
  - `3b107cd` — Plan 04 Task 25 — `config_set` allowlist alignment.
  - `7bfda42` — Plan 05 Task 25 Batch A — JSON-Schema min/max/enum.
  - `22814b7` — Plan 05 Task 25 Batch A — middleware 403 envelope shape parity.
  - `aa93335` — Plan 05 Task 25 Batch A — typed WS test helper (18 mypy errors).
  - `80d4b15` — Plan 08 Task 10+11 — VM dry-run harness (deferred to Plan 09).
- **In-source `TODO(Plan NN)` markers** — grep-discoverable pins:
  `packages/brain_api/src/brain_api/auth.py:30` (IPv6),
  `packages/brain_api/src/brain_api/auth.py:66` (Windows ACL),
  `packages/brain_api/src/brain_api/errors.py:166` (request-id),
  `packages/brain_mcp/src/brain_mcp/server.py:130` (config cache
  invalidation), `packages/brain_core/src/brain_core/tools/classify.py:47`
  and `tools/ingest.py:60` (classify-model config),
  `packages/brain_core/src/brain_core/tools/propose_note.py:12` and
  `tools/ingest.py:154` (ChatMode.MCP),
  `packages/brain_core/src/brain_core/ingest/pipeline.py:263` and
  `:356` (related-notes retrieval + ingest pricing).
- **Frontend `TODO(Task ...)` markers** —
  `apps/brain_web/src/components/setup/wizard.tsx:46`,
  `apps/brain_web/src/components/setup/steps/api-key.tsx:14`,
  `apps/brain_web/src/components/chat/chat-sub-header.tsx:52`,
  `apps/brain_web/src/components/chat/msg-actions.tsx:83`,
  `apps/brain_web/src/components/dialogs/edit-approve-dialog.tsx:68`.

**Items deliberately not included:**

- "Items still deferred" mentioned in `tasks/lessons.md:27` (Plan 02
  handler-hardening sweep) — these were all closed by the subsequent
  post-Task-10 sweep (`f36f223` / `cd8aad0` / `f9553b9`). Nothing left
  user-facing.
- Plan 03 Task 24 "Batch D 5 NICE-TO-HAVEs deferred to Plan 07 or later"
  (`tasks/lessons.md:164`) — the specific five were folded into the
  DEFER theme groups above (items 13, 14, 15, 16 frontend polish; items
  30 tools surface; the theme-provider consolidation is item 6). No
  additional unfiled items remain from Batch D.
- Items closed inline during later plans (e.g. Plan 04's VaultWriter
  relative-path bug fixed in `4913a5c`, Plan 05's middleware 403 shape
  fixed in `22814b7`, Plan 07's color-contrast gate closed in `5222ee5`).
  Those are not open issues; they appear in the appendix only as the
  commit citation for a related DEFER item.
